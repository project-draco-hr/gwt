{
  PerfLogger.start("Permutation #" + permNum);
  try {
    TreeLogger branch=logger.branch(TreeLogger.TRACE,"Permutation #" + permNum);
    logProperties(branch,perm.getPropertyOracles());
    return JavaToJavaScriptCompiler.compilePermutation(branch,unifiedAst,perm.getRebindAnswers());
  }
  finally {
    PerfLogger.end();
  }
}
