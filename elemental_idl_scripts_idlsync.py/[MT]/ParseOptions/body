def ParseOptions():
    parser = optparse.OptionParser()
    parser.add_option('--revision', '-r', dest='revision', help='Revision to install', default=None)
    (args, _) = parser.parse_args()
    return args.revision
