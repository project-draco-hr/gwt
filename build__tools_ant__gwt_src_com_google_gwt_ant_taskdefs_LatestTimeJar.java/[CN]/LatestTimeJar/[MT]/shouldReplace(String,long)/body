{
  EntryInfo oldInfo=paths.get(path);
  long existingTouchTime=((oldInfo != null) ? oldInfo.getLastModified() : -1L);
  return (existingTouchTime < touchTime || (existingTouchTime == touchTime && this.duplicate.equals("add")));
}
