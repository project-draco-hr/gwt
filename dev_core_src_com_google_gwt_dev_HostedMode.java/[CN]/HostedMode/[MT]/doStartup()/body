{
  if (!super.doStartup()) {
    return false;
  }
  getTopLogger().log(TreeLogger.WARN,"The class " + HostedMode.class.getName() + " is deprecated and will be removed -- use "+ DevMode.class.getName()+ " instead");
  return true;
}
