{
  if (fFileType != null) {
    String fileName=fFileBase + "." + fFileType+ ".xml";
    if (!runBooklet(new File(fOutDir,fileName)))     return false;
  }
  transformExistingIntoTopicXml();
  if (fGenerateHtml) {
    if (!genHtml())     return false;
  }
  return true;
}
