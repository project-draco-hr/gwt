{
  if (closureCompilerFormatEnabled) {
    return getPrototypeQualifierOf(type,type.getSourceInfo());
  }
 else {
    return constructInvocation(sourceInfo,"JavaClassHierarchySetupUtil.getClassPrototype",(JsExpression)transform(getRuntimeTypeReference(type)));
  }
}
