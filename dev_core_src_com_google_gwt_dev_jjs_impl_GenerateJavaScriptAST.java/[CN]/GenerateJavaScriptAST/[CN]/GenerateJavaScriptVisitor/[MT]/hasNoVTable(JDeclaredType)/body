{
  return type instanceof JInterfaceType || program.isRepresentedAsNativeJsPrimitive(type) || type.isJsoType()|| !program.typeOracle.isInstantiatedType(type)|| type.isJsNative()|| type.isJsFunction();
}
