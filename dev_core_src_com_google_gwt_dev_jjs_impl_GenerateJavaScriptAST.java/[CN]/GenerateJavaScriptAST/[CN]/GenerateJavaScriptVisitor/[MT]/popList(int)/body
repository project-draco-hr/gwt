{
  JsNode[] array=new JsNode[count];
  while (count > 0) {
    array[--count]=pop();
  }
  List list=new ArrayList();
  for (int i=0; i < array.length; i++) {
    JsNode item=array[i];
    if (item != null) {
      list.add(item);
    }
  }
  return list;
}
