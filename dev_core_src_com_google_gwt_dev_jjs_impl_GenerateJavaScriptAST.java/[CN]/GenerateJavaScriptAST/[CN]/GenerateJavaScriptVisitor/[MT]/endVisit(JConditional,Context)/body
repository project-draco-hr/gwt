{
  JsExpression elseExpr=(JsExpression)pop();
  JsExpression thenExpr=(JsExpression)pop();
  JsExpression ifTest=(JsExpression)pop();
  push(new JsConditional(x.getSourceInfo(),ifTest,thenExpr,elseExpr));
}
