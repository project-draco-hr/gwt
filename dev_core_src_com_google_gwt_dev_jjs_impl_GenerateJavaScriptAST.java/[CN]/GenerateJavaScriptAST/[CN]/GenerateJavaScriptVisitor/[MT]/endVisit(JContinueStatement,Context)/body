{
  JsNameRef labelRef=null;
  if (x.getLabel() != null) {
    JsLabel label=(JsLabel)pop();
    labelRef=label.getName().makeRef(x.getSourceInfo());
  }
  push(new JsContinue(x.getSourceInfo(),labelRef));
}
