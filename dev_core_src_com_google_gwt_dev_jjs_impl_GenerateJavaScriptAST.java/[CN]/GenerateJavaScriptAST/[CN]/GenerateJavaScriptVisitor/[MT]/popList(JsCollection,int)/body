{
  JsNode[] array=new JsNode[count];
  while (count > 0) {
    array[--count]=pop();
  }
  for (int i=0; i < array.length; i++) {
    JsNode item=array[i];
    if (item != null) {
      collection.addNode(item);
    }
  }
}
