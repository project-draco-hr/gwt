{
  JsIf stmt=new JsIf();
  if (x.elseStmt != null) {
    stmt.setElseStmt((JsStatement)pop());
  }
  if (x.thenStmt != null) {
    stmt.setThenStmt((JsStatement)pop());
  }
 else {
    stmt.setThenStmt(jsProgram.getEmptyStmt());
  }
  stmt.setIfExpr((JsExpression)pop());
  push(stmt);
}
