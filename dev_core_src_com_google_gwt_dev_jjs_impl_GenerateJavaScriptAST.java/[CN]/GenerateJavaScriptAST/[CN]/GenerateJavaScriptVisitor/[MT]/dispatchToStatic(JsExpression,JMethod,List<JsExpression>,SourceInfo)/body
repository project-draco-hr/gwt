{
  JsNameRef methodName=method.isJsNative() ? createJsQualifier(method.getQualifiedJsName(),sourceInfo) : names.get(method).makeRef(sourceInfo);
  JsExpression result=new JsInvocation(sourceInfo,methodName,args);
  return createCommaExpression(unnecessaryQualifier,result);
}
