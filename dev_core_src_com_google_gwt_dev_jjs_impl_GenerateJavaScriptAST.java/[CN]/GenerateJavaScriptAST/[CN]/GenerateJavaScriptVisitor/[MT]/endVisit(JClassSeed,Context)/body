{
  if (!program.typeOracle.isInstantiatedType(x.getRefType()) || program.isJavaScriptObject(x.getRefType()) || x.getRefType().equals(program.getTypeJavaLangString())) {
    push(jsProgram.getNullLiteral());
  }
 else {
    push(names.get(x.getRefType()).makeRef(x.getSourceInfo()));
  }
}
