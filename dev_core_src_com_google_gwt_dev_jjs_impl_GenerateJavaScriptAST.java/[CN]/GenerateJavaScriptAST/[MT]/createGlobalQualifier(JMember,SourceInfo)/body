{
  if (isWindow(member.getJsNamespace())) {
    return JsUtils.createQualifiedNameRef(member.getQualifiedJsName().substring(WINDOW.length() + 1),sourceInfo);
  }
  return createGlobalQualifier(member.getQualifiedJsName(),sourceInfo);
}
