{
  assert(specialObfuscatedIdents.containsKey(x.getName()));
  if (obfuscate) {
    return specialObfuscatedIdents.get(x.getName());
  }
 else   if (prettyNames) {
    return x.getName() + "$";
  }
 else {
    return mangleName(x) + "$";
  }
}
