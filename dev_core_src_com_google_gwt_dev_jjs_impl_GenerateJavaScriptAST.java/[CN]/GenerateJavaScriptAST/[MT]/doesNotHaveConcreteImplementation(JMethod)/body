{
  return method.isAbstract() || JjsUtils.isUnnecessarySyntheticAccidentalOverride(method) || (JProgram.isClinit(method) && method.getEnclosingType().getClinitTarget() != method.getEnclosingType());
}
