{
  if (obfuscate) {
    return specialObfuscatedIdents.get(x.getName());
  }
 else   if (prettyNames) {
    return x.getName() + "$";
  }
 else {
    return "java_lang_Object_" + getNameString(x) + "$";
  }
}
