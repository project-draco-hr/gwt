{
  if (widget == null) {
    throw new IllegalArgumentException("Widget must not be null");
  }
  if (widgets[0] == widget) {
    setWidget(0,null);
    return true;
  }
 else   if (widgets[1] == widget) {
    setWidget(1,null);
    return true;
  }
  return false;
}
