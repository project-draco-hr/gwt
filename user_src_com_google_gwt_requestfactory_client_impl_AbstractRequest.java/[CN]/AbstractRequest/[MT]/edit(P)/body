{
  P returnRecordImpl=(P)((ProxyImpl)record).schema().create(((ProxyImpl)record).asJso(),((ProxyImpl)record).unpersisted());
  ((ProxyImpl)returnRecordImpl).putDeltaValueStore(deltaValueStore);
  return returnRecordImpl;
}
