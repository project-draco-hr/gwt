{
  if (o instanceof ProxyImpl) {
    ProxyImpl proxy=(ProxyImpl)o;
    if (!proxy.mutable()) {
      @SuppressWarnings("unchecked") Q editable=(Q)edit(proxy);
      return editable;
    }
  }
  return o;
}
