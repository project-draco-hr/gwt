{
  for (  String token : related.getHistoryTokens()) {
    SimpleEntityProxyId<EntityProxy> id=requestContext.getRequestFactory().getProxyId(token);
    requestContext.processReturnRecord(id,related.getReturnRecord(token));
  }
}
