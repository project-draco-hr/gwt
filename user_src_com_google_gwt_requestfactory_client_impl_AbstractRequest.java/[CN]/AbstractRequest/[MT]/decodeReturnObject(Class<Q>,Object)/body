{
  ReturnRecord jso=(ReturnRecord)obj;
  SimpleEntityProxyId<Q> id=requestContext.getRequestFactory().getId(clazz,jso.getSimpleId());
  Q proxy=requestContext.processReturnRecord(id,(ReturnRecord)obj,WriteOperation.UPDATE);
  return proxy;
}
