{
  JType streamReaderType=typeOracle.findType(streamTypeName);
  if (streamReaderType == null) {
    throw new RuntimeException("Could not find a definition for " + streamTypeName);
  }
  return new JType[]{streamReaderType,serializee};
}
