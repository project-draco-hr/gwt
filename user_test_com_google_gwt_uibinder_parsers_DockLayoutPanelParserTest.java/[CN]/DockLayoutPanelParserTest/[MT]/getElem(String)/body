{
  doc=docHelper.documentFor(string);
  Element w3cElem=(Element)doc.getDocumentElement().getElementsByTagName("g:DockLayoutPanel").item(0);
  XMLElement elem=elemProvider.get(w3cElem);
  return elem;
}
