{
  if (type instanceof JReferenceType) {
    JReferenceType refType=(JReferenceType)type;
    if (program.typeOracle.canTriviallyCast(refType,program.getJavaScriptObject())) {
      rescue(refType,true,true);
    }
  }
}
