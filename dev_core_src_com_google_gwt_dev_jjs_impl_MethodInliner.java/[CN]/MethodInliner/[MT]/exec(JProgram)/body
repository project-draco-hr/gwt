{
  Event optimizeEvent=SpeedTracerLogger.start(CompilerEventType.OPTIMIZE,"optimizer","MethodInliner");
  boolean didChange=new MethodInliner(program).execImpl();
  optimizeEvent.end("didChange","" + didChange);
  return didChange;
}
