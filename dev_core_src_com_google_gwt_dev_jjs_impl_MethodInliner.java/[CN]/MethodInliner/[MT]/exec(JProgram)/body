{
  SpeedTracerLogger.start(CompilerEventType.OPTIMIZE,"optimizer","MethodInliner");
  boolean didChange=new MethodInliner(program).execImpl();
  SpeedTracerLogger.end(CompilerEventType.OPTIMIZE,"didChange","" + didChange);
  return didChange;
}
