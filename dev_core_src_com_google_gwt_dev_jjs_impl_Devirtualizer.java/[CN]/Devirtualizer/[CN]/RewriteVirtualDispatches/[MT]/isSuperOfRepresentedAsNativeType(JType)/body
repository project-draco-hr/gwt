{
  for (  JClassType type : program.getRepresentedAsNativeTypes()) {
    if (program.typeOracle.isInstantiatedType(type) && program.typeOracle.castSucceedsTrivially(type,targetType)) {
      return true;
    }
  }
  return false;
}
