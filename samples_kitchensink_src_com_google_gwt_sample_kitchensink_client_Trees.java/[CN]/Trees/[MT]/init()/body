{
  return new SinkInfo("Trees","GWT has a built-in <code>Tree</code> widget.  " + "The tree is focusable and has keyboard support as well."){
    public Sink createInstance(){
      return new Trees();
    }
  }
;
}
