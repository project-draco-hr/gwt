{
  try {
    if (moduleName == null) {
      handleUnload(frameWnd);
      return true;
    }
    int moduleID=++nextModuleID;
    Integer key=new Integer(moduleID);
    setIntProperty(frameWnd,"__gwt_module_id",moduleID);
    ModuleSpaceHost msh=getHost().createModuleSpaceHost(BrowserWidgetIE6.this,moduleName);
    ModuleSpaceIE6 moduleSpace=new ModuleSpaceIE6(msh,frameWnd,moduleName,key);
    attachModuleSpace(moduleSpace);
    return true;
  }
 catch (  Throwable e) {
    getHost().getLogger().log(TreeLogger.ERROR,"Failure to load module '" + moduleName + "'",e);
    return false;
  }
}
