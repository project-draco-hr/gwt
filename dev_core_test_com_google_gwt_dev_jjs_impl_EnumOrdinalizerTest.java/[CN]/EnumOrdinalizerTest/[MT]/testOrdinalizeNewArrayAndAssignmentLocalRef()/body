{
  EnumOrdinalizer.startTracker();
  setupFruitEnum();
  optimize("void","Fruit[] fruits = new Fruit[] {Fruit.APPLE, Fruit.ORANGE, Fruit.APPLE};","if (fruits[0] == Fruit.APPLE) {","  fruits[0] = Fruit.ORANGE;","}");
  EnumOrdinalizer.Tracker tracker=EnumOrdinalizer.getTracker();
  EnumOrdinalizer.stopTracker();
  assertTrue(tracker.isOrdinalized("test.EntryPoint$Fruit"));
}
