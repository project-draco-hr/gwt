{
  try {
    return InetAddress.getLocalHost().getHostAddress() + ":" + browserChannelPort;
  }
 catch (  UnknownHostException e) {
    throw new RuntimeException("Unable to determine my ip",e);
  }
}
