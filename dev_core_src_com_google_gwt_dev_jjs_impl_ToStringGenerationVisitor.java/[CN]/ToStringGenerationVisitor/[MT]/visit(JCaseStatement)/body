{
  if (x.getExpression() != null) {
    print(CHARS_CASE);
    x.getExpression().traverse(this);
  }
 else {
    print(CHARS_DEFAULT);
  }
  print(':');
  space();
  fNeedSemi=false;
  return false;
}
