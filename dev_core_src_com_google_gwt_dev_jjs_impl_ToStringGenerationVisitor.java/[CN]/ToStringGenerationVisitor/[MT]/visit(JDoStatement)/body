{
  print(CHARS_DO);
  if (x.body != null) {
    nestedStatementPush(x.body);
    x.body.traverse(this);
    nestedStatementPop(x.body);
  }
  if (fNeedSemi) {
    semi();
    newline();
  }
 else {
    space();
    fNeedSemi=true;
  }
  print(CHARS_WHILE);
  lparen();
  x.getTestExpr().traverse(this);
  rparen();
  return false;
}
