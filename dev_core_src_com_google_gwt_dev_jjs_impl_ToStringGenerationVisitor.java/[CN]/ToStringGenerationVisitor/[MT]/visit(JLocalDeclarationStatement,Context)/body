{
  if (!suppressType) {
    accept(x.getLocalRef().getTarget());
  }
 else {
    accept(x.getLocalRef());
  }
  JExpression initializer=x.getInitializer();
  if (initializer != null) {
    print(" = ");
    accept(initializer);
  }
  return false;
}
