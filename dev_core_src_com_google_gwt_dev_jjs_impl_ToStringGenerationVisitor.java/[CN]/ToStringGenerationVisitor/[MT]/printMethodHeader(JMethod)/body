{
  if (x.isPrivate()) {
    print(CHARS_PRIVATE);
  }
 else {
    print(CHARS_PUBLIC);
  }
  printStaticFlag(x);
  printAbstractFlag(x);
  printNativeFlag(x);
  printFinalFlag(x);
  printType(x);
  space();
  printName(x);
  printParameterList(x);
}
