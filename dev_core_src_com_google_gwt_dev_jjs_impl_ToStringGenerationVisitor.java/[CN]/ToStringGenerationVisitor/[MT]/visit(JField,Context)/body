{
  if (x.getConstInitializer() != null) {
    printFinalFlag(x);
  }
 else {
    printMemberFinalFlag(x);
  }
  printStaticFlag(x);
  printType(x);
  space();
  printUniqueName(x);
  return false;
}
