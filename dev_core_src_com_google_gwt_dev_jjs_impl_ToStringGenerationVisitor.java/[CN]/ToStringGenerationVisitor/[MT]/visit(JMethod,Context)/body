{
  if (isInitializer(x)) {
    if (x.isStatic()) {
      print(CHARS_STATIC);
    }
    accept(x.getBody());
  }
 else {
    printMethodHeader(x);
    if (x.isAbstract()) {
      semi();
    }
 else {
      space();
      accept(x.getBody());
    }
  }
  return false;
}
