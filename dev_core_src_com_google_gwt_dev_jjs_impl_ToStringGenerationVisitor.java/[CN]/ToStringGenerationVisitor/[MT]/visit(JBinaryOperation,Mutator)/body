{
  JExpression arg1=x.getLhs();
  parenPush(x,arg1);
  arg1.traverse(this);
  parenPop(x,arg1);
  space();
  print(x.op.getSymbol());
  space();
  JExpression arg2=x.getRhs();
  parenPush(x,arg2);
  arg2.traverse(this);
  parenPop(x,arg2);
  return false;
}
