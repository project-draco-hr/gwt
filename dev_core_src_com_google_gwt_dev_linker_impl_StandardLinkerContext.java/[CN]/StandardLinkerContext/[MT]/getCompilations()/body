{
  SortedSet<CompilationResult> toReturn=new TreeSet<CompilationResult>(COMPILATION_RESULT_COMPARATOR);
  toReturn.addAll(resultsByStrongName.values());
  return Collections.unmodifiableSortedSet(toReturn);
}
