{
  if (!closed) {
    closed=true;
    SoycArtifact newArtifact=new SoycArtifact(OUTPUT_DIRECTORY_NAME + "/" + path,baos.toByteArray());
    newArtifact.setPrivate(false);
    artifacts.add(newArtifact);
    baos=null;
  }
}
