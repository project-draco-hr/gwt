{
  Document doc=docHelper.documentFor(string);
  Element w3cElem=(Element)doc.getDocumentElement().getElementsByTagName("g:" + parsedType.getName()).item(0);
  XMLElement elem=elemProvider.get(w3cElem);
  return elem;
}
