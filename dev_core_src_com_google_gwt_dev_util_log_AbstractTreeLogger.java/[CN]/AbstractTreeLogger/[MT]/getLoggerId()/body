{
  if (fParent != null) {
    if (fParent.fParent == null) {
      return fParent.getLoggerId() + getBranchedIndex();
    }
 else {
      return fParent.getLoggerId() + "." + getBranchedIndex();
    }
  }
 else {
    return "#";
  }
}
