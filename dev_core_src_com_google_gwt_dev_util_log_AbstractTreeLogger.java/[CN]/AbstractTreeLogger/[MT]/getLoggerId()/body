{
  if (parent != null) {
    if (parent.parent == null) {
      return parent.getLoggerId() + getBranchedIndex();
    }
 else {
      return parent.getLoggerId() + "." + getBranchedIndex();
    }
  }
 else {
    return "#";
  }
}
