{
  StringBuffer sb=new StringBuffer();
  while (temp.hasNext()) {
    sb.append("\t\t\t" + oldClass.computeRelativeSignature(temp.next()) + ApiDiffGenerator.DELIMITER+ ApiChange.Status.MISSING+ "\n");
  }
  return sb.toString();
}
