{
  if (!ApiCompatibilityChecker.PRINT_INTERSECTIONS && (property.getStatus() == ApiChange.Status.COMPATIBLE)) {
    return;
  }
  if (!ApiCompatibilityChecker.PRINT_COMPATIBLE_WITH && property.getStatus() == ApiChange.Status.COMPATIBLE_WITH) {
    return;
  }
  HashSet<ApiChange> value=hashMap.get(key);
  if (value == null) {
    value=new HashSet<ApiChange>();
  }
  value.add(property);
  hashMap.put(key,value);
}
