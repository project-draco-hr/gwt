{
  TreeItem[] items=fTree.getItems();
  for (int i=0, n=items.length; i < n; ++i) {
    TreeItem item=items[i];
    expandAll(item);
  }
  if (items.length > 0)   fTree.setSelection(new TreeItem[]{items[0]});
}
