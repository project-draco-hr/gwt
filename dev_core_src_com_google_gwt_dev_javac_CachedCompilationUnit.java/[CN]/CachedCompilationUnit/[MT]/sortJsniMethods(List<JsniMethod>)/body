{
  if (jsniMethods == null) {
    return null;
  }
  ArrayList<JsniMethod> copy=new ArrayList<JsniMethod>(jsniMethods);
  Collections.sort(copy,new Comparator<JsniMethod>(){
    @Override public int compare(    JsniMethod o1,    JsniMethod o2){
      return o1.name().compareTo(o2.name());
    }
  }
);
  return copy;
}
