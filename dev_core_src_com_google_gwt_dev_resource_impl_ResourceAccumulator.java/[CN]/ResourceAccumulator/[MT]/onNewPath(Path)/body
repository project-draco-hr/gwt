{
  try {
    if (Files.isHidden(path)) {
      return;
    }
    if (Files.isRegularFile(path)) {
      onNewFile(path);
    }
 else {
      onNewDirectory(path);
    }
  }
 catch (  NoSuchFileException|FileNotFoundException e) {
  }
}
