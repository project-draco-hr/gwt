{
  Collection<JInterfaceType> jsFunctions=Collections2.filter(getImplementedInterfaces(type),new Predicate<JInterfaceType>(){
    @Override public boolean apply(    JInterfaceType implementedInterface){
      return implementedInterface.isJsFunction();
    }
  }
);
  return jsFunctions;
}
