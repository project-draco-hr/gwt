{
  return new ListModelAdapter<EmployeeKey>(){
    @Override protected void onRangeChanged(    ListRegistration reg,    int start,    int length){
      requests.employeeRequest().findAllEmployees().forProperties(getProperties()).to(this).fire();
    }
  }
;
}
