{
  assertEquals("When the root type is searched for, all members should be returned in the same" + " order as they appear in the original set.",set,set.getTypeIndex().findAssignableTo(RootComparable.class));
  assertEquals(childAs,set.getTypeIndex().findAssignableTo(ChildA.class));
}
