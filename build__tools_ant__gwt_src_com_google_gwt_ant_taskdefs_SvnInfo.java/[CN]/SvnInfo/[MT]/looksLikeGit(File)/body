{
  if (looksLikeSvn(dir)) {
    return false;
  }
  File gitDir=findGitDir(dir);
  if (gitDir != null && gitDir.isDirectory()) {
    return new File(gitDir,"svn").isDirectory();
  }
  return false;
}
