{
  return new SinkInfo("Frames","If you need to include multiple pages of good ol' static HTML, it's " + "easy to do using the <code>Frame</code> class."){
    public Sink createInstance(){
      return new Frames();
    }
  }
;
}
