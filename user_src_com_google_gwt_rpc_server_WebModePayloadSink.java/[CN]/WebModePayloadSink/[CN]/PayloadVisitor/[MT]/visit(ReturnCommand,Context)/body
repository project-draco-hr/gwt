{
  int size=x.getValues().size();
  begin(x);
  _return();
  lbracket();
  for (int i=0; i < size; i++) {
    accept(x.getValues().get(i));
    if (i < size - 1) {
      comma();
    }
  }
  rbracket();
  semi();
  commit(x);
  return false;
}
