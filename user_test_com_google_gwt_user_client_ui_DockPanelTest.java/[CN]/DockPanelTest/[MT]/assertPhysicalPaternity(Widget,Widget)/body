{
  Element panelElement=parent.getElement();
  Element childElement=expectedChild.getElement();
  assertTrue("The parent's Element of the child must be the panel's Element",panelElement.isOrHasChild(childElement));
}
