{
  if (GWT.isClient() && !handlersAreInitialized) {
    handlersAreInitialized=true;
    initHandler(getWindowCloseHandlerMethodString(),"__gwt_initWindowCloseHandler",new Command(){
      public void execute(){
        initWindowCloseHandler();
      }
    }
);
  }
}
