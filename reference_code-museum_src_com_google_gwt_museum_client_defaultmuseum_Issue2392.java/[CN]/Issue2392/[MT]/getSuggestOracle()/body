{
  if (oracle == null) {
    oracle=new MultiWordSuggestOracle();
    for (char a='a'; a <= 'z'; a++) {
      for (char b='a'; b <= 'm'; b++) {
        for (char c='a'; c <= 'd'; c++) {
          oracle.add("" + a + b+ c);
        }
      }
    }
  }
  return oracle;
}
