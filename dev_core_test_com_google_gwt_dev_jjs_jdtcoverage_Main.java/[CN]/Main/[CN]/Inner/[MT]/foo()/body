{
  final int z=this.y;
  new Inner(){
{
      x=z;
      this.x=z;
      Inner.this.x=z;
      next=Main.this.next;
      next.foo();
      Main.this.next.foo();
      Main.this.x=z;
      Main.super.x=z;
    }
    public void foo(){
      x=z;
      this.x=z;
      Inner.this.x=z;
      next=Main.this.next;
      next.foo();
      Main.this.next.foo();
      Main.this.x=z;
      Main.super.x=z;
    }
  }
;
class NamedLocal extends Inner {
    public void foo(){
      Main.this.getNext();
      Inner.this.foo();
      super.foo();
      int x=z;
    }
  }
  ;
  new InnerSub().new InnerSubSub().fda();
  new SecondMain().new FunkyInner();
}
