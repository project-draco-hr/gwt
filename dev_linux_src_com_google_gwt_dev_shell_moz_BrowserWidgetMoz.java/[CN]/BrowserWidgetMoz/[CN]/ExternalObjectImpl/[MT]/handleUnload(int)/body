{
  try {
    Integer key=null;
    if (scriptObject != 0) {
      key=Integer.valueOf(scriptObject);
    }
    doUnload(key);
    return true;
  }
 catch (  Throwable e) {
    getHost().getLogger().log(TreeLogger.ERROR,"Failure to unload modules",e);
    return false;
  }
}
