{
  Conditional conditional=res().conditional();
  String expectedCss="." + conditional.foo() + "{color:white;font-family:kennedy;font-weight:bold;top:5px;left:5px}";
  assertEquals(expectedCss,conditional.getText());
}
