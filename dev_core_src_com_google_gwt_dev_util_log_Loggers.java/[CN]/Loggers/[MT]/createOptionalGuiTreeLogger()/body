{
  if (System.getProperty("gwt.useGuiLogger") != null) {
    AbstractTreeLogger atl=TreeLoggerWidget.getAsDetachedWindow("CompilationServiceTest",800,600,true);
    return maybeSetDetailLevel(atl);
  }
 else {
    return TreeLogger.NULL;
  }
}
