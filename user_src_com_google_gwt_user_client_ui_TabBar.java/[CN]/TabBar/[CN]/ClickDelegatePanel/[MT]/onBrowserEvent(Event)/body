{
  if (!enabled) {
    return;
  }
switch (DOM.eventGetType(event)) {
case Event.ONCLICK:
    clickDelegate.onClick(this);
  break;
case Event.ONKEYDOWN:
keyDelegate.onKeyDown(this,((char)DOM.eventGetKeyCode(event)),KeyboardListenerCollection.getKeyboardModifiers(event));
break;
}
}
