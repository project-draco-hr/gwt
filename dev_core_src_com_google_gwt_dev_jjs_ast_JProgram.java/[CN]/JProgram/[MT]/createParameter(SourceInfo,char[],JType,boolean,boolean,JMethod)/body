{
  assert(name != null);
  assert(type != null);
  assert(enclosingMethod != null);
  JParameter x=new JParameter(this,info,String.valueOf(name),type,isFinal,isThis,enclosingMethod);
  enclosingMethod.params.add(x);
  return x;
}
