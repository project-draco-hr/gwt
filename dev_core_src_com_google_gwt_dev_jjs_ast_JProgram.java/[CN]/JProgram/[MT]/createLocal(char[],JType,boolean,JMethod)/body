{
  assert(name != null);
  assert(type != null);
  assert(enclosingMethod != null);
  JLocal x=new JLocal(this,String.valueOf(name),type,isFinal,enclosingMethod);
  enclosingMethod.locals.add(x);
  return x;
}
