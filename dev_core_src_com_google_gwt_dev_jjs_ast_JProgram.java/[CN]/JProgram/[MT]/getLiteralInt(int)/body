{
switch (i) {
case -1:
    return literalIntNegOne;
case 0:
  return literalIntZero;
case 1:
return literalIntOne;
default :
{
SourceInfo info=createSourceInfoSynthetic(JProgram.class,i + " literal");
info.addCorrelation(Correlation.by(Literal.INT));
return new JIntLiteral(this,info,i);
}
}
}
