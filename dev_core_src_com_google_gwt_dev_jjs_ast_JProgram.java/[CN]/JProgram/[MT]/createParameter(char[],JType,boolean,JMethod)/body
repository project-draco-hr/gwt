{
  assert(name != null);
  assert(type != null);
  assert(enclosingMethod != null);
  JParameter x=new JParameter(this,String.valueOf(name),type,isFinal,enclosingMethod);
  enclosingMethod.params.add(x);
  return x;
}
