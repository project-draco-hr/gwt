{
  JStringLiteral toReturn=stringLiteralMap.get(s);
  if (toReturn == null) {
    toReturn=new JStringLiteral(this,stringPoolSourceInfo.makeChild(JProgram.class,"String literal: " + s),s);
    stringLiteralMap.put(s,toReturn);
  }
  toReturn.getSourceInfo().merge(sourceInfo);
  return toReturn;
}
