{
  return Collections2.transform(typeOracle.getSubTypeNames(type.getName()),new Function<String,JType>(){
    public JType apply(    String typeName){
      return getFromTypeMap(typeName);
    }
  }
);
}
