{
  if (jsonCastableTypeMaps == null) {
    jsonCastableTypeMaps=new ArrayList<JsonObject>();
    jsonCastableTypeMaps.add(new JsonObject(createSourceInfoSynthetic(JProgram.class,"always-false typeinfo entry"),getJavaScriptObject()));
  }
  return jsonCastableTypeMaps.get(typeId);
}
