{
  int serial=getSerial(fragment);
  String parameters=((serial == 0) ? "" : ("?serial=" + serial));
  return GWT.getModuleBaseURL() + getDeferredJavaScriptDirectory() + GWT.getPermutationStrongName()+ "/"+ fragment+ ".cache.js"+ parameters;
}
