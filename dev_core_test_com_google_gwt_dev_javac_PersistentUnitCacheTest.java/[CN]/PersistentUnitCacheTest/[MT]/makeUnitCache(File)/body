{
  return new PersistentUnitCache(logger,cacheDir){
    @Override Future<Void> startRotating(){
      Futures.getUnchecked(super.startRotating());
      return Futures.immediateFuture(null);
    }
  }
;
}
