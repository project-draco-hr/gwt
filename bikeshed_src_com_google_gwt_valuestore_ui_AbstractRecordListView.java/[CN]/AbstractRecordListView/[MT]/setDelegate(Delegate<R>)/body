{
  table.setDelegate(delegate);
  table.setSelectionModel(new SingleSelectionModel<R>(){
    @Override public void setSelected(    R object,    boolean selected){
      super.setSelected(object,selected);
      delegate.showDetails(object);
    }
  }
);
}
