{
  if (x.annotations != null) {
    AnnotationBinding jsInterface=JdtUtil.getAnnotation(x.binding,JSINTERFACE_CLASS);
    if (jsInterface != null) {
      boolean isNative=JdtUtil.getAnnotationParameterBoolean(jsInterface,"isNative");
      interopType=jsPrototype != null ? (isNative ? JInterfaceType.JsInteropType.NATIVE_PROTOTYPE : JInterfaceType.JsInteropType.JS_PROTOTYPE) : JInterfaceType.JsInteropType.NO_PROTOTYPE;
    }
  }
  return interopType;
}
