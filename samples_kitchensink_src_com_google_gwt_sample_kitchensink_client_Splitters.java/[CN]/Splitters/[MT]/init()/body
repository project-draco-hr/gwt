{
  return new SinkInfo("Splitters","GWT includes horizontal and vertical split panels, which can be used " + "to create user-sizable regions in your application."){
    public Sink createInstance(){
      return new Splitters();
    }
  }
;
}
