{
  if (API_CHECK) {
    checkCriticalNotNull(reference,errorMessage);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalNotNull(reference,errorMessage);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
