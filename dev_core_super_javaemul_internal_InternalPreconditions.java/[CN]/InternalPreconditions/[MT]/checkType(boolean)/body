{
  if (TYPE_CHECK) {
    checkCriticalType(expression);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalType(expression);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
