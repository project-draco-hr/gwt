{
  if (API_CHECK) {
    checkCriticalArraySize(size);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalArraySize(size);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
