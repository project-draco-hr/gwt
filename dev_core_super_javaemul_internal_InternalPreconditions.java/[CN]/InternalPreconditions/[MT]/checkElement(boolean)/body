{
  if (API_CHECK) {
    checkCriticalElement(expression);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalElement(expression);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
