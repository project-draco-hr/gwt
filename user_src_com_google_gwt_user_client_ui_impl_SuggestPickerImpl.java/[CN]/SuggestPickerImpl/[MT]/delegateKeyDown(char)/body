{
  if (isAttached()) {
switch (keyCode) {
case KeyboardListener.KEY_DOWN:
      shiftSelection(1);
    return true;
case KeyboardListener.KEY_UP:
  shiftSelection(-1);
return true;
case KeyboardListener.KEY_ENTER:
commitSelection();
return true;
}
}
return false;
}
