{
  StringBuffer out=new StringBuffer();
  for (Iterator i=hosts.iterator(); i.hasNext(); ) {
    String rule=(String)i.next();
    out.append(rule);
    out.append(" ");
  }
  return out.toString();
}
