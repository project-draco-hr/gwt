{
  if (list.size() != 4) {
    return "size";
  }
  String temp;
  temp=checkA(list.get(0));
  if (!temp.isEmpty()) {
    return temp;
  }
  temp=checkB(list.get(1));
  if (!temp.isEmpty()) {
    return temp;
  }
  temp=checkC(list.get(2));
  if (!temp.isEmpty()) {
    return temp;
  }
  temp=checkD(list.get(3));
  if (!temp.isEmpty()) {
    return temp;
  }
  return "";
}
