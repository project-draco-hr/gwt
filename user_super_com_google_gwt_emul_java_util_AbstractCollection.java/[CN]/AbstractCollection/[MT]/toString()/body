{
  StringBuffer sb=new StringBuffer();
  String comma=null;
  sb.append("[");
  Iterator iter=iterator();
  while (iter.hasNext()) {
    if (comma != null) {
      sb.append(comma);
    }
 else {
      comma=", ";
    }
    sb.append(String.valueOf(iter.next()));
  }
  sb.append("]");
  return sb.toString();
}
