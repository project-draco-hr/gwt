{
  StringBuilder sb=new StringBuilder("[");
  boolean comma=false;
  for (  E e : this) {
    if (comma) {
      sb.append(", ");
    }
 else {
      comma=true;
    }
    sb.append(e == this ? "(this Collection)" : String.valueOf(e));
  }
  sb.append("]");
  return sb.toString();
}
