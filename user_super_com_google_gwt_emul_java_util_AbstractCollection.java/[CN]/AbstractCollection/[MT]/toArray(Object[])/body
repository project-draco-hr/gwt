{
  int size=size();
  if (a.length < size) {
    a=Array.clonify(a,size);
  }
  int i=0;
  for (Iterator it=iterator(); it.hasNext(); ) {
    a[i++]=it.next();
  }
  if (a.length > size) {
    a[size]=null;
  }
  return a;
}
