{
  Throwable caught=null;
  try {
    return (String)fMetaField.get(schema);
  }
 catch (  IllegalArgumentException e) {
    caught=e;
  }
catch (  IllegalAccessException e) {
    caught=e;
  }
  throw (IllegalStateException)new IllegalStateException("Unable to get attribute default value from meta field '" + fMetaField.getName() + "'").initCause(caught);
}
