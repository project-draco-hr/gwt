{
  SourceInfo parent=sourceInfoStack.peek();
  SourceInfo toReturn=program.createSourceInfo(node.getLineno(),parent.getFileName());
  toReturn.copyMissingCorrelationsFrom(parent);
  return toReturn;
}
