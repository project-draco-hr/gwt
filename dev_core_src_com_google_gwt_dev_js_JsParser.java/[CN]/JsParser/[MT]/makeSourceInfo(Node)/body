{
  SourceInfo parent=sourceInfoStack.peek();
  SourceInfo toReturn=program.createSourceInfo(node.getLineno() + parent.getStartLine() + 1,parent.getFileName());
  toReturn.copyMissingCorrelationsFrom(parent);
  return toReturn;
}
