{
  numClients=1;
  if (args != null) {
    try {
      numClients=Integer.parseInt(args);
    }
 catch (    NumberFormatException e) {
      throw new RuntimeException("Error parsing argument \"" + args + "\"",e);
    }
  }
  shell.setNumClients(numClients);
  return true;
}
