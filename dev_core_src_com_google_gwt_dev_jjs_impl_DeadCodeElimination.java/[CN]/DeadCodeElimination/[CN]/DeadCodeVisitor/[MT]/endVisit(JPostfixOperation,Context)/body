{
  if (x.getOp().isModifying()) {
    lvalues.remove(x.getArg());
  }
  if (x == ignoringExpressionOutput) {
    JPrefixOperation newOp=new JPrefixOperation(program,x.getSourceInfo(),x.getOp(),x.getArg());
    ctx.replaceMe(newOp);
  }
}
