{
  if (isLiteralOne(rhs)) {
    ctx.replaceMe(simplifyCast(type,lhs));
    return true;
  }
  if (isLiteralNegativeOne(rhs)) {
    ctx.replaceMe(simplifyNegate(lhs));
    return true;
  }
  return false;
}
