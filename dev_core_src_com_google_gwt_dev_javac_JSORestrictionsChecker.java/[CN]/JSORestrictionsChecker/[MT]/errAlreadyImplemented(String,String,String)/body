{
  return "Only one JavaScriptObject type may implement the methods of a " + "@SingleJsoImpl type interface. The interface (" + intfName + ") is implemented by both ("+ impl1+ ") and ("+ impl2+ ")";
}
