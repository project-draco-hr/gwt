{
  assert(name != null);
  assert(type != null);
  assert(enclosingMethod != null);
  JParameter x=new JParameter(info,String.valueOf(name),type,isFinal,isThis,enclosingMethod);
  enclosingMethod.addParam(x);
  return x;
}
