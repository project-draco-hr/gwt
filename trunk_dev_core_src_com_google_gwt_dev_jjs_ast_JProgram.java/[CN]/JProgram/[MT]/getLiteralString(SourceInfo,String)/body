{
  JStringLiteral toReturn=stringLiteralMap.get(s);
  if (toReturn == null) {
    toReturn=new JStringLiteral(stringPoolSourceInfo.makeChild(JProgram.class,"String literal: " + s),s,typeNonNullString);
    stringLiteralMap.put(s,toReturn);
  }
  toReturn.getSourceInfo().merge(sourceInfo);
  return toReturn;
}
