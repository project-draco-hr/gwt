{
  if (a == null) {
    return 0;
  }
  int hashCode=1;
  for (  boolean e : a) {
    hashCode=31 * hashCode + (Boolean.valueOf(e).hashCode());
    hashCode=ensureInt(hashCode);
  }
  return hashCode;
}
