{
  int size=size();
  if (out.length < size) {
    out=Array.clonify(out,size);
  }
  for (int i=0; i < size; ++i) {
    out[i]=(T)array[i];
  }
  if (out.length > size) {
    out[size]=null;
  }
  return out;
}
