{
  activeValue=p.getActiveValue();
  name=p.getName();
  provider=p.getProvider() == null ? null : p.getProvider().getBody().toSource();
  values=Collections.unmodifiableSortedSet(new TreeSet<String>(Arrays.asList(p.getKnownValues())));
}
