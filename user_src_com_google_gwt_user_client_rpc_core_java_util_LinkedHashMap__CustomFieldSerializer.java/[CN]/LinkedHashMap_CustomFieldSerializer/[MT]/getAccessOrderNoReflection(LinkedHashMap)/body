{
  instance=(LinkedHashMap)instance.clone();
  instance.clear();
  Object key1=new Object();
  Object key2=new Object();
  instance.put(key1,key1);
  instance.put(key2,key2);
  instance.get(key1);
  boolean accessOrder=false;
  for (  Object key : instance.keySet()) {
    if (key == key1) {
      break;
    }
    if (key == key2) {
      accessOrder=true;
      break;
    }
  }
  return accessOrder;
}
