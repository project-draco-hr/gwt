{
  int value;
  value=matchString(text,start,dateTimeConstants.standaloneWeekdays(),pos);
  if (value < 0) {
    value=matchString(text,start,dateTimeConstants.standaloneShortWeekdays(),pos);
  }
  if (value < 0) {
    return false;
  }
  cal.setDayOfWeek(value);
  return true;
}
