{
  if (count < 4) {
    int val=date.getTimezoneOffset();
    char sign='-';
    if (val < 0) {
      val=-val;
      sign='+';
    }
    val=(val / 3) * 5 + (val % MINUTES_PER_HOUR);
    buf.append(sign);
    zeroPaddingNumber(buf,val,4);
  }
 else {
    appendGMT(buf,date);
  }
}
