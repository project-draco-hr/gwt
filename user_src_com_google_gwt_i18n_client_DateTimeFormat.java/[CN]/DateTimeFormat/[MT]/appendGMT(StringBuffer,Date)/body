{
  int value=-date.getTimezoneOffset();
  if (value < 0) {
    buf.append("GMT-");
    value=-value;
  }
 else {
    buf.append("GMT+");
  }
  zeroPaddingNumber(buf,value / MINUTES_PER_HOUR,2);
  buf.append(':');
  zeroPaddingNumber(buf,value % MINUTES_PER_HOUR,2);
}
