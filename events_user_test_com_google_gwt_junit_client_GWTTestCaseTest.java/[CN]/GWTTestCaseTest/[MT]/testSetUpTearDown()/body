{
  assertSame(IS_SETUP,setupTeardownFlag);
  tearDown();
  assertSame(IS_TORNDOWN,setupTeardownFlag);
  setUp();
  assertSame(IS_SETUP,setupTeardownFlag);
  gwtTearDown();
  assertSame(IS_TORNDOWN,setupTeardownFlag);
  gwtSetUp();
  assertSame(IS_SETUP,setupTeardownFlag);
}
