{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  RegionPopulation other=(RegionPopulation)obj;
  return region.equals(other.region) && (Double.compare(literatePopulation,other.literatePopulation) == 0);
}
