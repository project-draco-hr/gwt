{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  LanguagePopulation other=(LanguagePopulation)obj;
  return language.equals(other.language) && (Double.compare(literatePopulation,other.literatePopulation) == 0);
}
