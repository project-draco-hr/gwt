{
  return new SinkInfo("Tabs","GWT's built-in <code>TabPanel</code> class makes it easy to build tabbed dialogs " + "and the like.  Notice that no page load occurs when you select the " + "different tabs in this page.  That's the magic of dynamic HTML."){
    public Sink createInstance(){
      return new Tabs();
    }
  }
;
}
