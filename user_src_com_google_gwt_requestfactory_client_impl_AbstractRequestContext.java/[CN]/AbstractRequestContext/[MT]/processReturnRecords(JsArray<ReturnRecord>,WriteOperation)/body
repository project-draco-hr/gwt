{
  for (int i=0, j=records.length(); i < j; i++) {
    ReturnRecord record=records.get(i);
    SimpleEntityProxyId<EntityProxy> id=requestFactory.getId(record.getSchema(),record.getEncodedId(),record.getFutureId());
    processReturnRecord(id,record,operations);
  }
}
