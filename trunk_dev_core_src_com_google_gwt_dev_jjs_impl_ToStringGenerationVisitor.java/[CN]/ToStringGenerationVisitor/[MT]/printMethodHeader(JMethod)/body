{
  if (x.isPrivate()) {
    print(CHARS_PRIVATE);
  }
 else {
    print(CHARS_PUBLIC);
  }
  printStaticFlag(x);
  printAbstractFlag(x);
  printNativeFlag(x);
  printFinalFlag(x);
  printType(x);
  space();
  if (x.isStatic()) {
    printUniqueName(x);
  }
 else {
    printName(x);
  }
  printParameterList(x);
}
