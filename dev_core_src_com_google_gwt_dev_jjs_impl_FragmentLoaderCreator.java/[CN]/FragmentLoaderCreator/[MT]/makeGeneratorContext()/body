{
  PropertyOracle propOracle=new StaticPropertyOracle(new BindingProperty[0],new String[0],new ConfigurationProperty[0]);
  StandardGeneratorContext context=new StandardGeneratorContext(compilationState,propOracle,publicOracle,genDir,outDir,artifactSet);
  return context;
}
