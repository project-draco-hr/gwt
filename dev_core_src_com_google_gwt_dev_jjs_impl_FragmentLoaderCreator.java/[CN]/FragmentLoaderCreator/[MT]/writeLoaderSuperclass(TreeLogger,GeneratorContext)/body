{
  PrintWriter printWriter=ctx.tryCreate(logger,getPackage(),getLoaderSuperclassSimpleName());
  if (printWriter == null) {
    logger.log(TreeLogger.ERROR,"Could not create type: " + getLoaderSuperclassQualifiedName());
    throw new UnableToCompleteException();
  }
  printWriter.println("package " + getPackage() + ";");
  printWriter.println("public class " + getLoaderSuperclassSimpleName() + " {");
  printWriter.println("public void runCallbacks() { }");
  printWriter.println("}");
  printWriter.close();
  ctx.commit(logger,printWriter);
}
