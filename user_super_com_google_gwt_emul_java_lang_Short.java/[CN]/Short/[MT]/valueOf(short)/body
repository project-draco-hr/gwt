{
  if (s > -129 && s < 128) {
    int rebase=s + 128;
    if (boxedValues[rebase] == null) {
      boxedValues[rebase]=new Short(s);
    }
    return boxedValues[rebase];
  }
  return new Short(s);
}
