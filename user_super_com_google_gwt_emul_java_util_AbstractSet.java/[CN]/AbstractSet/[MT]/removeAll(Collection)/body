{
  int size=size();
  if (size < c.size()) {
    for (Iterator iter=iterator(); iter.hasNext(); ) {
      Object o=iter.next();
      if (c.contains(o)) {
        iter.remove();
      }
    }
  }
 else {
    for (Iterator iter=c.iterator(); iter.hasNext(); ) {
      Object o=iter.next();
      remove(o);
    }
  }
  return (size != size());
}
