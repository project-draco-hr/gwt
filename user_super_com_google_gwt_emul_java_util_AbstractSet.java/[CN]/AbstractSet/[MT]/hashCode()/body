{
  int hashCode=0;
  for (Iterator<E> iter=iterator(); iter.hasNext(); ) {
    E next=iter.next();
    if (next != null) {
      hashCode+=next.hashCode();
      hashCode=~~hashCode;
    }
  }
  return hashCode;
}
