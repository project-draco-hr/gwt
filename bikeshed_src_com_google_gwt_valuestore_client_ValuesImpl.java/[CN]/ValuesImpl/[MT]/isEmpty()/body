{
  final Property<K,String> id=getKey().getId();
  final Property<K,Integer> version=getKey().getVersion();
  for (  Property<K,?> property : getKey().all()) {
    if ((property != id) && (property != version) && (isDefined(property.getName()))) {
      return false;
    }
  }
  return true;
}
