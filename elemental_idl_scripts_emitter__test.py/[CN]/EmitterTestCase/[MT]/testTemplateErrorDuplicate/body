def testTemplateErrorDuplicate(self):
    try:
        e = emitter.Emitter()
        b = e.Emit('$(A)$(!B)$(A)$(!B)')
    except RuntimeError as ex:
        return
    raise AssertionError('Expected error')
