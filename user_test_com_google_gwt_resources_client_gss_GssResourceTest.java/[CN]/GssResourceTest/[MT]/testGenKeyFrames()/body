{
  GenKeyFrames genKeyFrames=res().genKeyFrames();
  String expectedCss="@keyframes myframe{0%{top:0}to{top:200px}}@-webkit-keyframes myframe{0%{top:0}to{top:200px}}" + "div{animation:myframe 5s infinite}";
  assertEquals(expectedCss,genKeyFrames.getText());
}
