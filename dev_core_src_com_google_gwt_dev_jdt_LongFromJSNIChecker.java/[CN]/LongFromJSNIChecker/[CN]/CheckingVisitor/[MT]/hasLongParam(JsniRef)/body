{
  if (!jsniRef.isMethod()) {
    return false;
  }
  for (  String type : jsniRef.paramTypes()) {
    if (type.charAt(type.length() - 1) == JniConstants.DESC_LONG) {
      return true;
    }
  }
  return false;
}
