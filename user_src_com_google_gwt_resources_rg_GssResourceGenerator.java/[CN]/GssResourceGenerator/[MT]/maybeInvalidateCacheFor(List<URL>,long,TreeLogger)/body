{
  Long lastModifiedFromCache=LAST_MODIFIED_CACHE.getIfPresent(resources);
  if (lastModifiedFromCache == null || lastModified == 0 || (lastModified > lastModifiedFromCache)) {
    TREE_CACHE.invalidate(resources);
  }
}
