{
  String typeName=getTypeNameById(typeId);
  String superTypeName=minimalRebuildCache.getImmediateTypeRelations().getImmediateSuperclassesByClass().get(typeName);
  if (superTypeName != null) {
    return getTypeIdByName(superTypeName);
  }
  return -1;
}
