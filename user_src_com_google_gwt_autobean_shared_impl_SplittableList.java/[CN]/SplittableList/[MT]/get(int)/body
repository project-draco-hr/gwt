{
  if (data.isReified(String.valueOf(index))) {
    @SuppressWarnings("unchecked") E toReturn=(E)data.getReified(String.valueOf(index));
    return toReturn;
  }
  return SplittableList.<E>reify(state,data,index,elementCoder);
}
