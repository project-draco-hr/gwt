{
  XMLElement ret=null;
  for (  XMLElement child : consumeChildElements()) {
    if (ret != null) {
      throw new RuntimeException(String.format("%s may only contain a single child element, but found" + "%s and %s.",getLocalName(),ret,child));
    }
    ret=child;
  }
  return ret;
}
