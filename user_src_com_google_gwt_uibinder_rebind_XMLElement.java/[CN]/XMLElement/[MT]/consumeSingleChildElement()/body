{
  XMLElement ret=null;
  for (  XMLElement child : consumeChildElements()) {
    if (ret != null) {
      throw new RuntimeException(getLocalName() + " may only contain a single child element.");
    }
    ret=child;
  }
  return ret;
}
