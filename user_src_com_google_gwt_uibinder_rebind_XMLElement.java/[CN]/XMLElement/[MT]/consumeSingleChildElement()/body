{
  XMLElement ret=null;
  for (  XMLElement child : consumeChildElements()) {
    if (ret != null) {
      writer.die("%s may only contain a single child element, but found" + "%s and %s.",getLocalName(),ret,child);
    }
    ret=child;
  }
  return ret;
}
