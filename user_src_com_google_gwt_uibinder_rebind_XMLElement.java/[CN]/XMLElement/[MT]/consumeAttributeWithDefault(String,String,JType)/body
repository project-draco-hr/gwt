{
  String value=consumeRawAttribute(name);
  if ("".equals(value)) {
    return defaultValue;
  }
  value=getParser(getAttribute(name),types).parse(value,logger);
  if ("".equals(value)) {
    return defaultValue;
  }
  return value;
}
