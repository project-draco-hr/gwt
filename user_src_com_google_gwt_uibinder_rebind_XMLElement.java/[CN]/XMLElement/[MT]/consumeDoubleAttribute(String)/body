{
  try {
    return Double.parseDouble(consumeAttribute(attr));
  }
 catch (  NumberFormatException e) {
    writer.die(String.format("Error parsing \"%s\" attribute of \"%s\" " + "as a double value",attr,this));
    return 0;
  }
}
