{
  String strValue=consumeAttribute(attr);
  T value=null;
  try {
    if (strValue != null) {
      value=Enum.valueOf(type,strValue);
    }
  }
 catch (  IllegalArgumentException e) {
  }
  if (value == null) {
    writer.die(String.format("Error parsing \"%s\" attribute of \"%s\" " + "as a %s enum",attr,this,type.getSimpleName()));
  }
  return value;
}
