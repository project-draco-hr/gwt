{
  String newScript=createNativeMethodInjector(jsniSignature,paramNames,js);
  try {
    Variant result=execute(window,newScript);
    if (result != null) {
      result.dispose();
    }
  }
 catch (  RuntimeException e) {
    throw new RuntimeException(file + "(" + line+ "): Failed to create JSNI method with signature '"+ jsniSignature+ "'",e);
  }
}
