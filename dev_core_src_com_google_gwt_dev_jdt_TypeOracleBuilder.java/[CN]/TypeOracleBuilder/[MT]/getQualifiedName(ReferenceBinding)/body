{
  if (binding.isMemberType()) {
    return String.valueOf(CharOperation.concat(binding.enclosingType().readableName(),binding.sourceName,'.'));
  }
  return CharOperation.toString(binding.compoundName);
}
