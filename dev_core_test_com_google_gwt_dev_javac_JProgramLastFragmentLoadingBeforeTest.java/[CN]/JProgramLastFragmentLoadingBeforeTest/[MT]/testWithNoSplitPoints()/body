{
  List<Integer> initialSeq=Lists.create();
  int numSps=0;
  assertEquals(0,JProgram.lastFragmentLoadingBefore(initialSeq,numSps,0,0));
  assertEquals(0,JProgram.lastFragmentLoadingBefore(initialSeq,numSps,0,0,0));
  assertEquals(0,JProgram.lastFragmentLoadingBefore(initialSeq,numSps,0));
}
