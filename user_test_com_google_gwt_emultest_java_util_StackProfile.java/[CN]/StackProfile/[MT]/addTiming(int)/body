{
  Stack s=new Stack();
  resetTimer();
  for (int i=0; i < num; i++) {
    s.push("item" + i);
  }
  timing("push(" + num + ")");
  resetTimer();
  for (int i=0; i < num; i++) {
    s.pop();
  }
  timing("pop(" + num + ")");
}
