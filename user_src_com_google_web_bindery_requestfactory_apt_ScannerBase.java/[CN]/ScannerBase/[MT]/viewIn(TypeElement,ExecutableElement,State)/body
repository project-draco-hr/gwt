{
  try {
    return (ExecutableType)state.types.asMemberOf(state.types.getDeclaredType(lookIn),methodElement);
  }
 catch (  IllegalArgumentException e) {
    return (ExecutableType)methodElement.asType();
  }
}
