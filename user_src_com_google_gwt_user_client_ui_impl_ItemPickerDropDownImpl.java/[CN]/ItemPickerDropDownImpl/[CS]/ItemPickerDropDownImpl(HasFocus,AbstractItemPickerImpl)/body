{
  super(true);
  setWidget(picker);
  this.picker=picker;
  this.owner=owner;
  picker.addChangeListener(new ChangeListener(){
    public void onChange(    Widget sender){
      hide();
    }
  }
);
}
