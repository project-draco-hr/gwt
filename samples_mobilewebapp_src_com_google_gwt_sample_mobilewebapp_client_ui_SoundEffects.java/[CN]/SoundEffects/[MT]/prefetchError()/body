{
  if (isSupported && error == null) {
    error=createAudioElement(audioSource("audio/error.ogg",AudioType.OGG),audioSource("audio/error.mp3",AudioType.MP3),audioSource("audio/error.wav",AudioType.WAV));
  }
}
