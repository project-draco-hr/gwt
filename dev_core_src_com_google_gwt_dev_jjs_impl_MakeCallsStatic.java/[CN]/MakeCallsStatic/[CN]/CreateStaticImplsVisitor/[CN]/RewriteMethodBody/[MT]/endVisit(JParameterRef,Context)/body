{
  JParameter param=varMap.get(x.getTarget());
  JParameterRef paramRef=new JParameterRef(x.getProgram(),x.getSourceInfo().makeChild(RewriteMethodBody.class,"Reference to devirtualized parameter"),param);
  ctx.replaceMe(paramRef);
}
