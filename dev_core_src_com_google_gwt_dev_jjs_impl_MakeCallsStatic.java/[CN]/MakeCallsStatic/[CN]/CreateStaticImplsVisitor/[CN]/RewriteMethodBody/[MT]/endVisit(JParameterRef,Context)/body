{
  JParameter param=varMap.get(x.getTarget());
  JParameterRef paramRef=new JParameterRef(x.getProgram(),x.getSourceInfo().makeChild("Reference to devirtualized parameter"),param);
  ctx.replaceMe(paramRef);
}
