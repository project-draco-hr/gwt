{
  if (method.getEnclosingType() != null && method.getEnclosingType().getName().startsWith(FragmentLoaderCreator.ASYNC_LOADER_PACKAGE + "." + FragmentLoaderCreator.ASYNC_LOADER_CLASS_PREFIX) && method.getName().equals(FragmentLoaderCreator.RUN_CALLBACKS)) {
    return true;
  }
  return false;
}
