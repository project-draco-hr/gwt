{
  JMethodCall newCall=new JMethodCall(x.getProgram(),x.getSourceInfo().makeChild(MakeCallsStatic.class,"Devirtualized function call"),null,newMethod);
  newCall.getArgs().add(x.getInstance());
  newCall.getArgs().addAll(x.getArgs());
  return newCall;
}
