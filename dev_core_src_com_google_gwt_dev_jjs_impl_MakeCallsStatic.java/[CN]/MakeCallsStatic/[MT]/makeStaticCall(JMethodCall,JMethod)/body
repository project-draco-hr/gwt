{
  JMethodCall newCall=new JMethodCall(x.getSourceInfo().makeChild(MakeCallsStatic.class,"Devirtualized function call"),null,newMethod);
  newCall.addArg(x.getInstance());
  newCall.addArgs(x.getArgs());
  return newCall;
}
