{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof NodeImpl)) {
    return false;
  }
  NodeImpl that=(NodeImpl)obj;
  return (this.name == null ? that.name == null : this.name == that.name) && (this.index == null ? that.index == null : this.index == that.index) && (this.key == null ? that.key == null : this.key == that.key);
}
