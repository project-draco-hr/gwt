{
  Class<? extends EntityProxy> actual=((ProxyImpl)proxy).asJso().getRequestFactory().getClass(proxy.stableId());
  Class<? extends EntityProxy> expected=getProxyClass();
  boolean equals=actual.equals(expected);
  return equals;
}
