{
  this.serviceIntf=serviceIntf;
  this.serializableTypeOracle=serializableTypeOracle;
  if (serviceIntf.isInterface() == null) {
    throw new RuntimeException("Expecting a service interface, but " + serviceIntf.getQualifiedSourceName() + " is not an interface");
  }
}
