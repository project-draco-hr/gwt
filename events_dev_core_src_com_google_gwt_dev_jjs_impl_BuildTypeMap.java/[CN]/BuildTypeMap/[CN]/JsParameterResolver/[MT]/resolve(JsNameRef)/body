{
  if (x.getQualifier() == null) {
    JsName name=getScope().findExistingName(x.getIdent());
    if (name != null && jsFunction.getParameters().contains(name.getStaticRef())) {
      x.resolve(name);
    }
  }
}
