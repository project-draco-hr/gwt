{
  Set<AbstractResource> results=new HashSet<AbstractResource>();
  Set<AbstractResource> rs=cpe.findApplicableResources(logger,pathPrefixSet);
  for (  AbstractResource r : rs) {
    if (r.getPath().indexOf(".svn/") < 0) {
      results.add(r);
    }
  }
  return results;
}
