{
  if (tokenList.size() > 0) {
    buffer.append(",");
  }
  buffer.append("[");
  for (int i=0, c=stringTable.size(); i < c; ++i) {
    if (i > 0) {
      buffer.append(",");
    }
    buffer.append(escapeString(stringTable.get(i)));
  }
  buffer.append("]");
}
