{
  CompilationUnit unit=builder.build(compiledClasses,Collections.<JDeclaredType>emptyList(),new Dependencies(),Collections.<JsniMethod>emptyList(),new MethodArgNamesLookup(),cud.compilationResult().getProblems());
  results.add(unit);
}
