{
  for (  CompiledClass compiledClass : compiledClasses) {
    binaryTypes.put(compiledClass.getInternalName(),compiledClass);
    binaryTypesRefs.put(compiledClass.getInternalName(),compiledClass.getUnit().getDisplayLocation());
  }
}
