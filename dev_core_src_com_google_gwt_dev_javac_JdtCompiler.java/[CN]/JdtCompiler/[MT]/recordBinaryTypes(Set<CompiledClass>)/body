{
  for (  CompiledClass compiledClass : compiledClasses) {
    binaryTypes.put(compiledClass.getBinaryName(),compiledClass);
    binaryTypesRefs.put(compiledClass.getBinaryName(),compiledClass.getUnit().getDisplayLocation());
  }
}
