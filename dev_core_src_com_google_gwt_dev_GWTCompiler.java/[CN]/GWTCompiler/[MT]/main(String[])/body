{
  GWTCompiler compiler=new GWTCompiler();
  if (compiler.processArgs(args)) {
    if (compiler.run()) {
      return;
    }
  }
  if (!compiler.getUseGuiLogger()) {
    System.exit(1);
  }
 else {
  }
}
