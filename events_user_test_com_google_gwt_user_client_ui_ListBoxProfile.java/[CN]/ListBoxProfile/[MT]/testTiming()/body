{
  testTiming(1);
  testTiming(2);
  testTiming(4);
  testTiming(8);
  testTiming(16);
  testTiming(32);
  testTiming(64);
  testTiming(128);
  testTiming(256);
  testTiming(512);
  testTiming(1024);
  throw new Exception("Finished profiling");
}
