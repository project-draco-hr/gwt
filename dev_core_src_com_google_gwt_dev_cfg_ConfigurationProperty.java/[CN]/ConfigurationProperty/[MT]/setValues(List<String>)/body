{
  if (!allowMultipleValues && values.size() > 1) {
    throw new IllegalStateException("Attempt to set > 1 value for a single-valued ConfigurationProperty");
  }
  this.values.clear();
  this.values.addAll(values);
}
