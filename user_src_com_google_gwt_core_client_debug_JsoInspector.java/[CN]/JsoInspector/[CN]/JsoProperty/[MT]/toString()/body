{
  StringBuilder s=new StringBuilder();
  if (isOwnProperty) {
    s.append('*');
  }
  s.append(key).append(": ");
  if (value instanceof String) {
    s.append('"').append(value).append('"');
  }
 else {
    s.append(value);
  }
  return s.toString();
}
