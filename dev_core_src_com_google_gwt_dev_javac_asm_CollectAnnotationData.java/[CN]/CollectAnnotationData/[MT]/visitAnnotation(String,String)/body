{
  return new CollectAnnotationData(desc,true,new Callback<CollectAnnotationData.AnnotationData>(){
    @Override public void call(    CollectAnnotationData.AnnotationData value){
      annotation.addValue(name,value);
    }
  }
);
}
