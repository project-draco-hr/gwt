{
  JsStatement thenStmt=x.getThenStmt();
  JsStatement elseStmt=x.getElseStmt();
  if (elseStmt instanceof JsEmpty || (elseStmt == null && thenStmt instanceof JsEmpty)) {
    needsSemicolon=true;
  }
  return false;
}
