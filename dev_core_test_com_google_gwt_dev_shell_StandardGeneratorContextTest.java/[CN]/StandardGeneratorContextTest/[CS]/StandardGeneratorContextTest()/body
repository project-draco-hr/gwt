{
  tempGenDir=createTempDir("gwt-gen-");
  tempOutDir=createTempDir("gwt-out-");
  genCtx=new StandardGeneratorContext(mockTypeOracle,mockPropOracle,mockPublicOracle,tempGenDir,tempOutDir,mockCacheManager,artifactSet);
}
