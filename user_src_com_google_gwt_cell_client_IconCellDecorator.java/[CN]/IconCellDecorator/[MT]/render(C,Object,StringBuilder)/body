{
  sb.append(outerDivHtml);
  if (isIconUsed(value)) {
    sb.append(getIconHtml(value));
  }
 else {
    sb.append(placeHolderHtml);
  }
  sb.append("<div>");
  cell.render(value,key,sb);
  sb.append("</div></div>");
}
