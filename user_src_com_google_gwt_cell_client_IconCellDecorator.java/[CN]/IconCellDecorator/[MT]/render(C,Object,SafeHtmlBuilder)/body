{
  SafeHtmlBuilder cellBuilder=new SafeHtmlBuilder();
  cell.render(value,key,cellBuilder);
  sb.append(template.outerDiv(direction,imageWidth,isIconUsed(value) ? getIconHtml(value) : placeHolderHtml,cellBuilder.toSafeHtml()));
}
