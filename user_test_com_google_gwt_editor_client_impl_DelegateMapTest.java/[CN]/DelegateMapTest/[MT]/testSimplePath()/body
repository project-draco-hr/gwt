{
  assertSame(editor.name,map.get(person).get(0).getSimpleEditor("name"));
  assertSame(editor.managerName,map.get(person).get(0).getSimpleEditor("manager.name"));
  assertNull(map.get(person).get(0).getSimpleEditor("address"));
}
