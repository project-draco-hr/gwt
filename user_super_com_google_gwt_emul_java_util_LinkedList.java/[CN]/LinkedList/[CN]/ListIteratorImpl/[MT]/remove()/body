{
  checkState(lastNode != null);
  Node<E> nextNode=lastNode.next;
  removeNode(lastNode);
  if (currentNode == lastNode) {
    currentNode=nextNode;
  }
 else {
    --currentIndex;
  }
  lastNode=null;
}
