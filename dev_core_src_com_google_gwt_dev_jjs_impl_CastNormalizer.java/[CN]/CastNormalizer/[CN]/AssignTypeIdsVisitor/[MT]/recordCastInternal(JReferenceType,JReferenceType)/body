{
  JReferenceType toType=targetType;
  Set querySet=(Set)queriedTypes.get(toType);
  if (querySet == null) {
    queryIds.put(toType,new Integer(nextQueryId++));
    querySet=new HashSet();
    queriedTypes.put(toType,querySet);
  }
  querySet.add(rhsType);
}
