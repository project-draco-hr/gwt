{
  resourceContext=new MhtmlResourceContext(logger,context,resourceBundleType);
  partialPath=Util.computeStrongName(Util.getBytes(resourceBundleType.getQualifiedSourceName() + System.currentTimeMillis())) + BUNDLE_EXTENSION;
  resourceContext.setPartialPath(partialPath);
  return resourceContext;
}
