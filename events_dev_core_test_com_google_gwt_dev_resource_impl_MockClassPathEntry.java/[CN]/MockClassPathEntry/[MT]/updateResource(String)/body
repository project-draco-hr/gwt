{
  MockAbstractResource old=resourceMap.get(resourcePath);
  Assert.assertNotNull("resource does not already exists; use addResource() if you were trying to add",old);
  old.setStale(true);
  resourceMap.put(resourcePath,createMockResource(resourcePath));
}
