{
  if (!isRunning) {
    return;
  }
  wasStarted=isStarted;
  element=null;
  isRunning=false;
  isStarted=false;
  if (requestHandle != null) {
    requestHandle.cancel();
    requestHandle=null;
  }
  onCancel();
}
