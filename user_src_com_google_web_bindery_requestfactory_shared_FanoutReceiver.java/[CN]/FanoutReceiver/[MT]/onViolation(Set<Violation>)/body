{
  try {
    if (toCall != null) {
      for (      Receiver<? super T> r : toCall) {
        try {
          r.onViolation(errors);
        }
 catch (        Throwable t) {
          onUncaughtThrowable(t);
        }
      }
    }
  }
  finally {
    finish();
  }
}
