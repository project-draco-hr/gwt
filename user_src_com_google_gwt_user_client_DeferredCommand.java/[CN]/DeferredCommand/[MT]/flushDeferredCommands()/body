{
  for (int i=0, max=deferredCommands.size(); i < max; ++i) {
    Command current=(Command)deferredCommands.remove(0);
    if (current == null) {
      return;
    }
 else {
      current.execute();
    }
  }
}
