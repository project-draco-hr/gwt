{
  if (employee != null) {
    return "Reports for " + employee.getDisplayName();
  }
 else   if (department != null) {
    return "Reports for " + department;
  }
 else {
    return "All Reports";
  }
}
