{
  if (parents == null) {
    throw new IllegalArgumentException("The set of parents must not be null. Use an empty set" + " for a group that has no parents.");
  }
  groupInheritanceMap.put(group,parents);
}
