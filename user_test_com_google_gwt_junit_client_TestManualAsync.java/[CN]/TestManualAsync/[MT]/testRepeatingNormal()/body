{
  delayTestFinish(200);
  new Timer(){
    public void run(){
      if (++i < 4) {
        delayTestFinish(200);
      }
 else {
        cancel();
        finishTest();
      }
    }
    private int i=0;
  }
.scheduleRepeating(100);
}
