{
  if (url == null) {
    logger.branch(TreeLogger.DEBUG,"No contents for resource",null);
    return null;
  }
  logger=logger.branch(TreeLogger.DEBUG,"Attempting to get stream for " + url.toExternalForm(),null);
  try {
    return url.openStream();
  }
 catch (  IOException e) {
    logger.log(TreeLogger.ERROR,"Unable to open stream",e);
    throw new UnableToCompleteException();
  }
}
