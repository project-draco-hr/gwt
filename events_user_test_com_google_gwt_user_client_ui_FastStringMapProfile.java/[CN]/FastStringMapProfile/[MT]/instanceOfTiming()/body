{
  int count=0;
  int size=20000;
  Object a=new String();
  this.resetTimer();
  for (int i=0; i < size; i++) {
    if (a instanceof String) {
      ++count;
    }
  }
  this.timing("instanceOf(" + size + ","+ a+ ")");
  return count;
}
