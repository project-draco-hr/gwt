{
  EntityManager em=entityManager();
  try {
    List<Employee> resultList=em.createQuery("select o from Employee o").setFirstResult(firstResult).setMaxResults(maxResults).getResultList();
    resultList.size();
    return resultList;
  }
  finally {
    em.close();
  }
}
