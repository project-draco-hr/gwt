{
switch (opcode) {
case Opcodes.IF_ACMPEQ:
case Opcodes.IF_ACMPNE:
    Object type1=stack.get(stack.size() - 2);
  boolean jso1=type1 instanceof String && rewriterOracle.couldContainJso((String)type1);
Object type2=stack.get(stack.size() - 1);
boolean jso2=type2 instanceof String && rewriterOracle.couldContainJso((String)type2);
if (jso1 || jso2) {
if (jso2) {
super.visitTypeInsn(Opcodes.CHECKCAST,"java/lang/Object");
}
if (jso1) {
super.visitInsn(Opcodes.SWAP);
super.visitTypeInsn(Opcodes.CHECKCAST,"java/lang/Object");
}
}
}
super.visitJumpInsn(opcode,label);
}
