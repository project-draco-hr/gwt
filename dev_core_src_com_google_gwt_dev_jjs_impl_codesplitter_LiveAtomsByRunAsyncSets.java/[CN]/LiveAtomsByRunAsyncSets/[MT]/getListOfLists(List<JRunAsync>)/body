{
  return Lists.transform(runAsyncs,new Function<JRunAsync,List<JRunAsync>>(){
    @Override public List<JRunAsync> apply(    @Nullable JRunAsync runAsync){
      return Lists.newArrayList(runAsync);
    }
  }
);
}
