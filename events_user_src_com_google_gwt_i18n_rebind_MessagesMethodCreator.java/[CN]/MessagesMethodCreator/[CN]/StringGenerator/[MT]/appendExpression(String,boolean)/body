{
  if (inString) {
    buf.append('"');
    inString=false;
    producedOutput=true;
  }
  if (producedOutput) {
    buf.append(" + ");
  }
 else   if (!knownToBeString) {
    buf.append("\"\" + ");
  }
  buf.append(expression);
  producedOutput=true;
}
