{
  if (isLoggerDead()) {
    return;
  }
  TreeItemLogger commitChild=(TreeItemLogger)childBeingCommitted;
  fSharedPendingUpdates.add(new LogEvent(commitChild,true,commitChild.getBranchedIndex(),type,msg,caught));
}
