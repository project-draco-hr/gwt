{
  String expectedCss=CssResourceGenerator.makeExpression(logger,new FakeContext(),expected,false);
  String testCss=CssResourceGenerator.makeExpression(logger,new FakeContext(),test,false);
  assertEquals(expectedCss,testCss);
}
