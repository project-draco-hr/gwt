{
  String expectedCss=CssResourceGenerator.makeExpression(logger,new FakeContext(),null,expected,false);
  String testCss=CssResourceGenerator.makeExpression(logger,new FakeContext(),null,test,false);
  assertEquals(expectedCss,testCss);
}
