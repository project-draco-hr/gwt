{
  addSnippetImport("com.google.gwt.core.client.js.JsType");
  addSnippetClassDecl("@JsType(prototype = \"x\") public enum Buggy { A, B }");
  assertBuggyFails("Enum 'test.EntryPoint$Buggy' cannot be a native JsType.");
}
