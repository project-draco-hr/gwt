{
  addAll(jsFunctionInterface);
  addSnippetClassDecl("public static class BaseClass implements MyJsFunctionInterface {","  public int foo(int x) { return 0; }","}","public static class Buggy extends BaseClass  {","}");
  assertBuggyFails("JsFunction implementation 'test.EntryPoint$BaseClass' cannot be extended by " + "other classes.");
}
