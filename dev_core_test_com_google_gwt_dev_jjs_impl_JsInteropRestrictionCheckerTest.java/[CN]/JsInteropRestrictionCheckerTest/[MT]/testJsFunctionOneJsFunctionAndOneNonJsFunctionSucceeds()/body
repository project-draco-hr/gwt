{
  addAll(jsFunctionInterface1,plainInterface);
  addSnippetClassDecl("public static class Buggy implements MyJsFunctionInterface1, MyPlainInterface {","  public int foo(int x) { return 0; }","}");
  assertCompileSucceeds("new Buggy();");
}
