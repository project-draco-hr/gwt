{
  if (!consoleLogEnabled()) {
    return;
  }
  log(message);
  if (t != null) {
    String out=makeSimpleStackTrace(t);
    if (consoleErrorEnabled()) {
      error(out);
    }
 else {
      log(out);
    }
  }
}
