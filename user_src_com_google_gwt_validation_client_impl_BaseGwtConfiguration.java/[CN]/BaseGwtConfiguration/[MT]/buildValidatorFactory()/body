{
  GwtConfigurationState configurationState=new GwtConfigurationState(constraintValidatorFactory,messageInterpolator,properties,traversableResolver);
  return provider.buildValidatorFactory(configurationState);
}
