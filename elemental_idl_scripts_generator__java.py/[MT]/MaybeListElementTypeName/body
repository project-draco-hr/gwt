def MaybeListElementTypeName(type_name):
    'Returns the List element type T from string of form "List<T>", or None.'
    match = re.match('sequence<(\\w*)>$', type_name)
    if match:
        return match.group(1)
    return None
