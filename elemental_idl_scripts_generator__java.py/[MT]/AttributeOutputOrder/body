def AttributeOutputOrder(a, b):
    'Canonical output ordering for attributes.'
    if (a.id < b.id):
        return (-1)
    if (a.id > b.id):
        return 1
    if (a.is_fc_setter < b.is_fc_setter):
        return (-1)
    if (a.is_fc_setter > b.is_fc_setter):
        return 1
    return 0
