{
  super(getter);
  this.shim=bean.as();
  Method found;
  try {
    found=bean.getBeanType().getMethod("set" + getter.getName().substring(3),getter.getReturnType());
  }
 catch (  NoSuchMethodException expected) {
    found=null;
  }
  setter=found;
}
