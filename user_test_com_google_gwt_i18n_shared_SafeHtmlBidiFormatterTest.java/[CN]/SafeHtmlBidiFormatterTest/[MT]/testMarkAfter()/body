{
  String text=longEn + he;
  assertEquals("markAfter(SafeHtml)",LRM_STRING,ltrFormatter.markAfter(toSafeHtml(text)).asString());
  assertEquals("markAfter(String)",LRM_STRING,ltrFormatter.markAfter(text).asString());
}
