{
  if (deobfuscator != null) {
    slr=deobfuscator.deobfuscateLogRecord(slr,strongName);
  }
  LogRecord lr=slr.getLogRecord();
  if (lr == null) {
    throw new RemoteLoggingException("Failed to convert SerializableLogRecord to LogRecord");
  }
  String loggerName=loggerNameOverride == null ? lr.getLoggerName() : loggerNameOverride;
  Logger logger=Logger.getLogger(loggerName);
  logger.log(lr);
}
