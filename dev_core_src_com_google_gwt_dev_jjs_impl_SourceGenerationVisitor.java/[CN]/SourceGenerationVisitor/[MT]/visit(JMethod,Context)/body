{
  if (isInitializer(x)) {
    if (x.isStatic()) {
      print(CHARS_STATIC);
    }
    accept(x.body);
  }
 else {
    super.visit(x,ctx);
    if (x.isAbstract()) {
      semi();
    }
 else {
      space();
      accept(x.body);
    }
  }
  return false;
}
