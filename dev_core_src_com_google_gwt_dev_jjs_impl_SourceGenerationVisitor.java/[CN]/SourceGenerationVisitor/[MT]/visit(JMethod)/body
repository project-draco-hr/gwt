{
  if (isInitializer(x)) {
    if (x.isStatic()) {
      print(CHARS_STATIC);
    }
    x.body.traverse(this);
  }
 else {
    super.visit(x);
    if (x.isAbstract()) {
      semi();
    }
 else {
      space();
      x.body.traverse(this);
    }
  }
  return false;
}
