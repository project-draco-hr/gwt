{
  assertEquals("markAfter(String)",LRM_STRING,ltrFormatter.markAfter(longEn + he));
  assertEquals("markAfter(String, boolean)",LRM_STRING,ltrFormatter.markAfter(longEn + he + longHtmlTag,true));
}
