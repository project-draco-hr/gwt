{
  init("<doc><import field=\"bar.Bar.s\"/></doc>","renderer.OwnerClass.Renderer");
  assertNotNull(fieldManager.lookup("s"));
  assertTrue("ui:import field must have type IMPORTED",fieldManager.lookup("s").getFieldType().equals(FieldWriterType.IMPORTED));
}
