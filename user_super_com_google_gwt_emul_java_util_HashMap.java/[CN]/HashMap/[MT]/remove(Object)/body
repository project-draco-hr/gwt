{
  int i=implFindSlot(key);
  if (i >= 0) {
    ImplMapEntry entry=entries[i];
    if (entry != null && entry.inUse) {
      entry.inUse=false;
      --fullSlots;
      return entry.getValue();
    }
  }
  return null;
}
