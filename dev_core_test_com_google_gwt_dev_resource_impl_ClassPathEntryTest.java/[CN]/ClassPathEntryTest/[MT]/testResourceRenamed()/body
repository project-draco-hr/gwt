{
  testResourceRenamed(Lists.newArrayList(createInclusivePathPrefixSet()));
  testResourceRenamed(Lists.newArrayList(createInclusivePathPrefixSet(),createInclusivePathPrefixSet(),createInclusivePathPrefixSet()));
}
