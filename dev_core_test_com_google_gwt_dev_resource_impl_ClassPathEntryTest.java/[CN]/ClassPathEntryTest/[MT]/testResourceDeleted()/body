{
  testResourceDeleted(Lists.newArrayList(createInclusivePathPrefixSet()));
  testResourceDeleted(Lists.newArrayList(createInclusivePathPrefixSet(),createInclusivePathPrefixSet(),createInclusivePathPrefixSet()));
}
