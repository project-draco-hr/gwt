{
  final int[] rval=new int[1];
  if (!invokeImpl(jsContext,jsScriptObject,methodName,thisObj,args,args.length,rval)) {
    throw new RuntimeException("Failed to invoke native method: " + methodName + " with "+ args.length+ " arguments.");
  }
  return rval[0];
}
