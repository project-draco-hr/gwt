{
  int ai=0;
  int bi=0;
  for (; ai < a.length && bi < b.length; ++ai, ++bi) {
    int c=a[ai] - b[bi];
    if (c != 0) {
      return c;
    }
  }
  if (ai == a.length && bi < b.length) {
    return -1;
  }
  if (ai < a.length && bi == b.length) {
    return 1;
  }
  return 0;
}
