def Load(self, name):
    'Returns contents of template file as a string, or raises an exception.'
    template = self.TryLoad(name)
    if (template is not None):
        return template
    raise Exception(("Could not find template '%s' on %s / %s" % (name, self._root, self._subpaths)))
