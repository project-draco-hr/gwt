{
  int number=times;
switch (sbtype) {
case APPEND:
    result=doAppend(number);
  break;
case ARRAY:
result=doArray(number);
break;
case CONCAT:
result=doConcat(number);
break;
case PUSH:
result=doPush(number);
break;
}
result=null;
}
