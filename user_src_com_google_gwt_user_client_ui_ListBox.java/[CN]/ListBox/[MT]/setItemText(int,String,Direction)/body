{
  checkIndex(index);
  if (text == null) {
    throw new NullPointerException("Cannot set an option to have null text");
  }
  getSelectElement().getOptions().getItem(index).setText(unicodeWrapIfNeeded(text,dir));
  itemTexts.set(index,text);
}
