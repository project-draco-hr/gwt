{
  if (dir == null && estimator != null) {
    dir=estimator.estimateDirection(text);
  }
  return dir == null ? text : BidiFormatter.getInstanceForCurrentLocale().unicodeWrapWithKnownDir(dir,text,false,false);
}
