{
  Map<String,GssFunction> gssFunctionMap=super.get();
  return ImmutableMap.<String,GssFunction>builder().putAll(gssFunctionMap).put(EvalFunction.getName(),new EvalFunction()).put(ValueFunction.getName(),new ValueFunction()).put(ResourceUrlFunction.getName(),new ResourceUrlFunction(context)).build();
}
