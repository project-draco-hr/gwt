{
  try {
    RPCTypeCheckFactory strFactory=new RPCTypeCheckFactory(ContainerParamTestClass.class,"testVector");
    Vector<HashSet> vector=new Vector<HashSet>();
    vector.add(RPCTypeCheckFactory.generateTestHashSet());
    strFactory.write(vector);
    return strFactory.toString();
  }
 catch (  Exception e) {
    fail(e.getMessage());
    return null;
  }
}
