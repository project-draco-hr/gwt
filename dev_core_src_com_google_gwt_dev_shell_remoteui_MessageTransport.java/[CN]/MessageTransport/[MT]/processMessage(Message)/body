{
switch (message.getMessageType()) {
case RESPONSE:
{
      processServerResponse(message.getResponse().getRequestId(),message.getResponse());
      break;
    }
case REQUEST:
{
    processClientRequest(message.getRequest().getRequestId(),message.getRequest());
    break;
  }
default :
{
  break;
}
}
}
