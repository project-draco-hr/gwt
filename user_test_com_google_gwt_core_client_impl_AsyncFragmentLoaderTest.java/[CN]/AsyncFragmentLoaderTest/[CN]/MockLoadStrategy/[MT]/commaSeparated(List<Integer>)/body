{
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  Integer x : ary) {
    if (first) {
      first=false;
    }
 else {
      sb.append(",");
    }
    sb.append(x);
  }
  return sb.toString();
}
