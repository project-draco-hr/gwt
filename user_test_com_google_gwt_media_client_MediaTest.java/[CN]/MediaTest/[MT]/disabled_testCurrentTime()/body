{
  MediaElement element=getElement();
  if (element == null) {
    return;
  }
  Assert.assertTrue("currentTime must be positive.",element.getCurrentTime() >= 0.0);
  double seekTime=2.0;
  element.setCurrentTime(seekTime);
  Assert.assertEquals("currentTime must be able to be set.",seekTime,element.getCurrentTime());
}
