{
  Preconditions.checkArgument(Name.isSourceName(name),"Module name " + name + " is not a valid source name.");
  this.name=name;
  this.resources=resources;
  this.monolithic=monolithic;
  defaultFilters=new DefaultFilters();
}
