{
  idFactory=new IdFactory(){
    @Override @SuppressWarnings("unchecked") protected <P extends EntityProxy>Class<P> getTypeFromToken(    String typeToken){
      return (Class<P>)service.resolveClass(typeToken);
    }
    @Override protected String getTypeToken(    Class<?> clazz){
      return service.getTypeToken(clazz);
    }
  }
;
  domainObjectsToClientId=new IdentityHashMap<Object,Integer>();
}
