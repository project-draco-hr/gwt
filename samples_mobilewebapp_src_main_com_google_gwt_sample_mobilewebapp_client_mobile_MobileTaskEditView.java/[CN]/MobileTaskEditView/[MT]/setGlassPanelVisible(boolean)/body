{
  if (glassPanel == null) {
    glassPanel=new DecoratedPopupPanel(false,true);
    glassPanel.setWidget(new Label("Loading..."));
  }
  if (visible) {
    glassPanel.center();
  }
 else {
    glassPanel.hide();
  }
}
