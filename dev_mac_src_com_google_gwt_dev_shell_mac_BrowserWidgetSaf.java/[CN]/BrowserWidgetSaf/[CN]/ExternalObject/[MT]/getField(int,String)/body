{
  if ("gwtonload".equalsIgnoreCase(name)) {
    return LowLevelSaf.wrapDispatchMethod(jsContext,"gwtOnload",new GwtOnLoad());
  }
 else   if ("initmodule".equalsIgnoreCase(name)) {
    return LowLevelSaf.wrapDispatchMethod(jsContext,"initModule",new InitModule());
  }
  return LowLevelSaf.getJsUndefined(jsContext);
}
