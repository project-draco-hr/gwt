{
  List<E> values=new ArrayList<E>();
  for (int i=0; i < 31; ++i) {
    int mask=0x1 << i;
    if ((enumValues & mask) != 0) {
      values.add(allEnums[i]);
    }
  }
  iterator=values.iterator();
}
