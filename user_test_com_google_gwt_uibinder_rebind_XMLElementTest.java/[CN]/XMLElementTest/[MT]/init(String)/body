{
  doc=DocumentTestHelp.documentForString(domString);
  item=(Element)doc.getDocumentElement().getElementsByTagName("elm").item(0);
  elemProvider=new XMLElementProviderImpl(new AttributeParsers(),null,new DummyMortalLogger());
  elm=elemProvider.get(item);
}
