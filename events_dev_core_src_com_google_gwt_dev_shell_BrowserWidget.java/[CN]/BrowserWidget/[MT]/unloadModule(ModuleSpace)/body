{
  String moduleName=moduleSpace.getModuleName();
  Object key=moduleSpace.getKey();
  moduleSpace.dispose();
  logger.log(TreeLogger.SPAM,"Unloading module " + moduleName + " (id "+ key.toString()+ ")",null);
}
