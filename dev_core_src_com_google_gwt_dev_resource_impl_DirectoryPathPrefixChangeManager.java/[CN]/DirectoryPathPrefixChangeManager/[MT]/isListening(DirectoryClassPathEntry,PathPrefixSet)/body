{
synchronized (changedFileAccumulatorsByDirectoryAndPathPrefix) {
    return changedFileAccumulatorsByDirectoryAndPathPrefix.containsKey(new DirectoryAndPathPrefix(directoryClassPathEntry,pathPrefixSet));
  }
}
