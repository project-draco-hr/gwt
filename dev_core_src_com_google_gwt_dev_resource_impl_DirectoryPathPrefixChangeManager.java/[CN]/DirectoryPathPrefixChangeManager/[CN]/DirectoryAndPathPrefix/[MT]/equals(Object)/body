{
  if (object instanceof DirectoryAndPathPrefix) {
    DirectoryAndPathPrefix directoryAndPathPrefix=(DirectoryAndPathPrefix)object;
    return Objects.equals(directoryClassPathEntryReference.get(),directoryAndPathPrefix.directoryClassPathEntryReference.get()) && Objects.equals(pathPrefixSetReference.get(),directoryAndPathPrefix.pathPrefixSetReference.get());
  }
  return false;
}
