{
  JsFunctionInterface f=new JsFunctionInterface(){
    @Override public Double m(){
      return new Double(2.0);
    }
  }
;
  assertEquals(2,f.callM().intValue());
  assertEquals(5,createNative().callM().intValue());
}
