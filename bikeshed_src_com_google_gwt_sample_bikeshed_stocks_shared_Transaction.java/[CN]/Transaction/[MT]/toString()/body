{
  String op=isBuy ? "Bought" : "Sold";
  if (actualPrice == -1) {
    return op + " " + quantity+ " shares of "+ ticker;
  }
 else {
    return op + " " + quantity+ " shares of "+ ticker+ " at "+ NumberFormat.getCurrencyFormat("USD").format(actualPrice / 100.0);
  }
}
