{
  checkDelegate();
  SimpleViolation.pushViolations(SimpleViolation.iterableFromConstrantViolations(violations),delegateMap);
  errors=new ArrayList<EditorError>();
  delegate.flushErrors(errors);
  return hasErrors();
}
