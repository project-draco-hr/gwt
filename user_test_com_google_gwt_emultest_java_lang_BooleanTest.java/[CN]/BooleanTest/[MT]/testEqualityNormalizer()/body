{
  Boolean b=false;
  if (b != null) {
    assertEquals(b.booleanValue(),false);
  }
 else {
    fail("false should not evaluate to null");
  }
  Object s="";
  assertTrue(b != s);
  Object d=0.0;
  assertTrue(b != d);
}
