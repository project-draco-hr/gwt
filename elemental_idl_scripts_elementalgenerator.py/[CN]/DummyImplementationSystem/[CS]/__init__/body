def __init__(self, templates, database, emitters, output_dir):
    super(DummyImplementationSystem, self).__init__(templates, database, emitters, output_dir)
    factory_providers_file = os.path.join(self._output_dir, 'src', 'dummy', 'RegularFactoryProviders.dart')
    self._factory_providers_emitter = self._emitters.FileEmitter(factory_providers_file)
    self._impl_file_paths = [factory_providers_file]
