{
  this.toEvaluate=toEvaluate;
  this.unevaluated=new ArrayList<JsName>(toEvaluate);
  new JsVisitor(){
    @Override public void endVisit(    JsParameter x,    JsContext<JsParameter> ctx){
      paramsOrLocals.add(x.getName());
    }
    @Override public boolean visit(    JsVar x,    JsContext<JsVar> ctx){
      paramsOrLocals.add(x.getName());
      return true;
    }
  }
.accept(callee);
}
