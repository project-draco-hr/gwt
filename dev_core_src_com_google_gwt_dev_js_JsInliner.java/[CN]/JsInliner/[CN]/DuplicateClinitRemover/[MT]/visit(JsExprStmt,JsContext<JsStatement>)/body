{
  if (isDuplicateClinit(x.getExpression())) {
    if (ctx.canRemove()) {
      ctx.removeMe();
    }
 else {
      ctx.replaceMe(program.getEmptyStmt());
    }
    return false;
  }
 else {
    return true;
  }
}
