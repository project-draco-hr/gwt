{
  if (functionStack.peek() == programFunction) {
    if (x.getExpression() instanceof JsInvocation) {
      return safeToInlineAtTopLevel.contains(JsUtils.maybeGetFunctionName(x.getExpression()));
    }
  }
  return true;
}
