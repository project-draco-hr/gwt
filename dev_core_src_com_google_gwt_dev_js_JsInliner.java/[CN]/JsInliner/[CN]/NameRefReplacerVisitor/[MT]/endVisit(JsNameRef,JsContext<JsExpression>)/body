{
  if (x.getQualifier() != null) {
    return;
  }
  JsExpression replacement=tryGetReplacementExpression(x.getSourceInfo().makeChild(NameRefReplacerVisitor.class,"Inlined expression"),x.getName());
  if (replacement != null) {
    ctx.replaceMe(replacement);
  }
}
