{
  if (x.getQualifier() != null) {
    return;
  }
  JsExpression replacement=tryGetReplacementExpression(x.getSourceInfo().makeChild("Inlined expression"),x.getName());
  if (replacement != null) {
    ctx.replaceMe(replacement);
  }
}
