{
  generatorContext=new MockGeneratorContext(compilerContext,CompilationStateBuilder.buildFrom(TreeLogger.NULL,compilerContext,Sets.<Resource>newHashSet()),null,true,false);
  compiler=new MockLibraryJavaToJavaScriptCompiler(TreeLogger.NULL,compilerContext,generatorContext);
  precompiler=compiler.createPrecompiler();
}
