{
  try {
    String localeValue=generatorContext.getPropertyOracle().getSelectionProperty(logger,"locale").getCurrentValue();
    PrintWriter pw=generatorContext.tryCreate(logger,"com.google.gwt",localeValue + typeName);
    if (pw != null) {
      generatorContext.commit(logger,pw);
    }
    return typeName + localeValue;
  }
 catch (  BadPropertyValueException e) {
    throw new UnableToCompleteException();
  }
}
