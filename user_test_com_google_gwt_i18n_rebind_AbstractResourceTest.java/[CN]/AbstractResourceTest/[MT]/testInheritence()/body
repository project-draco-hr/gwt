{
  ResourceFactory.clearCache();
  AbstractResource resource=ResourceFactory.getBundle(ColorsAndShapes.class,LOCALE_NAME_PIGLATIN,true);
  assertEquals(LOCALE_NAME_PIGLATIN,resource.getLocaleName());
  assertEquals("ueblay",resource.getString("blue"));
  assertEquals("??r????",resource.getString("grey"));
}
