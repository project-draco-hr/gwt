{
  JsStringLiteral lit=stringLiteralMap.get(value);
  if (lit == null) {
    lit=new JsStringLiteral(stringPoolSourceInfo.makeChild(JsProgram.class,"String literal: " + value),value);
    stringLiteralMap.put(value,lit);
  }
  lit.getSourceInfo().merge(sourceInfo);
  return lit;
}
