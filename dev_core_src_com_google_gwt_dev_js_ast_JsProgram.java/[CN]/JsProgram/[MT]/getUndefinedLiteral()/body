{
  SourceInfo info=createSourceInfoSynthetic(JsProgram.class,"undefined reference");
  info.addCorrelation(correlator.by(Literal.JS_UNDEFINED));
  return rootScope.findExistingName("undefined").makeRef(info);
}
