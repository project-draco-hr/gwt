{
  DeckPanel deck=new DeckPanel();
  RootPanel.get().add(deck);
  Label content=new Label("detached"){
    @Override public void onLoad(){
      assertTrue(this.getOffsetHeight() > 0);
      assertTrue("Expect positive offsetWidth. " + "This will fail in WebKit if run headless",this.getOffsetWidth() > 0);
      setText("attached");
    }
  }
;
  deck.add(content);
  assertEquals("attached",content.getText());
}
