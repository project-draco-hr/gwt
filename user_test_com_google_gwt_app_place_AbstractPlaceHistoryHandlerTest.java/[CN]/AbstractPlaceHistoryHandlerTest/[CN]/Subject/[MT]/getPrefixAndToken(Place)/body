{
  if (newPlace instanceof Place1) {
    return new PrefixAndToken(PREFIX1,factory.getTokenizer1().getToken((Place1)newPlace));
  }
  if (newPlace instanceof Place2) {
    return new PrefixAndToken(PREFIX2,new Tokenizer2().getToken((Place2)newPlace));
  }
  return null;
}
