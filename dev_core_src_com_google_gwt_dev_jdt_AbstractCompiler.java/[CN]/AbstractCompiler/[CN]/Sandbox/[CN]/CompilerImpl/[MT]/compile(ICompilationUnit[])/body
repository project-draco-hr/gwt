{
  SpeedTracerLogger.Event compileEvent=SpeedTracerLogger.start(CompilerEventType.JDT_COMPILER_SANDBOX);
  try {
    super.compile(sourceUnits);
  }
  finally {
    compileEvent.end();
  }
  cuds=null;
}
