{
  Set serializationIssues=mti.getSerializationIssues();
  Iterator iter=serializationIssues.iterator();
  while (iter.hasNext()) {
    MetaTypeInfo.SerializationIssue serializationIssue=(MetaTypeInfo.SerializationIssue)iter.next();
    logger.branch(serializationIssue.issueType,serializationIssue.issueMessage,null);
  }
}
