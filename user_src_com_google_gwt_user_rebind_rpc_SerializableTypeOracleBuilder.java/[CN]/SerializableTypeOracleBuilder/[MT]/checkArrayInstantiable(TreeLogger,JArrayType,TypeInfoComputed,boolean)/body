{
  TreeLogger branch=logger.branch(TreeLogger.DEBUG,"Analyzing component type:",null);
  boolean success=checkTypeInstantiable(branch,arrayType.getComponentType(),isSpeculative);
  tic.setInstantiable(success);
  return success;
}
