{
  logger=logger.branch(TreeLogger.INFO,"Identified " + types.length + " serializable type"+ ((types.length == 1) ? "" : "s"),null);
  for (int i=0; i < types.length; ++i) {
    logger.branch(TreeLogger.INFO,types[i].getParameterizedQualifiedSourceName(),null);
  }
}
