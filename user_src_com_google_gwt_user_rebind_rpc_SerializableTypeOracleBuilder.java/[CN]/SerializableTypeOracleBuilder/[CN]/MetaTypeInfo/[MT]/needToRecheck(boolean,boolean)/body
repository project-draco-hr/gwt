{
  assert(state == SerializableTypeOracleBuilder.CHECK_SUCCEEDED);
  if (!qualifiesForSerialization() && !inManualContext && getCheckedInManualContext()) {
    return true;
  }
  if (checkSubtypes && !getCheckedSubtypes()) {
    return true;
  }
  return false;
}
