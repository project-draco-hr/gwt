{
  TreeLogger branch=logger.branch(TreeLogger.DEBUG,"Analyzing component type:",null);
  return checkTypeInstantiable(branch,arrayType.getComponentType(),isSpeculative,createArrayComponentPath(arrayType,parent));
}
