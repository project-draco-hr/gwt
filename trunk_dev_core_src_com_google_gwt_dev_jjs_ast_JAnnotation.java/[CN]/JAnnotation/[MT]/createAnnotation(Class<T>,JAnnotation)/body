{
  Object o=Proxy.newProxyInstance(clazz.getClassLoader(),new Class<?>[]{clazz},new AnnotationInvocationHandler<T>(clazz,annotation));
  @SuppressWarnings("unchecked") T toReturn=(T)o;
  return toReturn;
}
