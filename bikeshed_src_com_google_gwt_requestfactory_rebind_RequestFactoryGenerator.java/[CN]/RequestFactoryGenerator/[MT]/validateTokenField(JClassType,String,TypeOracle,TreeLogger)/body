{
  JField field=publicRecordType.getField(fieldName);
  if (field == null || field.getType() != typeOracle.findType("java.lang.String")) {
    logger.log(TreeLogger.ERROR,"The field " + fieldName + " of type String must be defined on "+ publicRecordType.getName());
    throw new UnableToCompleteException();
  }
}
