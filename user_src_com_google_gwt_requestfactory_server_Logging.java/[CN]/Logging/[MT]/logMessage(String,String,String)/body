{
  Level level=Level.SEVERE;
  try {
    level=Level.parse(levelString);
  }
 catch (  IllegalArgumentException e) {
    return 0L;
  }
  String message=String.format("Client Side Logger: %s Message: %s",loggerName,originalMessage);
  logger.log(level,message);
  return 1L;
}
