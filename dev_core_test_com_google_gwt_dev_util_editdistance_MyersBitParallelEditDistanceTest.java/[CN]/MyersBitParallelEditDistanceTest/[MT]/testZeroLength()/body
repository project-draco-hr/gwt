{
  assertEquals(0,generate("").getDistance("",1));
  String other="other";
  GeneralEditDistanceTest.genericVerification(generate(""),"",other,other.length());
  GeneralEditDistanceTest.genericVerification(generate(other),other,"",other.length());
}
