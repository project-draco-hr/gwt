{
  return new ValueStoreListViewAdapter<K>(){
    @Override protected void onRangeChanged(    ListView<Values<K>> view){
      Range range=view.getRange();
      getDelegate().onRangeChanged(range.getStart(),range.getLength());
    }
  }
;
}
