{
  return new ListModelAdapter<K>(){
    @Override protected void onRangeChanged(    ListRegistration<Values<K>> reg,    int start,    int length){
      getDelegate().onRangeChanged(start,length);
    }
  }
;
}
