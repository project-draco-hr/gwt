{
  String name=signature.substring(0,signature.indexOf('('));
  JMethod result=new JMethod(SourceOrigin.UNKNOWN,name,enclosingType,null,false,false,false,false);
  result.signature=signature;
  return result;
}
