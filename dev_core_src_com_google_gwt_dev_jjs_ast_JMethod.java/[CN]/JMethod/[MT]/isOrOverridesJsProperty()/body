{
  if (isJsProperty()) {
    return true;
  }
  for (  JMethod overriddenMethod : getOverriddenMethods()) {
    if (overriddenMethod.isOrOverridesJsProperty()) {
      return true;
    }
  }
  return false;
}
