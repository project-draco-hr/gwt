{
  String namespace=getExportNamespace();
  if (jsName.isEmpty()) {
    return namespace;
  }
 else   if (namespace.isEmpty()) {
    return jsName;
  }
 else {
    return namespace + "." + jsName;
  }
}
