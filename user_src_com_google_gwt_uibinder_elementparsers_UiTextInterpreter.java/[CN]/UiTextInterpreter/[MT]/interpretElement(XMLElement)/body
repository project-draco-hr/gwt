{
  if (BINDER_URI.equals(elem.getNamespaceUri()) && LOCAL_NAME.equals(elem.getLocalName())) {
    String fieldRef=elem.consumeStringAttribute("from");
    if (fieldRef == null) {
      logger.die("Attribute 'from' not found in '%s'.",elem);
    }
    return "\" + " + fieldRef + " + \"";
  }
  return null;
}
