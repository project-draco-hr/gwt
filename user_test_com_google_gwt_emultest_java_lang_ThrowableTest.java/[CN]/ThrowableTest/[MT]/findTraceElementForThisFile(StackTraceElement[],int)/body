{
  if (trace == null) {
    throw new RuntimeException("null trace");
  }
  for (int i=0; i < maxDepthToExamine && i < trace.length; i++) {
    StackTraceElement e=trace[i];
    if (e.getFileName() != null && e.getFileName().contains("ThrowableTest")) {
      return e;
    }
  }
  return null;
}
