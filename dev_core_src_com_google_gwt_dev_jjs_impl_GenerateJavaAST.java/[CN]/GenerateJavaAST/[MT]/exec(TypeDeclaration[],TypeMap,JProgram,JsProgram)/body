{
  JavaASTGenerationVisitor v=new JavaASTGenerationVisitor(typeMap,jprogram,jsProgram);
  for (int i=0; i < types.length; ++i) {
    v.processType(types[i]);
  }
  Collections.sort(jprogram.getDeclaredTypes(),new HasNameSort());
}
