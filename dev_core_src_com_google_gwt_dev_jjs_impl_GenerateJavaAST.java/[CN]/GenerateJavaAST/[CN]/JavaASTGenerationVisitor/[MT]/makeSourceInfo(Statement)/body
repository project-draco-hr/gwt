{
  int startLine=Util.getLineNumber(x.sourceStart,currentSeparatorPositions,0,currentSeparatorPositions.length - 1);
  return program.createSourceInfo(x.sourceStart,x.sourceEnd,startLine,currentFileName);
}
