{
  if (expected != expression.getType()) {
    JReferenceType toType=(JReferenceType)expected;
    return new JCastOperation(program,expression.getSourceInfo(),toType,expression);
  }
 else {
    return expression;
  }
}
