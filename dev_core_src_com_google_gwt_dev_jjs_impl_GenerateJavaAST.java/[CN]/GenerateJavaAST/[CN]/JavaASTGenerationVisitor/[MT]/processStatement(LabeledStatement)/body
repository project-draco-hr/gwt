{
  JStatement body=dispProcessStatement(x.statement);
  if (body == null) {
    return null;
  }
  JSourceInfo info=makeSourceInfo(x);
  return new JLabeledStatement(program,info,getOrCreateLabel(info,currentMethod,x.label),body);
}
