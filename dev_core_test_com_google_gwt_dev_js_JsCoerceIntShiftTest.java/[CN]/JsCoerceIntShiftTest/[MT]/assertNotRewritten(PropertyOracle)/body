{
  assertEquals("a<<b;",process("a<<b;",oracles));
  assertEquals("a>>b;",process("a>>b;",oracles));
  assertEquals("a>>>b;",process("a>>>b;",oracles));
  assertEquals("1+1>>2;",process("1+1>>2;",oracles));
}
