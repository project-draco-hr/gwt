{
  assertEquals("The pig's in a hurry",widgetUi.getMenuItemLegacy().getElement().getInnerText());
  assertTrue("Style should include \"moppy\"",widgetUi.getMenuItemMop().getStyleName().contains("moppy"));
}
