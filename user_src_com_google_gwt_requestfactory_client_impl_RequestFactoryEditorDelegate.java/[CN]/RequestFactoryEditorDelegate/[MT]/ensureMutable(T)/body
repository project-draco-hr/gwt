{
  if (request == null) {
    return object;
  }
  if (object instanceof EntityProxy) {
    @SuppressWarnings("unchecked") T toReturn=(T)request.edit((EntityProxy)object);
    return toReturn;
  }
  return object;
}
