{
  if (request == null) {
    throw new IllegalStateException("No Request");
  }
 else   if (object instanceof EntityProxy) {
    @SuppressWarnings("unchecked") P toReturn=(P)request.edit(((EntityProxy)object));
    return toReturn;
  }
 else {
    return object;
  }
}
