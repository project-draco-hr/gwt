{
  String input=Joiner.on('\n').join("function m_forceInline(a,b,c) {a[c]=b;}","function b1(a) { m_forceInline(a++,a++,a++);} b1();");
  assertCheckerError(input,"Function m_forceInline is marked as @ForceInline but it could not be inlined");
}
