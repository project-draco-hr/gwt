{
  try {
    node.traverse(this,ctx);
  }
 catch (  InternalCompilerException ice) {
    ice.addNode(node);
    throw ice;
  }
catch (  Throwable e) {
    InternalCompilerException ice=new InternalCompilerException("Unexpected error during visit.",e);
    ice.addNode(node);
    throw ice;
  }
}
