{
  Object key=getKey(object);
  if (selected) {
    curSelection=object;
    curKey=key;
  }
 else   if (curKey != null && curKey.equals(key)) {
    curSelection=null;
    curKey=null;
  }
  scheduleSelectionChangeEvent();
}
