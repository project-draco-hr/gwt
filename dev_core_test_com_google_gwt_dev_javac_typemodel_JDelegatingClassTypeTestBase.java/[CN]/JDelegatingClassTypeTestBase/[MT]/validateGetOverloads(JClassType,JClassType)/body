{
  JMethod[] methods=preSubstitution.getMethods();
  for (  JMethod method : methods) {
    assertEquals(preSubstitution.getOverloads(method.getName()).length,postSubstitution.getOverloads(method.getName()).length);
  }
}
