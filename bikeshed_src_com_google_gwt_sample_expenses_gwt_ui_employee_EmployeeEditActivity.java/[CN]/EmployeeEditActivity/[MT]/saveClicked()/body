{
  if (deltas.isChanged()) {
    view.setEnabled(false);
    DeltaValueStore toCommit=deltas;
    deltas=null;
    requests.syncRequest(toCommit).fire();
    placeController.goTo(new ListScaffoldPlace(EmployeeRecord.class));
  }
}
