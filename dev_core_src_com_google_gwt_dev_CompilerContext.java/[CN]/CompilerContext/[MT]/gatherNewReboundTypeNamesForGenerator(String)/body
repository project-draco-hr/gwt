{
  Set<String> newReboundTypeNames=getLibraryGroup().gatherNewReboundTypeSourceNamesForGenerator(generatorName);
  newReboundTypeNames.addAll(libraryWriter.getReboundTypeSourceNames());
  return newReboundTypeNames;
}
