{
  Set<String> newReboundTypeNames=getLibraryGroup().gatherNewReboundTypeNamesForGenerator(generatorName);
  newReboundTypeNames.addAll(libraryBuilder.getReboundTypeNames());
  return newReboundTypeNames;
}
