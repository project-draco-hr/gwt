{
  CollectionsTestServiceAsync service=getServiceAsync();
  delayTestFinishForRpc();
  service.echoUnmodifiableMap(TestSetFactory.createUnmodifiableMap(),new AsyncCallback<Map<Integer,MarkerTypeUnmodifiable>>(){
    public void onFailure(    Throwable caught){
      TestSetValidator.rethrowException(caught);
    }
    public void onSuccess(    Map<Integer,MarkerTypeUnmodifiable> result){
      assertNotNull(result);
      assertTrue(TestSetValidator.isValidUnmodifiableMap(result));
      finishTest();
    }
  }
);
}
