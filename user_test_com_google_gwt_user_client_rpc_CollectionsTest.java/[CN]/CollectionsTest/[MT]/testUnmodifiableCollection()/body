{
  CollectionsTestServiceAsync service=getServiceAsync();
  delayTestFinishForRpc();
  service.echoUnmodifiableCollection(TestSetFactory.createUnmodifiableCollection(),new AsyncCallback<Collection<MarkerTypeUnmodifiable>>(){
    public void onFailure(    Throwable caught){
      TestSetValidator.rethrowException(caught);
    }
    public void onSuccess(    Collection<MarkerTypeUnmodifiable> result){
      assertNotNull(result);
      assertTrue(TestSetValidator.isValidUnmodifiableCollection(result));
      finishTest();
    }
  }
);
}
