{
  newJsoRecord=ProxyJsoImpl.emptyCopy(newJsoRecord);
  EntityProxy javaRecord=newJsoRecord.getSchema().create(newJsoRecord);
  eventBus.fireEvent(newJsoRecord.getSchema().createChangeEvent(javaRecord,op));
}
