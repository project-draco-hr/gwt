{
  if (isMinValue(a)) {
    if (computeRemainder) {
      remainder=create();
    }
    return create(BigLongLib.Const.ONE);
  }
  if (computeRemainder) {
    remainder=create(a);
  }
  return create();
}
