{
  if (binding.isPrivate() || (type.getName().startsWith("java.") && !binding.isPublic() && !binding.isProtected())) {
    return;
  }
  if (binding.isConstructor()) {
    processConstructor(binding,null,type.getSourceInfo());
  }
 else {
    createMethod(binding,type.getSourceInfo());
  }
}
