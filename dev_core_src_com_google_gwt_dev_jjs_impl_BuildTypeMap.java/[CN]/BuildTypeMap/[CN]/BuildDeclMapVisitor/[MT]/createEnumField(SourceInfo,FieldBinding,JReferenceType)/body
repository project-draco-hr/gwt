{
  JType type=(JType)typeMap.get(binding.type);
  JField field=program.createEnumField(info,binding.name,(JEnumType)enclosingType,(JClassType)type,binding.original().id);
  info.addCorrelation(Correlation.by(field));
  typeMap.put(binding,field);
  return field;
}
