{
  Benchmark testCase=new MockBenchmark();
  Strategy strategy=testCase.getStrategy();
  assertEquals("com.google.gwt.benchmarks.Benchmarks",strategy.getModuleInherit());
  assertEquals("Benchmarks",strategy.getSyntheticModuleExtension());
  assertEquals("com.google.gwt.mock.Mock.Benchmarks",testCase.getSyntheticModuleName());
}
