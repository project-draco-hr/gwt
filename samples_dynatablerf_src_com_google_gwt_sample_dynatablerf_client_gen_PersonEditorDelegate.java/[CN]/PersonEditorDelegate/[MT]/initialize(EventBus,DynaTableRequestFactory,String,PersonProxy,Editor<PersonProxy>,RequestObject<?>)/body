{
  super.initialize(eventBus,factory,pathSoFar,object,editor,editRequest);
{
    @SuppressWarnings("unchecked") Editor<AddressProxy> addressEditor=(Editor<AddressProxy>)editor.getEditorForPath(ADDRESS);
    if (addressEditor != null) {
      addressDelegate=new AddressEditorDelegate();
      addressDelegate.initialize(eventBus,factory,appendPath(ADDRESS),object.getAddress(),addressEditor,editRequest);
    }
  }
  pushValues(object);
}
