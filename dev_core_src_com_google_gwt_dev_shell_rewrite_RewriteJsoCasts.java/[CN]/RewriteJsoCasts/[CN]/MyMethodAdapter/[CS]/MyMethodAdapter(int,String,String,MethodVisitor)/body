{
  super(currentClass,access,name,desc,mv);
  Method m=new Method(name,desc);
  returnType=m.getReturnType();
  returnNeedsCanonical=returnType.getSort() == Type.OBJECT && rewriterOracle.jsoAssignmentRequiresCanonicalization(returnType.getInternalName());
}
