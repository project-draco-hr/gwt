{
  return type instanceof JClassType && interfaceMethod.isDefaultMethod() && (method == null || method.isDefaultMethod() && defaultMethodsByForwardingMethod.keySet().contains(method) && defaultMethodsByForwardingMethod.get(method) != interfaceMethod);
}
