{
  push(new StringBuilder());
  AutoBean<?> bean=AutoBeanUtils.getAutoBean(value);
  if (!seen.add(bean)) {
    haltOnCycle();
  }
  bean.accept(this);
  accumulator.append(pop().toString());
  seen.remove(bean);
}
