{
  dataStack.pop();
  if (dataStack.isEmpty()) {
    data=null;
  }
 else {
    data=dataStack.peek();
  }
  AutoBean<?> toReturn=beanStack.pop();
  if (beanStack.isEmpty()) {
    bean=null;
  }
 else {
    bean=beanStack.peek();
  }
  return toReturn;
}
