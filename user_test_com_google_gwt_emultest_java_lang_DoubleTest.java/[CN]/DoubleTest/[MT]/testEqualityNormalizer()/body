{
  Double d=0.0;
  if (d != null) {
    assertEquals(d.doubleValue(),0.0);
  }
 else {
    fail("0.0 should not evaluate to null");
  }
  Object s="0.0";
  assertTrue(d != s);
  Object b=Boolean.FALSE;
  assertTrue(b != s);
}
