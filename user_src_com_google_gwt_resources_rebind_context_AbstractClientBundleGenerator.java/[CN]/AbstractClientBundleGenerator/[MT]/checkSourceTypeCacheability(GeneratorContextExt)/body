{
  CachedRebindResult lastRebindResult=genContext.getCachedGeneratorResult();
  if (lastRebindResult == null || !genContext.isGeneratorResultCachingEnabled()) {
    return false;
  }
  @SuppressWarnings("unchecked") Map<String,String> cachedTypeSignatures=(Map<String,String>)lastRebindResult.getClientData(CACHED_TYPE_INFORMATION);
  return cachedTypeSignatures != null && checkCachedTypeSignatures(genContext,cachedTypeSignatures);
}
