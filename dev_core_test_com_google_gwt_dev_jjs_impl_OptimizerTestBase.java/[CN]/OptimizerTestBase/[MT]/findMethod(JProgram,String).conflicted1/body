{
  JsniRef ref=JsniRef.parse(methodJsniRef);
  assertNotNull(ref);
  assertTrue(ref.isMethod());
  return (JMethod)JsniRefLookup.findJsniRefTarget(ref,program,new ErrorReporter(){
    public void reportError(    String error){
    }
  }
);
}
