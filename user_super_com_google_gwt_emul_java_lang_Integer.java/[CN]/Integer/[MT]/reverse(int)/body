{
  int ui=i & 0x7fffffff;
  int acc=0;
  int front=0x80000000;
  int back=1;
  int swing=31;
  while (swing > 0) {
    acc=acc | ((ui & front) >> swing) | ((ui & back) << swing);
    swing-=2;
    front=front >> 1;
    back=back << 1;
  }
  if (i < 0) {
    acc=acc | 0x1;
  }
  return acc;
}
