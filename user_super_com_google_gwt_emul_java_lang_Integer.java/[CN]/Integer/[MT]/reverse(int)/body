{
  int acc=0;
  int front=0x80000000;
  int back=1;
  int swing=31;
  while (swing > 15) {
    acc=acc | ((i & front) >> swing) | ((i & back) << swing);
    swing--;
    front=front >> 1;
    back=back << 1;
  }
  return acc;
}
