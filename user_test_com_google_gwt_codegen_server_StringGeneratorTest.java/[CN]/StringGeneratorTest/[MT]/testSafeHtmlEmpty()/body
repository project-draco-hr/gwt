{
  StringBuilder buf=new StringBuilder();
  StringGenerator gen=StringGenerator.create(buf,true);
  gen.completeString();
  assertEquals("new com.google.gwt.safehtml.shared.SafeHtmlBuilder().toSafeHtml()",buf.toString());
}
