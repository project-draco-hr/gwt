{
  section=isFooter ? HtmlBuilderFactory.get().createTFootBuilder() : HtmlBuilderFactory.get().createTHeadBuilder();
  idToHeaderMap.clear();
  idToColumnMap.clear();
  if (!buildHeaderOrFooterImpl()) {
    return null;
  }
  while (section.getDepth() > 0) {
    section.end();
  }
  return section;
}
