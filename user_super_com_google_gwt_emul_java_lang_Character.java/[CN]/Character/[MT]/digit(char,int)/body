{
  if (radix < MIN_RADIX || radix > MAX_RADIX) {
    return -1;
  }
  if (c >= '0' && c <= '9') {
    return c - '0';
  }
 else   if (c >= 'a' && c < ('a' + radix - 10)) {
    return c - 'a' + 10;
  }
 else   if (c >= 'A' && c < ('A' + radix - 10)) {
    return c - 'A' + 10;
  }
  return -1;
}
