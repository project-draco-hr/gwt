{
  if (enclosingType == null) {
    return jsNamespace == null || jsNamespace.isEmpty() ? getName() : jsNamespace + "." + getLeafName();
  }
 else {
    return enclosingType.getQualifiedExportName() + "." + getLeafName();
  }
}
