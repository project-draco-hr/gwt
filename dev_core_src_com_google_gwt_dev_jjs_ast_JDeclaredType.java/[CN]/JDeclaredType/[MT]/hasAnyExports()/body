{
  for (  JMethod method : getMethods()) {
    if (method.isExported()) {
      return true;
    }
  }
  for (  JField field : getFields()) {
    if (field.isExported()) {
      return true;
    }
  }
  return false;
}
