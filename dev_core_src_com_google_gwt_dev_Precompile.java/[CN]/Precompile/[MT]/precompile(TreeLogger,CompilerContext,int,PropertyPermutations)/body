{
  Precompilation precompile=precompile(logger,compilerContext,permutationBase,allPermutations,ManagementFactory.getRuntimeMXBean().getStartTime());
  if (compilerContext.getOptions().warnOverlappingSource()) {
    compilerContext.getModule().printOverlappingSourceWarnings(logger);
  }
  return precompile;
}
