{
  PropertyPermutations allPermutations=new PropertyPermutations(compilerContext.getModule().getProperties(),compilerContext.getModule().getActiveLinkerNames());
  return precompile(logger,compilerContext,0,allPermutations);
}
