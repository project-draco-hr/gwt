{
  File workDir=super.getWorkDir();
  if (workDir == null) {
    workDir=Utility.makeTemporaryDirectory(null,"work-");
    System.err.println("Created new work directory at " + workDir.getAbsolutePath() + ", which will be removed on exit.  (Use -workDir DIRNAME to retain.)");
    super.setWorkDir(workDir);
    return true;
  }
  return false;
}
