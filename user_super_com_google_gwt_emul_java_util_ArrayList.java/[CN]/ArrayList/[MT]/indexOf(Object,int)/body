{
  if (index < 0) {
    indexOutOfBounds(index,size);
  }
  for (; index < size; ++index) {
    if (Utility.equalsWithNullCheck(o,getImpl(array,index))) {
      return index;
    }
  }
  return -1;
}
