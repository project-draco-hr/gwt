{
  if (out.length < size) {
    out=Array.createFrom(out,size);
  }
  for (int i=0; i < size; ++i) {
    out[i]=ArrayList.<T>getImpl(array,i);
  }
  if (out.length > size) {
    out[size]=null;
  }
  return out;
}
