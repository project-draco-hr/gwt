{
  if (key == null) {
    return putNullSlot(value);
  }
  V oldValue=null;
  if (stringMap.contains(key)) {
    oldValue=stringMap.get(key);
  }
 else {
    ++size;
  }
  stringMap.set(key,value);
  return oldValue;
}
