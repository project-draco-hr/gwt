{
  if (key == null) {
    return removeHashValue(null);
  }
  V value=null;
  if (stringMap.contains(key)) {
    value=stringMap.get(key);
    --size;
    stringMap.remove(key);
  }
  return value;
}
