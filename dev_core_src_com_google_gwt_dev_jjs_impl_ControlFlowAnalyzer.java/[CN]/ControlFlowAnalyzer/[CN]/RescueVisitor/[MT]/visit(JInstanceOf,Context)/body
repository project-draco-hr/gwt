{
  JReferenceType targetType=x.getTestType();
  if (program.typeOracle.isJsType(targetType) && ((JDeclaredType)targetType).getJsPrototype() != null) {
    rescue(targetType,true,true);
  }
  return true;
}
