{
class Foo {
    int i;
    Foo(    int i){
      this.i=i;
      if (i == 0)       return;
 else       if (i == 1)       return;
      return;
    }
  }
  assertEquals(new Foo(0).i,0);
  assertEquals(new Foo(1).i,1);
  assertEquals(new Foo(2).i,2);
}
