{
  if (LowLevelIE6.init()) {
    return LowLevelIE6.httpGet(branch,userAgent,url,System.getProperty(PROPERTY_DEBUG_HTTP_GET) != null);
  }
 else {
    return super.doHttpGet(branch,userAgent,url);
  }
}
