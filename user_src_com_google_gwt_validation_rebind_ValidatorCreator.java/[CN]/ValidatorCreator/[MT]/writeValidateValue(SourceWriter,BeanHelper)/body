{
  sw.println("if (beanType.getClass().equals(" + bean.getTypeCanonicalName() + ".class)) {");
  sw.indent();
  writeContext(sw,bean,"null");
  sw.println("return " + bean.getValidatorInstanceName() + ".validateValue(context, (Class<"+ bean.getTypeCanonicalName()+ ">)beanType, propertyName, value, groups);");
  sw.outdent();
  sw.println("}");
}
