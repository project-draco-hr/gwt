{
  for (  GwtLocale locale : allLocales.keySet()) {
    if (locale.isDefault()) {
      continue;
    }
    addCurrencyEntries(category,locale,cldrFactory,currencyFractions,defaultCurrencyFraction,stillInUse,rounding);
  }
  GwtLocale locale=localeFactory.getDefault();
  addCurrencyEntries(category,locale,cldrFactory,currencyFractions,defaultCurrencyFraction,stillInUse,rounding);
}
