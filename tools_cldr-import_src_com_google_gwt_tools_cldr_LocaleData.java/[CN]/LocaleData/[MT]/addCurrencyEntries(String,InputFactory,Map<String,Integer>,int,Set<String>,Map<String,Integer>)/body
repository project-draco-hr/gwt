{
  for (  GwtLocale locale : allLocales.keySet()) {
    if (locale.isDefault()) {
      continue;
    }
    addCurrencyEntries(category,locale,inputFactory,currencyFractions,defaultCurrencyFraction,stillInUse,rounding);
  }
  GwtLocale locale=localeFactory.getDefault();
  addCurrencyEntries(category,locale,inputFactory,currencyFractions,defaultCurrencyFraction,stillInUse,rounding);
}
