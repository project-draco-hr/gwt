{
  String name=typeName.replace('/','_');
  String prefix=name + "_";
  String suffix=name + "`";
  SortedMap<String,Method> toReturn=new TreeMap<String,Method>(mangledNamesToImplementations.subMap(prefix,suffix));
  toReturn.keySet().removeAll(implementedMethods);
  return toReturn;
}
