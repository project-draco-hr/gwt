{
  try {
    if (!startUp()) {
      return;
    }
    BootStrapPlatform.initGui();
    launchStartupUrls(getTopLogger());
    pumpEventLoop();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    shutDown();
  }
}
