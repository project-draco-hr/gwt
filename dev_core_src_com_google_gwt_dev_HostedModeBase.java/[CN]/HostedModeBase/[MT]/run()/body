{
  try {
    if (startUp()) {
      BootStrapPlatform.initGui();
      launchStartupUrls(getTopLogger());
    }
    blockUntilDone.acquire();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    shutDown();
  }
}
