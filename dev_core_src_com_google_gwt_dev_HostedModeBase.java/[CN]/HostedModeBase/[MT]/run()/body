{
  try {
    if (!startUp()) {
      return;
    }
    BootStrapPlatform.maybeInitializeAWT();
    launchStartupUrls(getTopLogger());
    pumpEventLoop();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    shutDown();
  }
}
