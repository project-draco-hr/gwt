{
  Set<JMethod> overrides=x.getOverriddenMethods();
  if (!overrides.isEmpty() || program.typeOracle.isJsTypeMethod(x) || program.typeOracle.isJsFunctionMethod(x)|| program.typeOracle.isExportedMethod(x)) {
    for (    JMethod m : overrides) {
      rescuedMethods.add(m);
    }
    rescuedMethods.add(x);
  }
  return true;
}
