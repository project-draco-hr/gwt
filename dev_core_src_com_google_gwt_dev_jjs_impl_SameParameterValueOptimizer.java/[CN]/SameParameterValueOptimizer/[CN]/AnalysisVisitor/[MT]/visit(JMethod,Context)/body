{
  Set<JMethod> overriddenMethods=x.getOverriddenMethods();
  if (!overriddenMethods.isEmpty() || program.typeOracle.isJsTypeMethod(x) || program.typeOracle.isJsFunctionMethod(x)|| program.typeOracle.isExportedMethod(x)) {
    for (    JMethod m : overriddenMethods) {
      rescuedMethods.add(m);
    }
    rescuedMethods.add(x);
  }
  return true;
}
