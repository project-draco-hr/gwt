{
  if (paramType.isClass() != null) {
    return "GWT.isScript() ? Impl.getNameOf(\"@" + paramType.getQualifiedSourceName() + "\") : \""+ TypeOracleMediator.computeBinaryClassName(paramType)+ "\"";
  }
 else {
    return "\" " + TypeOracleMediator.computeBinaryClassName(paramType) + "\"";
  }
}
