{
  if (paramType.isClass() != null) {
    return "GWT.isScript() ? Impl.getNameOf(\"@" + paramType.getQualifiedSourceName() + "\") : \""+ SerializationUtils.getRpcTypeName(paramType)+ "\"";
  }
 else {
    return "\" " + SerializationUtils.getRpcTypeName(paramType) + "\"";
  }
}
