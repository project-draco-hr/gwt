{
  element=Document.get().createDivElement();
  directionalTextHelper=new DirectionalTextHelper(element,false);
  directionalTextHelper.setText(EN_TEXT);
  assertEquals(EN_TEXT,directionalTextHelper.getText());
}
