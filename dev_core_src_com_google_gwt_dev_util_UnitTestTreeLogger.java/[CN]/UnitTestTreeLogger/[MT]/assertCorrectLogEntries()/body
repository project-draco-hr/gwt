{
  Assert.assertEquals("Log lengths do not match",expectedEntries.size(),actualEntries.size());
  for (int i=0, c=expectedEntries.size(); i < c; ++i) {
    assertCorrectLogEntry(expectedEntries.get(i),actualEntries.get(i));
  }
}
