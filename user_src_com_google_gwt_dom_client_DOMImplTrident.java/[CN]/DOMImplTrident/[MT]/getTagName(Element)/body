{
  String tagName=getTagNameInternal(elem);
  String scopeName=getScopeNameInternal(elem);
  if ("html".equalsIgnoreCase(scopeName) || "undefined".equalsIgnoreCase(scopeName)) {
    return tagName;
  }
  return scopeName + ":" + tagName;
}
