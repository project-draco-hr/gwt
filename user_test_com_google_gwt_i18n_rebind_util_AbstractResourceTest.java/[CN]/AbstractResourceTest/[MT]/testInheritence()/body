{
  ResourceFactory.clearCache();
  AbstractResource resource=ResourceFactory.getBundle(ColorsAndShapes.class,pigLatin);
  assertEquals(pigLatin,resource.getLocale());
  assertEquals("ueblay",resource.getString("blue"));
  assertEquals("??r????",resource.getString("grey"));
}
