{
  AbstractResource resource=ResourceFactory.getBundle(ColorsAndShapesAndConcepts.class,LOCALE_NAME_PIGLATIN_UK);
  String s=resource.getString("internationalization");
  assertEquals("????????r??????????????????????????????",s);
  assertTrue(resource.keySet().size() > 5);
}
