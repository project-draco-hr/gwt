{
  AbstractResource resource=ResourceFactory.getBundle(ColorsAndShapesAndConcepts.class,new Locale("piglatin","UK"));
  String s=resource.getString("internationalization");
  assertEquals("????????r??????????????????????????????",s);
  assertTrue(resource.keySet().size() > 5);
}
