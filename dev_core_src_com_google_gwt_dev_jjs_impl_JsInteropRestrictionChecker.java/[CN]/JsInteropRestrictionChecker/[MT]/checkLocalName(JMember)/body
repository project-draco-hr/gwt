{
  String jsName=member.getJsName();
  if (currentLocalNameByMemberNames.put(jsName,member.getQualifiedName()) != null) {
    logError("'%s' can't be exported in type '%s' because the name '%s' is already taken.",member.getQualifiedName(),currentType.getName(),jsName);
  }
}
