{
  if (!x.isJsProperty()) {
    return false;
  }
  if (x.needsVtable()) {
    checkJsTypeFieldName(x,x.getJsName());
  }
 else   if (currentType == x.getEnclosingType()) {
    checkExportName(x);
  }
  return false;
}
