{
  if (setter.getParams().size() != 1 || setter.getType() != JPrimitiveType.VOID) {
    logError(setter,"There needs to be single parameter and void return type for the " + "JsProperty setter %s.",getMemberDescription(setter));
  }
}
