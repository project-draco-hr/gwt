{
  if (currentJsMethodNameBySetterNames.containsKey(setterName) && currentLocalNameByMemberNames.containsKey(setterName)) {
    logError("'%s' and '%s' can't both be named '%s' in type '%s'.",currentLocalNameByMemberNames.get(setterName),currentJsMethodNameBySetterNames.get(setterName),setterName,typeName);
  }
}
