{
  boolean success=currentJsTypeMemberNames.add(memberName);
  if (!success) {
    logError("'%s' can't be exported because the member name '%s' is already taken.",x.getQualifiedName(),memberName);
  }
}
