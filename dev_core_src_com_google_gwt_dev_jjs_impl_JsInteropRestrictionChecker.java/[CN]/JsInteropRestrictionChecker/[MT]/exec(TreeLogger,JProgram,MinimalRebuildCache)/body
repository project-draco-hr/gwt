{
  JsInteropRestrictionChecker jsInteropRestrictionChecker=new JsInteropRestrictionChecker(logger,jprogram,minimalRebuildCache);
  try {
    jsInteropRestrictionChecker.accept(jprogram);
  }
 catch (  InternalCompilerException e) {
    throw new UnableToCompleteException();
  }
}
