{
  if (!minimalRebuildCache.addExportedGlobalName(member.getQualifiedJsName(),member.getEnclosingType().getName())) {
    logError(member,"%s cannot be exported because the global name '%s' is already taken.",getMemberDescription(member),member.getQualifiedJsName());
  }
}
