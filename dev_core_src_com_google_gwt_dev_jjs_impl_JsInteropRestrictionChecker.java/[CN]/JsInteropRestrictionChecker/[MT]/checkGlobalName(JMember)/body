{
  if (!minimalRebuildCache.addExportedGlobalName(x.getQualifiedJsName(),currentType.getName())) {
    logError("'%s' can't be exported because the global name '%s' is already taken.",x.getQualifiedName(),x.getQualifiedJsName());
  }
}
