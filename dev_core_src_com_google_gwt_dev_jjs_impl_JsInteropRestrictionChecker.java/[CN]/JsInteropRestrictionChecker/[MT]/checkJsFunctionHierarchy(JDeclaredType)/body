{
  List<JInterfaceType> implementedJsFunctions=Lists.newArrayList(jprogram.typeOracle.getImplementedJsFunctions(type));
  Collections.sort(implementedJsFunctions,HasName.BY_NAME_COMPARATOR);
  if (implementedJsFunctions.size() > 1) {
    logError("'%s' implements more than one JsFunction interfaces: %s",type.getName(),implementedJsFunctions);
  }
}
