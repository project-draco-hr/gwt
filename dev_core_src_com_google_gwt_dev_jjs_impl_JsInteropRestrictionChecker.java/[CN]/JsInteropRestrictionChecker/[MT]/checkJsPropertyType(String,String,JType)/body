{
  JType recordedType=currentJsPropertyTypeByName.put(propertyName,type);
  if (recordedType != null && recordedType != type) {
    logError("The setter and getter for JsProperty '%s' in type '%s' must have consistent types.",propertyName,enclosingTypeName);
  }
}
