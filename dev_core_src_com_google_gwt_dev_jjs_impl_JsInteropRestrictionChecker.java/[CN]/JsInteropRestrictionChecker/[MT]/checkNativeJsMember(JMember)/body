{
  if (member.isSynthetic()) {
    return;
  }
  if (member.getJsName() == null) {
    logError("Native JsType member '%s' is not public or has @JsIgnore.",member.getQualifiedName());
    return;
  }
}
