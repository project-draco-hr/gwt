{
  if (currentJsMethodNameByGetterNames.containsKey(getterName) && currentLocalNameByMemberNames.containsKey(getterName)) {
    logError("'%s' and '%s' can't both be named '%s' in type '%s'.",currentLocalNameByMemberNames.get(getterName),currentJsMethodNameByGetterNames.get(getterName),getterName,typeName);
  }
}
