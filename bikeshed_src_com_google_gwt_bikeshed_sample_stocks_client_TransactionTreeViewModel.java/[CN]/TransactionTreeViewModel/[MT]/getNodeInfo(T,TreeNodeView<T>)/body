{
  if (value == null) {
    return new TreeViewModel.DefaultNodeInfo<String>(tickersListModel,TEXT_CELL);
  }
 else   if (value instanceof String) {
    return new TreeViewModel.DefaultNodeInfo<Transaction>(transactionListListModelsByTicker.get(value),TRANSACTION_CELL);
  }
 else   if (value instanceof Transaction) {
    return null;
  }
  throw new IllegalArgumentException(value.toString());
}
