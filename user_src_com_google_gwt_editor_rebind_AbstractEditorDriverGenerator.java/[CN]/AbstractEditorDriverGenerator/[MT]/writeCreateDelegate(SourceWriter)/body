{
  String editorDelegateName=getEditorDelegate(model.getProxyType(),model.getEditorType());
  sw.println("protected %s<%s,%s> createDelegate() {",Name.getSourceNameForClass(getEditorDelegateType()),model.getProxyType().getQualifiedSourceName(),model.getEditorType().getQualifiedSourceName());
  sw.indent();
  sw.println("return new %1$s();",editorDelegateName);
  sw.outdent();
  sw.println("}");
}
