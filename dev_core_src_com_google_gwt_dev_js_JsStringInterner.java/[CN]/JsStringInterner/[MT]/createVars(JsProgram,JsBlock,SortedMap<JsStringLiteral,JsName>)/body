{
  if (toCreate.size() > 0) {
    JsVars vars=new JsVars(program.createSourceInfoSynthetic(JsStringInterner.class,"Interned string pool"));
    SourceInfo sourceInfo=program.createSourceInfoSynthetic(JsStringInterner.class,"Interned string assignment");
    for (    Map.Entry<JsStringLiteral,JsName> entry : toCreate.entrySet()) {
      JsVar var=new JsVar(sourceInfo,entry.getValue());
      var.setInitExpr(entry.getKey());
      vars.add(var);
    }
    block.getStatements().add(0,vars);
  }
}
