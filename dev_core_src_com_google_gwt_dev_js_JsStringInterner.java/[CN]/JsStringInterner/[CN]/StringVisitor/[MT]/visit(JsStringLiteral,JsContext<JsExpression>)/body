{
  JsName name=toCreate.get(x);
  if (name == null) {
    String ident=PREFIX + lastId++;
    name=scope.declareName(ident);
    toCreate.put(x,name);
  }
  ctx.replaceMe(name.makeRef(x.getSourceInfo().makeChild("Interned reference")));
  return false;
}
