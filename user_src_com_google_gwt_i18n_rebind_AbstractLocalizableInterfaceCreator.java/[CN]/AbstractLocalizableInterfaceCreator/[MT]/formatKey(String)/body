{
  for (  ResourceKeyFormatter formatter : formatters) {
    key=formatter.format(key);
  }
  if (Util.isValidJavaIdent(key) == false) {
    throw new IllegalArgumentException(key + " is not a legitimate method name.");
  }
  return key;
}
