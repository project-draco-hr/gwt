{
  String scheme=extractScheme(uri);
  if (scheme == null) {
    return true;
  }
  String schemeLc=StringCase.toLower(scheme);
  return ("http".equals(schemeLc) || "https".equals(schemeLc) || "ftp".equals(schemeLc)|| "mailto".equals(schemeLc)|| "MAILTO".equals(StringCase.toUpper(scheme)));
}
