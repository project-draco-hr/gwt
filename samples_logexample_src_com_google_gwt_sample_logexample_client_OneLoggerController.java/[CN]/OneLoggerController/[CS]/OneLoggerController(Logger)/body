{
  this.logger=logger;
  panel=uiBinder.createAndBindUi(this);
  String name=logger.getName();
  if (name.isEmpty()) {
    name="RootLogger";
  }
  loggerName.setInnerText(name);
  addLevelButtons();
  addLogButtons();
}
