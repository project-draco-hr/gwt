{
  if (size == this.dataSize && isExact == this.dataSizeIsExact) {
    return;
  }
  this.dataSize=size;
  this.dataSizeIsExact=isExact;
  updateLoadingState();
  if (updateCachedData()) {
    redraw();
  }
  onSizeChanged();
}
