{
  Element container=getContainer(w);
  boolean removed=super.remove(w);
  if (removed) {
    DOM.removeChild(getElement(),container);
    if (visibleWidget == w) {
      visibleWidget=null;
    }
  }
  return removed;
}
