{
  if (fromType == toType) {
    return false;
  }
  if (!hasClinit(toType)) {
    return false;
  }
  if (fromType instanceof JClassType && toType instanceof JClassType && isSuperClass((JClassType)fromType,(JClassType)toType)) {
    return false;
  }
  return true;
}
