{
  AbstractTreeLogger logger=new PrintWriterTreeLogger();
  logger.setMaxDetail(com.google.gwt.core.ext.TreeLogger.ERROR);
  apiCheckLoop=new ApiContainer("ApiClassTest",new HashSet<CompilationUnit>(Arrays.asList(new StaticCompilationUnit[]{new StaticCompilationUnit("java.lang.Object",getSourceForNewObject())})),new HashSet<String>(),logger);
  apiCheck=new ApiContainer("ApiContainerTest",new HashSet<CompilationUnit>(Arrays.asList(getScuArray())),new HashSet<String>(),logger);
}
