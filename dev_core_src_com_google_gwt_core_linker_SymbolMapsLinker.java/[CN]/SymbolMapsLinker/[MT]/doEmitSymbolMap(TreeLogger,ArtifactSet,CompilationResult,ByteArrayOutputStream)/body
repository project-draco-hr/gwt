{
  EmittedArtifact symbolMapArtifact=emitBytes(logger,out.toByteArray(),result.getStrongName() + STRONG_NAME_SUFFIX);
  symbolMapArtifact.setVisibility(Visibility.LegacyDeploy);
  artifacts.add(symbolMapArtifact);
}
