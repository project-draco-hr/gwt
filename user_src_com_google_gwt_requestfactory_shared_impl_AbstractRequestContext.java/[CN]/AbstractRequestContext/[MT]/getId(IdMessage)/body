{
  SimpleEntityProxyId<EntityProxy> id;
  if (op.getClientId() > 0) {
    id=requestFactory.getId(op.getTypeToken(),op.getServerId(),op.getClientId());
  }
 else {
    id=requestFactory.getId(op.getTypeToken(),op.getServerId());
  }
  return id;
}
