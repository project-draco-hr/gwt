{
  XMLElement xmlElement=writer.get(elem);
  domCursor.visitChild(xmlElement);
  new ChildWalker().accept(elem,new GetInnerHtmlVisitor(buffer,interpreter,writer,domCursor,logger));
  domCursor.finishChild(xmlElement);
}
