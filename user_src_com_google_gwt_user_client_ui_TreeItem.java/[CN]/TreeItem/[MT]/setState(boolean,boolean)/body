{
  if (open && children.size() == 0) {
    return;
  }
  if (this.open != open) {
    this.open=open;
    updateState(true);
  }
  if (fireEvents && tree != null) {
    tree.fireStateChanged(this);
  }
}
