{
  int scrollHeight=DOM.getElementPropertyInt(curItem.childSpanElem,"scrollHeight");
  int height=(int)(progress * scrollHeight);
  if (!opening) {
    height=scrollHeight - height;
  }
  DOM.setStyleAttribute(curItem.childSpanElem,"height",height + "px");
}
