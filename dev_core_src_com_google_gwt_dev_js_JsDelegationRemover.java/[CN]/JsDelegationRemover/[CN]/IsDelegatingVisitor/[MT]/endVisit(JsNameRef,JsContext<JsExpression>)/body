{
  if (!delegating) {
    return;
  }
  if (x.getQualifier() != null) {
    return;
  }
  JsName name=x.getName();
  if (parameterNames.contains(name)) {
    if (flexibleParameters.contains(name)) {
    }
 else     if (strictParameters.indexOf(name) != 0) {
      delegating=false;
    }
 else {
      if (strictParameters.remove(0) != name) {
        throw new InternalCompilerException("Unexpected name removed from strict parameter list.");
      }
    }
  }
 else {
    boolean isStatic=name.getEnclosing() == program.getScope() || name.getEnclosing() == program.getRootScope();
    delegating&=isStatic;
  }
}
