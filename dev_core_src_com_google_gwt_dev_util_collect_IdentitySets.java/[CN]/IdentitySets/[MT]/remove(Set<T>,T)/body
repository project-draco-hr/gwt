{
switch (set.size()) {
case 0:
    return set;
case 1:
  if (set.contains(toRemove)) {
    return create();
  }
return set;
case 2:
if (set.remove(toRemove)) {
return create(set.iterator().next());
}
return set;
default :
set.remove(toRemove);
return set;
}
}
