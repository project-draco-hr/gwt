{
  StringBuilder sb=new StringBuilder();
  String[] ss=(String[])toFormat;
  for (int i=0, n=ss.length; i < n; ++i) {
    if (i > 0) {
      sb.append(", ");
    }
    sb.append(ss[i]);
  }
  return sb.toString();
}
