{
  assertValidatorFailsToCompile(TckCompileTestValidatorFactory.GwtValidator.class,ValidationException.class,Pattern.compile("Unable to create a validator for " + "org.hibernate.jsr303.tck.tests.validation.Boy " + "because Annotated methods must follow the "+ "JavaBeans naming convention. age() does not.",Pattern.LITERAL));
}
