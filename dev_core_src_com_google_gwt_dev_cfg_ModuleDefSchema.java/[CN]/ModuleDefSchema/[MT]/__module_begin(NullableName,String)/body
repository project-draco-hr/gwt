{
  if (deployTo != null && deployTo.length() > 0) {
    if (!deployTo.startsWith("/")) {
      logger.log(TreeLogger.ERROR,"deploy-to '" + deployTo + "' must begin with forward slash (e.g. '/foo')");
      throw new UnableToCompleteException();
    }
  }
  return bodySchema;
}
