{
  if (!Util.isValidJavaIdent(name)) {
    logger.log(TreeLogger.ERROR,"Linker names must be valid identifiers",null);
    throw new UnableToCompleteException();
  }
  moduleDef.addLinker(name,className);
  return null;
}
