{
{
    MutableArray<Object> b=createMutableArray();
    b.clear();
    assertEquals(0,b.size());
  }
{
    MutableArray<Object> b=createMutableArray();
    b.add("string");
    b.add(false);
    b.add(3);
    b.clear();
    assertEquals(0,b.size());
  }
}
