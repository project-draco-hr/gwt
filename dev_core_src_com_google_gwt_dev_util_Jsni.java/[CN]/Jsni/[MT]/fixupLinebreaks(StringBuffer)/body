{
  for (int i=0; i < body.length(); ++i) {
    char c=body.charAt(i);
    if (c == '\r') {
      body.setCharAt(i,'r');
      body.insert(i,'\\');
      i+=1;
    }
 else     if (c == '\n') {
      body.setCharAt(i,'n');
      body.insert(i,'\\');
      i+=1;
    }
  }
}
