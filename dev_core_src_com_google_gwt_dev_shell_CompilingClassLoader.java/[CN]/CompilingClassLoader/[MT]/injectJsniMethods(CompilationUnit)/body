{
  if (unit == null || unit.getJsniMethods() == null) {
    return;
  }
  logger.log(TreeLogger.SPAM,"Injecting JSNI methods for " + unit.getTypeName());
  shellJavaScriptHost.createNativeMethods(logger,unit.getJsniMethods(),this);
}
