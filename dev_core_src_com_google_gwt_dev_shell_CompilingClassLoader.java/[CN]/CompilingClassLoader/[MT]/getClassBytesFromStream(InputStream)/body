{
  try {
    byte classBytes[]=new byte[is.available()];
    int read=0;
    while (read < classBytes.length)     read+=is.read(classBytes,read,classBytes.length - read);
    return classBytes;
  }
  finally {
    Utility.close(is);
  }
}
