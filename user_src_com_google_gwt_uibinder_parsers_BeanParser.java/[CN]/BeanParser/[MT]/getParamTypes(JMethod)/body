{
  StringBuilder args=new StringBuilder();
  for (Iterator<JParameter> i=Arrays.asList(setter.getParameters()).iterator(); i.hasNext(); ) {
    args.append(i.next().getType().getSimpleSourceName());
    if (i.hasNext()) {
      args.append(", ");
    }
  }
  return args;
}
