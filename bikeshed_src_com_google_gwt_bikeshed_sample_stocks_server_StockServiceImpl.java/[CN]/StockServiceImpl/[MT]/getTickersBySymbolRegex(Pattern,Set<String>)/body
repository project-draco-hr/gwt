{
  if (pattern == null) {
    return;
  }
  for (  String ticker : Stocks.stockTickers) {
    if (tickers.size() >= MAX_RESULTS_TO_RETURN) {
      return;
    }
    if (match(ticker,pattern)) {
      tickers.add(ticker);
    }
  }
}
