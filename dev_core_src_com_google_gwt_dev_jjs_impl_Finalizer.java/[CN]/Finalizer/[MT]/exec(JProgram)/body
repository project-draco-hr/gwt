{
  Event optimizeEvent=SpeedTracerLogger.start(CompilerEventType.OPTIMIZE,"optimizer","Finalizer");
  boolean didChange=new Finalizer().execImpl(program);
  optimizeEvent.end("didChange","" + didChange);
  return didChange;
}
