{
  SpeedTracerLogger.start(CompilerEventType.OPTIMIZE,"optimizer","Finalizer");
  boolean didChange=new Finalizer().execImpl(program);
  SpeedTracerLogger.end(CompilerEventType.OPTIMIZE,"didChange","" + didChange);
  return didChange;
}
