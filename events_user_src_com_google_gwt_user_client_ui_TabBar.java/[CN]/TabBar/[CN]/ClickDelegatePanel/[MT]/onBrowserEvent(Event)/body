{
  if (!enabled) {
    return;
  }
switch (DOM.eventGetType(event)) {
case Event.ONCLICK:
    TabBar.this.onClick(this);
  break;
case Event.ONKEYDOWN:
TabBar.this.onKeyDown(this,((char)DOM.eventGetKeyCode(event)),KeyEvent.getKeyModifiers(event));
break;
}
}
