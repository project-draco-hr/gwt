{
  File f=new File(fileName);
  if (f.exists()) {
    return f.toURI();
  }
 else {
    try {
      return new URI(fileName);
    }
 catch (    URISyntaxException e) {
      return null;
    }
  }
}
