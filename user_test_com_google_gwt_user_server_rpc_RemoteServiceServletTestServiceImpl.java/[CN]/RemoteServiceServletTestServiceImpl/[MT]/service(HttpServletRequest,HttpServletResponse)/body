{
  if (req.getPathInfo() != null && req.getPathInfo().endsWith("404")) {
    resp.sendError(HttpServletResponse.SC_NOT_FOUND,"Intentionally not found URL");
    return;
  }
  super.service(req,resp);
}
