{
  if (value == null) {
    throw new IllegalArgumentException("value must not be null");
  }
  if (isChecked() == value) {
    return;
  }
  setChecked(value);
  if (fireEvents) {
    ValueChangeEvent.fire(this,value);
  }
}
