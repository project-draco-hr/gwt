{
  if (w.getParent() != this) {
    return false;
  }
  Element td=DOM.getParent(w.getElement());
  Element tr=DOM.getParent(td);
  DOM.removeChild(getBody(),tr);
  super.remove(w);
  return true;
}
