{
  String str="????t??r????t????????l??z??t??????";
  byte[] bytes=str.getBytes("ISO-8859-1");
  assertEquals(str.length(),bytes.length);
  for (int i=0; i < str.length(); ++i) {
    assertEquals("latin1 byte " + i + " differs",(byte)str.charAt(i),bytes[i]);
  }
}
