{
  A a;
  if (assumptions != null) {
    a=assumptions.get(edge);
  }
 else {
    a=assumptionMap.getAssumption(edge);
  }
  if (a != null) {
    result.append(" ");
    result.append(a.toString());
  }
}
