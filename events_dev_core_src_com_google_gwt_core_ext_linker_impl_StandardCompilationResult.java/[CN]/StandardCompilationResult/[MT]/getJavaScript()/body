{
  String toReturn=null;
  if (js != null) {
    toReturn=js.get();
  }
  if (toReturn == null) {
    toReturn=Util.readFileAsString(cacheFile);
    js=new SoftReference<String>(toReturn);
  }
  return toReturn;
}
