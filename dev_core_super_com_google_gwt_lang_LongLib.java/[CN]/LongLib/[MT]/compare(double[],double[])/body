{
  if (eq(a,b)) {
    return 0;
  }
  boolean nega=isNegative(a);
  boolean negb=isNegative(b);
  if (nega && !negb) {
    return -1;
  }
  if (!nega && negb) {
    return 1;
  }
  assert(nega == negb);
  if (isNegative(sub(a,b))) {
    return -1;
  }
 else {
    return 1;
  }
}
