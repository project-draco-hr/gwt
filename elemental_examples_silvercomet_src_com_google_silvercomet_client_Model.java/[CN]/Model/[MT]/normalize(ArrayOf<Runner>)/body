{
  runners.sort(new CanCompare<Runner>(){
    @Override public int compare(    Runner a,    Runner b){
      return a.time() - b.time();
    }
  }
);
  for (int i=0, n=runners.length(); i < n; ++i) {
    runners.get(i).setPlace(i + 1);
  }
  return runners;
}
