{
  if (BOUND_CHECK) {
    checkCriticalPositionIndexes(start,end,size);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalPositionIndexes(start,end,size);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
