{
  if (API_CHECK) {
    checkCriticalArgument(expression);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalArgument(expression);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
