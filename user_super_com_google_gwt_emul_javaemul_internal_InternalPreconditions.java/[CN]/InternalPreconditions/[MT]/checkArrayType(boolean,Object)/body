{
  if (TYPE_CHECK) {
    checkCriticalArrayType(expression,errorMessage);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalArrayType(expression,errorMessage);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
