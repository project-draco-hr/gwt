{
  if (API_CHECK) {
    checkCritcalState(expression);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCritcalState(expression);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
