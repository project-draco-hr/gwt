{
  if (API_CHECK) {
    checkCriticalState(expression);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalState(expression);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
