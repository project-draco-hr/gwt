{
  if (API_CHECK) {
    checkCriticalState(expression,errorMessage);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalState(expression,errorMessage);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
