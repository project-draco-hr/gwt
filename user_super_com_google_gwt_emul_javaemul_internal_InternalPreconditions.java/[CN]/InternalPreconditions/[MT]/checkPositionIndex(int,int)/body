{
  if (BOUND_CHECK) {
    checkCriticalPositionIndex(index,size);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalPositionIndex(index,size);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
