{
  if (BOUND_CHECK) {
    checkCriticalElementIndex(index,size);
  }
 else   if (CHECKED_MODE) {
    try {
      checkCriticalElementIndex(index,size);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
