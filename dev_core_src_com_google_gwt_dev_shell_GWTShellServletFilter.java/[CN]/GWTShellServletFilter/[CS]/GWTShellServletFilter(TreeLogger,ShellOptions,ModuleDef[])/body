{
  this.logger=logger;
  this.options=options;
  for (  ModuleDef moduleDef : moduleDefs) {
    String scriptName=moduleDef.getDeployTo() + moduleDef.getName() + ".nocache.js";
    autogenScripts.put(scriptName,moduleDef);
  }
}
