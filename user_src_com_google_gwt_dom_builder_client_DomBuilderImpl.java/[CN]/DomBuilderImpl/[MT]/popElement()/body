{
  Element toRet=getCurrentElement();
  int itemCount=stackElements.size();
  stackElements.remove(itemCount - 1);
  if (itemCount == 1) {
    currentElement=null;
  }
 else {
    currentElement=stackElements.get(itemCount - 2);
  }
}
