{
  boolean fixedPoint;
  do {
    compilerContext.getLibraryWriter().markReboundTypesProcessed(getTypeNames(gatherReboundTypes(rpo)));
    fixedPoint=runGenerators();
  }
 while (!fixedPoint);
  for (  Entry<String,File> entry : ResourceGeneratorUtilImpl.getGeneratedFilesByName().entrySet()) {
    String resourcePath=entry.getKey();
    File resourceFile=entry.getValue();
    compilerContext.getLibraryWriter().addBuildResource(FileResource.of(resourcePath,resourceFile));
  }
}
