{
  String str;
  str=NumberFormat.getFormat("#.#").format(0);
  assertTrue(str.equals("0"));
  str=NumberFormat.getFormat("#.").format(0);
  assertTrue(str.equals("0,"));
  str=NumberFormat.getFormat(".#").format(0);
  assertTrue(str.equals(",0"));
  str=NumberFormat.getFormat("#").format(0);
  assertTrue(str.equals("0"));
  str=NumberFormat.getFormat("#0.#").format(0);
  assertTrue(str.equals("0"));
  str=NumberFormat.getFormat("#0.").format(0);
  assertTrue(str.equals("0,"));
  str=NumberFormat.getFormat("#.0").format(0);
  assertTrue(str.equals(",0"));
  str=NumberFormat.getFormat("#").format(0);
  assertTrue(str.equals("0"));
}
