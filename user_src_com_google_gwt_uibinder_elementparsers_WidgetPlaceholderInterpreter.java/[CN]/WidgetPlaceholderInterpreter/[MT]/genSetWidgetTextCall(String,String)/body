{
  if (elementIsHasText.contains(elementHolder)) {
    uiWriter.addInitStatement("%s.setText(((com.google.gwt.user.client.Element)%s).getInnerText());",childField,elementHolder);
  }
  if (elementIsHasHTML.contains(elementHolder)) {
    uiWriter.addInitStatement("%s.setHTML(((com.google.gwt.user.client.Element)%s).getInnerHTML());",childField,elementHolder);
  }
}
