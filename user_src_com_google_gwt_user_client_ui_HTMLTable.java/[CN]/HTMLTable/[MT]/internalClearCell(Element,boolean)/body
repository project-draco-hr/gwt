{
  Element maybeChild=DOM.getFirstChild(td);
  Widget widget=null;
  if (maybeChild != null) {
    widget=getWidget(maybeChild);
  }
  if (widget != null) {
    removeWidget(widget);
    return true;
  }
 else {
    if (clearInnerHTML) {
      DOM.setInnerHTML(td,"");
    }
    return false;
  }
}
