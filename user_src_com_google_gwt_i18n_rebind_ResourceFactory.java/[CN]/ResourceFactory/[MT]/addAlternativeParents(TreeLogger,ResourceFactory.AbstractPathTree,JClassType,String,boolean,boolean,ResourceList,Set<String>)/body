{
  if (tree != null) {
    for (int i=0; i < tree.numChildren(); i++) {
      ResourceFactory.AbstractPathTree child=tree.getChild(i);
      addResources(logger,child,child.getJClassType(clazz),locale,useAlternativeParents,isConstants,resources,seenPaths);
    }
  }
}
