{
  deletedCompilationUnitNames.clear();
  deletedCompilationUnitNames.addAll(Sets.difference(this.allCompilationUnitNames,newAllCompilationUnitNames));
  logger.log(TreeLogger.DEBUG,"caching list of known deleted compilation units " + deletedCompilationUnitNames);
  this.allCompilationUnitNames.clear();
  this.allCompilationUnitNames.addAll(newAllCompilationUnitNames);
}
