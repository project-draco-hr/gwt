{
  if (newPlace instanceof ProxyPlace) {
    ProxyPlace proxyPlace=(ProxyPlace)newPlace;
    if (proxyPlace.getOperation() != Operation.CREATE && proxyPlace.getProxyId().getProxyClass().equals(proxyType)) {
      select(cast(proxyPlace));
      return;
    }
  }
  select(null);
}
