{
  if (newPlace instanceof ProxyPlace) {
    ProxyPlace proxyPlace=(ProxyPlace)newPlace;
    if (proxyPlace.getOperation() != Operation.CREATE && proxyPlace.getProxyClass().equals(proxyClass)) {
      select(cast(proxyPlace));
      return;
    }
  }
  select(null);
}
