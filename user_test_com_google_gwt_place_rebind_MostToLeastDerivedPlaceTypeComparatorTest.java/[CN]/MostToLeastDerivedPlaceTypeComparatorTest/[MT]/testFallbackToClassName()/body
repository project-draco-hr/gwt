{
  JClassType[] places={place1,place2,place3,place4,place5};
  for (int i=0; i < places.length - 1; i++) {
    assertEquals(-1,(int)Math.signum(comparator.compare(places[i],places[i + 1])));
    assertEquals(1,(int)Math.signum(comparator.compare(places[i + 1],places[i])));
  }
}
