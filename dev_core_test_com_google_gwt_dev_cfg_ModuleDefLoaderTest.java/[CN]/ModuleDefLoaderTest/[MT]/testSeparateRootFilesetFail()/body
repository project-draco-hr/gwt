{
  TreeLogger logger=TreeLogger.NULL;
  try {
    ModuleDefLoader.loadFromClassPath(logger,"com.google.gwt.dev.cfg.testdata.separate.filesetone.FileSetOne",compilerContext,false,false);
    fail("Expected a fileset loaded as the root of a module tree to fail, but it didn't.");
  }
 catch (  UnableToCompleteException e) {
  }
}
