{
  SimpleEntityProxyId<?> id=(SimpleEntityProxyId<?>)proxy;
  if (id.isEphemeral()) {
    return id.getClientId() + EPHEMERAL_SEPARATOR + getHistoryToken(proxy.getProxyClass());
  }
 else {
    return id.getServerId() + TOKEN_SEPARATOR + getHistoryToken(proxy.getProxyClass());
  }
}
