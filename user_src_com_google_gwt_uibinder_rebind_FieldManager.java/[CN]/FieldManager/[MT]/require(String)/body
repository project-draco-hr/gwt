{
  FieldWriter fieldWriter=lookup(fieldName);
  if (fieldWriter == null) {
    throw new RuntimeException(String.format("The required field \"%s\" doesn't exist.",fieldName));
  }
  return fieldWriter;
}
