{
  Collection<FieldWriter> fields=fieldsMap.values();
  for (  FieldWriter field : fields) {
    if (!field.write(writer)) {
      die(NO_DEFAULT_CTOR_ERROR,field.getType().getQualifiedSourceName(),ownerTypeName,field.getType().getName());
    }
  }
}
