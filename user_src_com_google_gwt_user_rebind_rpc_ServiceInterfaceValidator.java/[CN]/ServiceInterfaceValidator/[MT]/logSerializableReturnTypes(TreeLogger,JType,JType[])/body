{
  logger=logger.branch(TreeLogger.SPAM,"Return type " + returnType.getQualifiedSourceName() + " can only be one of the following serializable types: ",null);
  logSerializableTypes(logger,serializableTypes);
}
