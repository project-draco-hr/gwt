{
  TreeLogger logger=rootLogger.branch(TreeLogger.SPAM,"Validating service interface '" + serviceIntf.getQualifiedSourceName() + "'",null);
  typeToFinalInstanceFieldCache.clear();
  if (!validServiceInterface(logger)) {
    return false;
  }
  if (!validAsyncServiceInterface(logger)) {
    return false;
  }
  return true;
}
