{
  logger=logger.branch(TreeLogger.SPAM,"Exception type " + ex.getQualifiedSourceName() + " can only be one of the following serializable types: ",null);
  logSerializableTypes(logger,serializableTypes);
}
