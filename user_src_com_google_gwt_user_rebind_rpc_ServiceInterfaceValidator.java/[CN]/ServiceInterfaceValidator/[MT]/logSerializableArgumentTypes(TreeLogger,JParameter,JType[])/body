{
  logger=logger.branch(TreeLogger.SPAM,"Argument '" + param.getName() + "' of type '"+ param.getType().getQualifiedSourceName()+ "' can only be serialized as: ",null);
  logSerializableTypes(logger,serializableTypes);
}
