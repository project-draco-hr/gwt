{
  List<String> statements=call_putAttributeStrings(new String[]{"a","b"},impl);
  assertEquals(1,statements.size());
  assertEquals("dtPutAttribute(\"0/0 attr\", new String[] {a, b});",statements.get(0));
}
