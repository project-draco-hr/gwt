{
  PropertyOracle props=new BindingProperties(new BindingProperty[0],new String[0],ConfigurationProperties.EMPTY).toPropertyOracle();
  return new SerializableTypeOracleBuilder(logger,new MockContext(to,props));
}
