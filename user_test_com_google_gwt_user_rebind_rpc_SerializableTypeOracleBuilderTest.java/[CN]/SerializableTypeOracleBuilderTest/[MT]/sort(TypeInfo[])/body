{
  Arrays.sort(typeInfos,new Comparator<TypeInfo>(){
    @Override public int compare(    TypeInfo ti1,    TypeInfo ti2){
      if (ti1 == ti2) {
        return 0;
      }
      return ti1.sourceName.compareTo(ti2.sourceName);
    }
  }
);
}
