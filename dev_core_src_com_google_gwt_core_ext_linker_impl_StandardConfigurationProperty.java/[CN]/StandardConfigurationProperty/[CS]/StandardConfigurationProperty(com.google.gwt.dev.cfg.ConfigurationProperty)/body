{
  name=p.getName();
  values=p.getValues();
  if (values == null) {
    throw new IllegalArgumentException("values is null");
  }
  if (!p.allowsMultipleValues() && values.size() != 1) {
    throw new IllegalArgumentException("p is single-valued but values.size != 1");
  }
}
