{
  StringBuffer code=new StringBuffer();
  code.append("class Buggy {\n");
  code.append("  native void jsniMeth(long x) /*-{ return; }-*/;\n");
  code.append("}\n");
  shouldGenerateWarning(code,2,"Parameter 'x': 'long' is an opaque, non-numeric value in JS code");
}
