{
  long startTime=System.currentTimeMillis();
  try {
    testRun();
  }
 catch (  Exception ex) {
    ex.printStackTrace();
    System.err.println("Exception message: " + ex.getMessage());
  }
  System.out.println("AppCacheWarmer: ran at " + new Date() + " for "+ (System.currentTimeMillis() - startTime)+ "ms, #Requests fetched: "+ RequestObject.fetchedRequests);
}
