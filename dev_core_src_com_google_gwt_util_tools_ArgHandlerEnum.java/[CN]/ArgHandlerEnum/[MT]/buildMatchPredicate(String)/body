{
  return new Predicate<Enum<?>>(){
    @Override public boolean apply(    Enum<?> t){
      if (allowAbbreviation && value.length() >= ABBREVIATION_MIN_SIZE) {
        return t.name().startsWith(value);
      }
      return t.name().equals(value);
    }
  }
;
}
