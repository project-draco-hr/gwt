{
synchronized (clientStatusesLock) {
    if (testBlocks.size() > 0) {
      throw new IllegalStateException("Cannot change number of clients after adding tests");
    }
    this.numClients=numClients;
  }
}
