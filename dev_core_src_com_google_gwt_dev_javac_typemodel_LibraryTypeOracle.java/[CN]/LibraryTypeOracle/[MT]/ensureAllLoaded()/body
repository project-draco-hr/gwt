{
  if (allLoaded) {
    return;
  }
  allLoaded=true;
  for (  String typeName : compilerContext.getLibraryGroup().getCompilationUnitTypeSourceNames()) {
    findType(typeName);
  }
  for (  String superSourceTypeName : compilerContext.getLibraryGroup().getSuperSourceCompilationUnitTypeSourceNames()) {
    findType(superSourceTypeName);
  }
}
