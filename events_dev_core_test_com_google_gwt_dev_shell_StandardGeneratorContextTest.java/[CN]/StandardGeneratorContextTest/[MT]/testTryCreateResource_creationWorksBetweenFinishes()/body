{
  genCtx.finish(mockLogger);
  testTryCreateResource_normalCompletionWithoutSubDir();
  genCtx.finish(mockLogger);
  testTryCreateResource_normalCompletionWithoutSubDir();
  genCtx.finish(mockLogger);
}
