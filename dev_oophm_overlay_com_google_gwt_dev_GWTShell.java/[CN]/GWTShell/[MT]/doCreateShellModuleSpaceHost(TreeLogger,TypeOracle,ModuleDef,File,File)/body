{
  Util.recursiveDelete(options.getShellBaseWorkDir(moduleDef),true);
  return new ShellModuleSpaceHost(logger,typeOracle,moduleDef,options.getGenDir(),options.getShellPrivateGenDir(moduleDef),doCreateArtifactAcceptor(moduleDef));
}
