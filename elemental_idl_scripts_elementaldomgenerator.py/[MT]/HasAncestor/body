def HasAncestor(interface, names_to_match, database):
    for parent in interface.parents:
        if ((parent.type.id in names_to_match) or (database.HasInterface(parent.type.id) and HasAncestor(database.GetInterface(parent.type.id), names_to_match, database))):
            return True
    return False
