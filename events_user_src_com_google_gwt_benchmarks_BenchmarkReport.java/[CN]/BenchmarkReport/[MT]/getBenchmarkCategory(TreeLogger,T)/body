{
  String category=getSimpleMetaData(element,GWT_BENCHMARK_CATEGORY);
  if (category != null && deprecationBranch != null) {
    deprecationBranch.log(TreeLogger.WARN,GWT_BENCHMARK_CATEGORY + " has " + "been deprecated with no replacement.",null);
  }
  return category;
}
