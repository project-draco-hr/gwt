{
  sw.println("protected void traverseEditors(%s<String> paths) {",List.class.getName());
  sw.indentln("%s.traverseEditor(getEditor(), \"\", paths);",getEditorDelegate(model.getRootData()));
  sw.println("}");
}
