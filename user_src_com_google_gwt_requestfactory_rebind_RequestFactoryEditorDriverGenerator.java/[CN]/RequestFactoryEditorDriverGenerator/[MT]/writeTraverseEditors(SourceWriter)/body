{
  sw.println("protected void traverseEditors(%s<String> paths) {",List.class.getName());
  sw.println("  %s.traverseEditor(getEditor(), \"\", paths);",getEditorDelegate(model.getProxyType(),model.getEditorType()));
  sw.println("}");
}
