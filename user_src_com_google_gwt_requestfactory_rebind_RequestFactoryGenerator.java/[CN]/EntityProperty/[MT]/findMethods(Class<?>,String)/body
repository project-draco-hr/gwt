{
  List<Method> toReturn=new ArrayList<Method>();
  for (  Method meth : domainType.getDeclaredMethods()) {
    if (meth.getName().equals(name) && (meth.getModifiers() & Modifier.PUBLIC) != 0) {
      toReturn.add(meth);
    }
  }
  return toReturn;
}
