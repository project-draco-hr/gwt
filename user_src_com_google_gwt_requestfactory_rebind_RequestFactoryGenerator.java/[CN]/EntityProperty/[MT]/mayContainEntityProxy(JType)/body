{
  JClassType paramClass=paramType.isClassOrInterface();
  if (paramClass == null) {
    return false;
  }
  return entityProxyType.isAssignableFrom(paramClass) || listType.isAssignableFrom(paramClass) || setType.isAssignableFrom(paramClass);
}
