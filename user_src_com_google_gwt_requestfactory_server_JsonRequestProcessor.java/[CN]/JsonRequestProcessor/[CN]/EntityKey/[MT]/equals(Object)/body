{
  if (!(ob instanceof EntityKey)) {
    return false;
  }
  EntityKey other=(EntityKey)ob;
  return (encodedId.equals(other.encodedId)) && (isFuture == other.isFuture) && (proxyType.equals(other.proxyType));
}
