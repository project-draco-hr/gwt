{
  Object returnValue=domainMethod.invoke(domainObject,args);
  if (returnValue == null && domainMethod.getReturnType() != Void.class) {
    throw new IllegalStateException(domainMethod + " with arguments " + args+ " is returning null, even though its return type is "+ domainMethod.getReturnType());
  }
  return returnValue;
}
