{
  Class<?> entityClass=getEntityFromRecordAnnotation(entityKey.record);
  Object entityInstance=entityClass.getMethod("find" + entityClass.getSimpleName(),String.class).invoke(null,entityKey.id);
  JSONObject serializedEntity=serializeEntity(entityInstance,entityKey.record);
  return new SerializedEntity(entityInstance,serializedEntity);
}
