{
  String encodedId=isEntityReference(returnValue,propertyType);
  String keyRef=new EntityKey(encodedId,false,(Class<? extends EntityProxy>)propertyType).toString();
  addRelatedObject(keyRef,returnValue,castToRecordClass(propertyType),propertyContext.getProperty(propertyName));
  return keyRef;
}
