{
  storage=getStorage();
  if (storage == null) {
    return;
  }
  if (handler != null) {
    storage.removeStorageEventHandler(handler);
    handler=null;
  }
  if (handler2 != null) {
    storage.removeStorageEventHandler(handler2);
    handler2=null;
  }
  storage.clear();
}
