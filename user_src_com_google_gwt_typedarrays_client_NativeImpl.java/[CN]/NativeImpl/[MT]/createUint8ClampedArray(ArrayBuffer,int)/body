{
  if (checkUint8ClampedArraySupport()) {
    return Uint8ArrayNative.createClamped(buffer,byteOffset);
  }
 else {
    return Uint8ClampedArrayNativeEmul.create(buffer,byteOffset,buffer.byteLength() - byteOffset);
  }
}
