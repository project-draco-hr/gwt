{
  image.replaceElement(Document.get().createImageElement());
  Event.sinkEvents(image.getElement(),Event.ONLOAD);
  image.sinkEvents(Event.ONCLICK | Event.MOUSEEVENTS | Event.ONLOAD| Event.ONERROR| Event.ONMOUSEWHEEL);
}
