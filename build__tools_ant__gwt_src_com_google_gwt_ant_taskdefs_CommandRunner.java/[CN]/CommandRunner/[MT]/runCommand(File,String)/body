{
  ProcessBuilder pb=new ProcessBuilder(cmd);
  pb.directory(workDir);
  try {
    return pb.start();
  }
 catch (  IOException e) {
    throw new BuildException("Unable to launch command: " + makeCmdString(cmd),e);
  }
}
