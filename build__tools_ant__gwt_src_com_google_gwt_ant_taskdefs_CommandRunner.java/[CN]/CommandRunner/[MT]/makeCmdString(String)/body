{
  StringBuilder sb=new StringBuilder();
  for (  String arg : cmd) {
    if (sb.length() > 0) {
      sb.append(' ');
    }
    sb.append(arg);
  }
  String cmdString=sb.toString();
  return cmdString;
}
