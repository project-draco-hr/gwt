{
  if (idValue.getClass() == idType) {
    return idValue;
  }
  if (idValue.getClass() == String.class && idType == Long.class) {
    return new Long((String)idValue);
  }
  throw new IllegalArgumentException("id is of type: " + idValue.getClass() + ",  expected type: "+ idType);
}
