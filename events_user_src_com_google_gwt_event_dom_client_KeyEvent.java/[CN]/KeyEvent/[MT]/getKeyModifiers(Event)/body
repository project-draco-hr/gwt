{
  return (DOM.eventGetShiftKey(event) ? MODIFIER_SHIFT : 0) | (DOM.eventGetMetaKey(event) ? MODIFIER_META : 0) | (DOM.eventGetCtrlKey(event) ? MODIFIER_CTRL : 0)| (DOM.eventGetAltKey(event) ? MODIFIER_ALT : 0);
}
