{
  try {
    Class<? extends EntityProxy> found=Class.forName(typeToken,false,Thread.currentThread().getContextClassLoader()).asSubclass(EntityProxy.class);
    return (Class<P>)found;
  }
 catch (  ClassNotFoundException e) {
    return null;
  }
}
