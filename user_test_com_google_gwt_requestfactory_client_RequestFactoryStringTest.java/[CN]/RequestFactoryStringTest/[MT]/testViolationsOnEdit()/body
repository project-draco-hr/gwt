{
  delayTestFinish(5000);
  fooCreationRequest().fire(new Receiver<SimpleFooStringProxy>(){
    @Override public void onSuccess(    SimpleFooStringProxy returned){
      SimpleFooStringRequest context=req.simpleFooStringRequest();
      Request<SimpleFooStringProxy> editRequest=context.persistAndReturnSelf().using(returned);
      new FailFixAndRefire<SimpleFooStringProxy>(returned,context,editRequest).doTest();
    }
  }
);
}
