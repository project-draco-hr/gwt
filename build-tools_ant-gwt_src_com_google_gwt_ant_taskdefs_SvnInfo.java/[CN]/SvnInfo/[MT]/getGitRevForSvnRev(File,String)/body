{
  String output=CommandRunner.getCommandOutput(dir,"git","svn","find-rev","r" + svnRevision);
  output=output.trim();
  if (output.length() == 0) {
    throw new BuildException("git svn find-rev didn't give any answer");
  }
  return output;
}
