{
  if (table == null || !table.isActive(this)) {
    throw new IllegalStateException("compile job is not active: " + id);
  }
  finishedSteps++;
  table.publish(new Progress.Compiling(this,finishedSteps,totalSteps,stepMessage),getLogger());
}
