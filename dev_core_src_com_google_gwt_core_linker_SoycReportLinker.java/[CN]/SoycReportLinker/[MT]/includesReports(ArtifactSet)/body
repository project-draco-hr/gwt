{
  for (  SyntheticArtifact art : artifacts.find(SyntheticArtifact.class)) {
    if (art.getPartialPath().startsWith(ArtifactsOutputDirectory.COMPILE_REPORT_DIRECTORY + "/")) {
      return true;
    }
  }
  return false;
}
