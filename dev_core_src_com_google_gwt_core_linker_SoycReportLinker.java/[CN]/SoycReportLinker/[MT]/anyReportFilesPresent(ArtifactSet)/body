{
  String prefix="soycReport/" + ArtifactsOutputDirectory.COMPILE_REPORT_DIRECTORY + "/";
  for (  EmittedArtifact art : artifacts.find(EmittedArtifact.class)) {
    if (art.getPartialPath().startsWith(prefix)) {
      return true;
    }
  }
  return false;
}
