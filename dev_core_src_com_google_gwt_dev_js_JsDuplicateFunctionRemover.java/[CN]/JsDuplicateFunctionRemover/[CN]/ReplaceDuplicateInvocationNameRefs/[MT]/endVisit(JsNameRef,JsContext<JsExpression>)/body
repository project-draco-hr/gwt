{
  JsName orig=duplicateMap.get(x.getName());
  if (orig != null && x.getName() != null && x.getName().getEnclosing() == program.getScope()) {
    ctx.replaceMe(orig.makeRef(x.getSourceInfo()));
  }
}
