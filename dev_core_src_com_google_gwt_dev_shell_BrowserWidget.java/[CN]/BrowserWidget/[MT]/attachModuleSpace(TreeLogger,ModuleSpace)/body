{
  Object key=space.getKey();
  loadedModules.put(key,space);
  logger.log(TreeLogger.SPAM,"Loading module " + space.getModuleName() + " (id "+ key.toString()+ ")",null);
  space.onLoad(logger);
  toolbar.updateWebMode();
}
