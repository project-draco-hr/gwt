{
  Object key=space.getKey();
  loadedModules.put(key,space);
  moduleSpaceLogger.log(TreeLogger.SPAM,"Loading module " + space.getModuleName() + " (id "+ key.toString()+ ")",null);
  space.onLoad(moduleSpaceLogger);
  toolbar.updateWebMode();
}
