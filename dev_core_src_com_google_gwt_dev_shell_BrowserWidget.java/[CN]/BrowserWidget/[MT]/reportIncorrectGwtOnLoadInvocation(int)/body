{
  getHost().getLogger().log(TreeLogger.ERROR,"Not enough arguments (" + numArgs + ") passed to external.gwtOnLoad(), expected (3); "+ "your hosted mode bootstrap file may be out of date; "+ "if you are using -noserver try recompiling and redeploying "+ "your app");
}
