{
  Widget someWidget=new TextBox();
  Panel panel1=new SimplePanel();
  Panel panel2=new SimplePanel();
  panel1.add(someWidget);
  panel2.add(someWidget);
  RootPanel.get().setParent(null);
  RootPanel.get().setParent(null);
}
