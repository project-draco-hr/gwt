{
  if (listener == null) {
    codeServerPort=options.getCodeServerPort();
    listener=new BrowserListener(getTopLogger(),codeServerPort,new OophmSessionHandler(getTopLogger(),browserHost));
    listener.start();
    try {
      codeServerPort=listener.getSocketPort();
    }
 catch (    UnableToCompleteException e) {
    }
  }
}
