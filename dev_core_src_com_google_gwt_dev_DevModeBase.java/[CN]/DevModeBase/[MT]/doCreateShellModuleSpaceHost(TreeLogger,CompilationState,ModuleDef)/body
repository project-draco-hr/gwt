{
  Util.recursiveDelete(options.getShellBaseWorkDir(moduleDef),true);
  return new ShellModuleSpaceHost(logger,compilationState,moduleDef,options.getGenDir(),new File(options.getShellBaseWorkDir(moduleDef),"gen"),doCreateArtifactAcceptor(moduleDef));
}
