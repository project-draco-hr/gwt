{
  Set<String> events=new HashSet<String>();
  events.add("focus");
  events.add("blur");
  events.add("keydown");
  if (userEvents != null && userEvents.size() > 0) {
    events.addAll(userEvents);
  }
  return events;
}
