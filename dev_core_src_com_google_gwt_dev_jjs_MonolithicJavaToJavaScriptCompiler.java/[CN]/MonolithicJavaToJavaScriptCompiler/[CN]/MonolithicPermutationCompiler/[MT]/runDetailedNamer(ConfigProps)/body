{
  Map<JsName,JsLiteral> internedTextByVariableName=null;
  if (shouldOptimize()) {
    internedTextByVariableName=JsLiteralInterner.exec(jprogram,jsProgram,(byte)(JsLiteralInterner.INTERN_ALL & (byte)(jprogram.typeOracle.isJsInteropEnabled() ? ~JsLiteralInterner.INTERN_STRINGS : ~0)));
  }
  JsVerboseNamer.exec(jsProgram,config);
  return internedTextByVariableName;
}
