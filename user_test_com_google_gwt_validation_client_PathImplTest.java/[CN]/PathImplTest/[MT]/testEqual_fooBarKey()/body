{
  PathImpl fooBarKey=new PathImpl().append("foo").appendKey("bar","key");
  PathImpl fooBarKeyCopy=new PathImpl().append("foo").appendKey("bar","key");
  assertEqualsAndHash(fooBarKey,fooBarKeyCopy);
}
