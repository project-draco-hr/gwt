{
  Array a=asArrayType(array);
  Array result=arraySlice(a,fromIndex,toIndex);
  initValues(a.getClass(),Util.getCastableTypeMap(a),Array.getElementTypeId(a),Array.getElementTypeClass(a),result);
  return Array.<T>asArray(result);
}
