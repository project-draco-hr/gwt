{
  super(node.getType(),node.getName().deepCopy(),null,node.getBlock() != null ? node.getBlock().deepCopy() : null);
  setSourceCodeLocation(node.getSourceCodeLocation());
  setRuntimeCondition(condition);
}
