{
  if (reuseTemps) {
    if (tempLocalsIndex < tempLocals.size()) {
      return tempLocals.get(tempLocalsIndex++);
    }
  }
  JLocal newTemp=program.createLocal(null,("$t" + localCounter++).toCharArray(),type,false,currentMethodBody);
  tempLocals.add(newTemp);
  return newTemp;
}
