{
  if (localIndex < tempLocals.size()) {
    return tempLocals.get(localIndex++);
  }
  JLocal newTemp=program.createLocal(null,("$t" + localIndex++).toCharArray(),type,false,currentMethodBody);
  tempLocals.add(newTemp);
  return newTemp;
}
