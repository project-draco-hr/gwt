{
  if (localIndex < tempLocals.size()) {
    return (JLocal)tempLocals.get(localIndex++);
  }
  JLocal newTemp=program.createLocal(null,("$t" + localIndex++).toCharArray(),program.getTypeVoid(),false,currentMethodBody);
  tempLocals.add(newTemp);
  return newTemp;
}
