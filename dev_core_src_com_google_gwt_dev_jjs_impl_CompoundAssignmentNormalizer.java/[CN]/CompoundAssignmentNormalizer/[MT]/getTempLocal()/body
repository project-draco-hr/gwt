{
  if (fLocalIndex < fTempLocals.size()) {
    return (JLocal)fTempLocals.get(fLocalIndex++);
  }
  JLocal newTemp=program.createLocal(("$t" + fLocalIndex++).toCharArray(),program.getTypeVoid(),false,fCurrentMethod);
  fTempLocals.add(newTemp);
  return newTemp;
}
