{
  if (localIndex < tempLocals.size()) {
    return (JLocal)tempLocals.get(localIndex++);
  }
  JLocal newTemp=program.createLocal(("$t" + localIndex++).toCharArray(),program.getTypeVoid(),false,currentMethod);
  tempLocals.add(newTemp);
  return newTemp;
}
