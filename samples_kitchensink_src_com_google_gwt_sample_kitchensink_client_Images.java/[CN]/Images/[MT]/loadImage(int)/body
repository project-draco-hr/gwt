{
  if (index < 0)   index=sImages.length - 1;
 else   if (index > sImages.length - 1)   index=0;
  curImage=index;
  loadingImage.setUrl("images/searching.gif");
  image.setUrl(sImages[curImage]);
}
