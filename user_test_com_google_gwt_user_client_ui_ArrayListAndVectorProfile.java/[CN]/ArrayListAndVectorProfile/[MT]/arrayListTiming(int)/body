{
  resetTimer();
  ArrayList v=new ArrayList();
  for (int i=0; i < num; i++) {
    v.add("hello");
  }
  timing("arrayList | add(" + num + ")");
  resetTimer();
  for (int k=0; k < num; k++) {
    v.get(k);
  }
  timing("arrayList | get(" + num + ")");
}
