{
  resetTimer();
  Vector v=new Vector();
  for (int i=0; i < num; i++) {
    v.add("hello");
  }
  timing("vector | add(" + num + ")");
  resetTimer();
  for (int k=0; k < num; k++) {
    v.get(k);
  }
  timing("vector | get(" + num + ")");
}
