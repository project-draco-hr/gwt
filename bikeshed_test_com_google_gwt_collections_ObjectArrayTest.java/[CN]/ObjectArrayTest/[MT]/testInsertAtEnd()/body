{
  final int N=10;
  MutableArray<Integer> b=createMutableArray();
  for (int i=0; i < N; ++i) {
    b.insert(b.size(),i);
  }
  for (int i=0; i < N; ++i) {
    assertEquals(i,b.get(i).intValue());
  }
}
