{
  final int N=10;
  MutableArray<Integer> b=createMutableArray();
  for (int i=0; i < N; ++i) {
    b.insert(0,i);
  }
  for (int i=0; i < N; ++i) {
    assertEquals(N - i - 1,b.get(i).intValue());
  }
}
