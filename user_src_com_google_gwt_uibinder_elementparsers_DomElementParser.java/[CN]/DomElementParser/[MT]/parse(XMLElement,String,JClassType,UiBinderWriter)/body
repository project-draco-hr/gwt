{
  HtmlInterpreter interpreter=new HtmlInterpreter(writer,fieldName,new HtmlMessageInterpreter(writer,fieldName));
  interpreter.interpretElement(elem);
  DomCursor cursor=writer.beginDomSection(fieldName);
  String html=elem.consumeOpeningTag() + elem.consumeInnerHtml(interpreter,cursor) + elem.getClosingTag();
  writer.endDomSection();
  writer.setFieldInitializer(fieldName,String.format("(%1$s) UiBinderUtil.fromHtml(\"%2$s\")",type.getQualifiedSourceName(),html));
}
