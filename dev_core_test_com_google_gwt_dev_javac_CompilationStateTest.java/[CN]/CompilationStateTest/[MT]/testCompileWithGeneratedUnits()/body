{
  assertUnitsChecked(state.getCompilationUnits());
  state.addGeneratedCompilationUnits(createTreeLogger(),getCompilationUnits(JavaSourceCodeBase.FOO));
  assertUnitsChecked(state.getCompilationUnits());
}
