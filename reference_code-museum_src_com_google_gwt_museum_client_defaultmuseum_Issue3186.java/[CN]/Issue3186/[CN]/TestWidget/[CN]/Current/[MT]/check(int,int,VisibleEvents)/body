{
  String errorReport=getErrorReport(x,y);
  if (errorReport == null) {
    eventToElement.get(event).setInnerHTML("<span style='color:green'>pass</span>");
  }
 else {
    fail(errorReport,event);
  }
}
