{
  if (remoteToken > 0) {
    try {
      browserManager.keepAlive(remoteToken,PING_KEEPALIVE_MS);
    }
 catch (    Exception e) {
      shell.getTopLogger().log(TreeLogger.WARN,"Unexpected exception keeping remote browser alive",e);
      return true;
    }
  }
  return false;
}
