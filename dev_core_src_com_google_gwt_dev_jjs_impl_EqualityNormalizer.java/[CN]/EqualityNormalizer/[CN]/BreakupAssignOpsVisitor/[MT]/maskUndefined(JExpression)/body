{
  JMethod maskMethod=program.getIndexedMethod("Cast.maskUndefined");
  JMethodCall lhsCall=new JMethodCall(program,lhs.getSourceInfo(),null,maskMethod,lhs.getType());
  lhsCall.addArg(lhs);
  return lhsCall;
}
