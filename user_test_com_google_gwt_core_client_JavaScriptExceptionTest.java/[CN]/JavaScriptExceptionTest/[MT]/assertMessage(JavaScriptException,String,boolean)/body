{
  String msg=e.getMessage();
  if (contains) {
    assertTrue("message contains '" + partOfMessage + "', but shouldn't: "+ msg,msg.contains(partOfMessage));
  }
 else {
    assertFalse("message does not contain '" + partOfMessage + "', but should: "+ msg,msg.contains(partOfMessage));
  }
}
