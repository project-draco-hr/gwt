{
  for (  JsniMethod jsniMethod : compiledClass.getJsniMethods()) {
    String body=Jsni.getJavaScriptForHostedMode(logger,jsniMethod);
    if (body == null) {
      continue;
    }
    shellJavaScriptHost.createNative(jsniMethod.location(),jsniMethod.line(),jsniMethod.name(),jsniMethod.paramNames(),body);
  }
}
