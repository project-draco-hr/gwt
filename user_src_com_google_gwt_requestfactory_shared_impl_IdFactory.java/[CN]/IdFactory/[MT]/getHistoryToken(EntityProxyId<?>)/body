{
  SimpleEntityProxyId<?> id=(SimpleEntityProxyId<?>)proxy;
  if (id.isEphemeral()) {
    return IdUtil.ephemeralId(id.getClientId(),getHistoryToken(proxy.getProxyClass()));
  }
 else {
    return IdUtil.persistedId(id.getServerId(),getHistoryToken(proxy.getProxyClass()));
  }
}
