def _extra_repr(self):
    extra = []
    if self.is_fc_getter:
        extra.append('get')
    if self.is_fc_setter:
        extra.append('set')
    if self.is_read_only:
        extra.append('readonly')
    if self.raises:
        extra.append('raises')
    return extra
