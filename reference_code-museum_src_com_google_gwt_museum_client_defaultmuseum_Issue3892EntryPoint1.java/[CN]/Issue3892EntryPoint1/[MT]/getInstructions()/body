{
  return "After all three modules have loaded (indicated by alert boxes), " + "click the buttons and verify that you see the expected results. " + "For each test, all three modules should preview the event (even if "+ "one of the modules cancels the event).";
}
