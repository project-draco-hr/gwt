{
  Type result=genericType;
  while (result instanceof TypeVariable<?> && resolvedTypes.get((TypeVariable<?>)result) != result && resolvedTypes.get((TypeVariable<?>)result) != null) {
    result=resolvedTypes.get((TypeVariable<?>)result);
  }
  return result;
}
