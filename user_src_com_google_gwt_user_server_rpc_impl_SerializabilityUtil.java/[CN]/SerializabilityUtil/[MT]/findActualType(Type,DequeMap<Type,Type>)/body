{
  Type result=genericType;
  while (result instanceof TypeVariable<?> && resolvedTypes.get(result) != result && resolvedTypes.get(result) != null) {
    result=resolvedTypes.get(result);
  }
  return result;
}
