{
  RequestFactoryInterfaceValidator v=new RequestFactoryInterfaceValidator(Logger.getAnonymousLogger(),new RequestFactoryInterfaceValidator.ClassLoaderLoader(getClass().getClassLoader()));
  v.validateRequestFactory(Factory.class.getName());
  assertFalse(v.isPoisoned());
}
