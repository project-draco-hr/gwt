{
  assertTrue("DashboardNotifier.devModeSessionEnd() without prior call to " + "DashboardNotifier.devModeSessionBegin()",started);
  assertFalse("DashboardNotifier.devModeSessionEnd() called more than once",ended);
  assertEquals("Wrong session passed to DashboardNotifier.devModeSessionEnd()",currentSession,session);
  ended=true;
}
