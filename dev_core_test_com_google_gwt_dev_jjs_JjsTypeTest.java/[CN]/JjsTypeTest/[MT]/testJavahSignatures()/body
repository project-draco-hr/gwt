{
  for (  JReferenceType type : severalTypes()) {
    assertSignaturesMatch(type,new Function<JReferenceType,String>(){
      @Override public String apply(      JReferenceType type){
        return type.getJavahSignatureName();
      }
    }
);
  }
}
