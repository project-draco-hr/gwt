{
  if (targetJsoType == null) {
    throw new ClassCastException("There is no known JavaScriptObject subtype that implements " + intfType.getCanonicalName() + ". Fix by adding an @SingleJsoImpl annotation to "+ intfType.getSimpleName()+ " or by referencing the concrete JSO type.");
  }
}
