{
  TreeLogger branch=reportErrors(logger,unit.getProblems(),unit.getResourceLocation(),unit.isError());
  if (branch != null) {
    Util.maybeDumpSource(branch,unit.getResourceLocation(),unit.getSource(),unit.getTypeName());
  }
}
