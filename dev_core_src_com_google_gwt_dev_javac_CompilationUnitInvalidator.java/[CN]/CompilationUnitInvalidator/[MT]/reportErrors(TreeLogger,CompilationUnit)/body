{
  TreeLogger branch=reportErrors(logger,unit.getProblems(),unit.getDisplayLocation(),unit.isError());
  if (branch != null) {
    Util.maybeDumpSource(branch,unit.getDisplayLocation(),unit.getSource(),unit.getTypeName());
  }
}
