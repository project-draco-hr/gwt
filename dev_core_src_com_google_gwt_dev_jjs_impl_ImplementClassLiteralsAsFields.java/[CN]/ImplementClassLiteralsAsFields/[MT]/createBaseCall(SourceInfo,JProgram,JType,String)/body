{
  String[] compoundName=maybeMangleJSOTypeName(program,type);
  JMethodCall call=new JMethodCall(info,null,program.getIndexedMethod(indexedMethodName),program.getStringLiteral(info,type.getPackageName()),getCompoundNameLiteral(program,info,compoundName));
  return call;
}
