{
  z=true;
  i=0;
  x=0;
  outer:   while (z) {
    ++x;
    inner:     while (z) {
      ++i;
      if (i == 1)       continue;
      if (i == 2)       continue outer;
      if (i == 3)       break;
      if (i == 4)       break outer;
    }
  }
  assertEquals(4,i);
  assertEquals(3,x);
}
