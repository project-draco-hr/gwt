{
  return "Click on first date box, see that date picker is displayed, " + "use arrow keys to navigate to second date box, select a date. " + "The second set includes an error display (one, shared by both "+ "fields). See that it notices  when you type garbage, and that "+ "its error message is cleared when you empty the field or provide "+ "a valid date.";
}
