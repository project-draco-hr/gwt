{
  T panel=createPanel();
  Label l=new Label("L");
  Label l2=new Label("L2");
  panel.add(l);
  try {
    panel.add(l2);
    fail("expected IllegalStateException");
  }
 catch (  IllegalStateException e) {
  }
}
