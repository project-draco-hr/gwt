{
  T panel=createPanel();
  Label l=new Label("L");
  panel.setWidget(l);
  panel.remove(l);
  assertNull(panel.getWidget());
  panel.setWidget(l);
  assertEquals(l,panel.getWidget());
  panel.clear();
  assertNull(panel.getWidget());
}
