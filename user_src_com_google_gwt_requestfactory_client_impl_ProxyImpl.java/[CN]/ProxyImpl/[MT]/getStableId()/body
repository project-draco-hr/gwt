{
  if (!isFuture) {
    return new EntityProxyIdImpl(getId(),getSchema(),false,jso.getRequestFactory().datastoreToFutureMap.get(getId(),getSchema()));
  }
  return new EntityProxyIdImpl(getId(),getSchema(),isFuture,null);
}
