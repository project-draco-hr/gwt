{
  if (property instanceof CollectionProperty) {
    V toReturn=(V)jso.getCollection((CollectionProperty)property);
    if (toReturn != null) {
      ((JsoCollection)toReturn).setDependencies(deltaValueStore,property,this);
    }
    return toReturn;
  }
  return jso.<V>get(property);
}
