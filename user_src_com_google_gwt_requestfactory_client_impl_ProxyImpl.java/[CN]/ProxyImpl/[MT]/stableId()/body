{
  if (!isFuture) {
    return new EntityProxyIdImpl<ProxyImpl>(encodedId(),getSchema(),false,jso.getRequestFactory().datastoreToFutureMap.get(encodedId(),getSchema()));
  }
  return new EntityProxyIdImpl(encodedId(),getSchema(),isFuture,null);
}
