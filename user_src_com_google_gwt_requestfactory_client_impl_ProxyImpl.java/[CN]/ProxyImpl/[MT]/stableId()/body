{
  if (isFuture) {
    return new EntityProxyIdImpl(encodedId(),schema(),isFuture,null);
  }
  return new EntityProxyIdImpl<ProxyImpl>(encodedId(),schema(),false,jso.getRequestFactory().datastoreToFutureMap.get(encodedId(),schema()));
}
