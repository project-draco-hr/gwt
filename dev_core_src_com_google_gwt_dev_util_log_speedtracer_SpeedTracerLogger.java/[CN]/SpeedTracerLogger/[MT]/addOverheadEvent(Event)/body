{
  Event overheadEvent=new Event(refEvent.devModeSession,refEvent,SpeedTracerEventType.OVERHEAD);
  overheadEvent.setStartsAfter(refEvent);
  overheadEvent.updateDuration();
  refEvent.extendDuration(overheadEvent);
}
