{
  JsBlock fragment=jsProgram.getFragment(fragmentNum).getGlobalBlock();
  assertFalse("function " + functionName + " should not be in fragment "+ fragmentNum,findFunctionIn(functionName,fragment));
}
