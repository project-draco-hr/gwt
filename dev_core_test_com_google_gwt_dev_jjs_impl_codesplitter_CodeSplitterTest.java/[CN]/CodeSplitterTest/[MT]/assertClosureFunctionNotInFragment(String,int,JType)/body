{
  JsBlock fragment=jsProgram.getFragmentBlock(fragmentNum);
  assertFalse("function " + functionName + " should not be in fragment "+ fragmentNum,findClosureFunctionIn(functionName,fragment,args));
}
