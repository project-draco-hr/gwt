{
  if (program == null) {
    assert(this instanceof JProgram);
    this.program=(JProgram)this;
  }
 else {
    this.program=program;
  }
  if (info != null) {
    this.info=info;
  }
 else {
    this.info=SourceInfo.UNKNOWN.makeChild("Unknown " + getClass().getSimpleName());
  }
}
