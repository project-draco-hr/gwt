{
  try {
    if (!startUp()) {
      return;
    }
    BootStrapPlatform.initGui();
    launchStartupUrls(getTopLogger());
    pumpEventLoop();
    shutDown();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
