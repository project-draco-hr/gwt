{
  String whyFailed=EmbeddedTomcatServer.start(getTopLogger(),getPort(),options,shouldAutoGenerateResources());
  if (whyFailed != null) {
    System.err.println(whyFailed);
    return -1;
  }
  return EmbeddedTomcatServer.getPort();
}
