{
  if (isRelink) {
    File outputDir=options.getShellPublicGenDir(module);
    outputDir.mkdirs();
    OutputFileSetOnDirectory outFileSet=new OutputFileSetOnDirectory(outputDir,"");
    linkerStack.produceOutput(logger,artifacts,Visibility.Public,outFileSet);
    outFileSet.close();
  }
}
