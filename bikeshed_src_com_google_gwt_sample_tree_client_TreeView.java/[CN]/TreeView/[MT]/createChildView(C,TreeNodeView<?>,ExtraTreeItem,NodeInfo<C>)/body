{
  TreeNodeView<C> childView=new TreeNodeView<C>(childValue,this,parentTreeNodeView,childItem,parentNodeInfo);
  NodeInfo<?> nodeInfo=model.getNodeInfo(childValue,childView);
  childView.initNodeInfo(nodeInfo);
  return childView;
}
