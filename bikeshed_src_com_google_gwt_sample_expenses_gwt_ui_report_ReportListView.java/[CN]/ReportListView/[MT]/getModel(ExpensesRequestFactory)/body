{
  return new ListModelAdapter<ReportKey>(){
    @Override protected void onRangeChanged(    ListRegistration reg,    int start,    int length){
      requests.reportRequest().findAllReports().forProperties(getProperties()).to(this).fire();
    }
  }
;
}
