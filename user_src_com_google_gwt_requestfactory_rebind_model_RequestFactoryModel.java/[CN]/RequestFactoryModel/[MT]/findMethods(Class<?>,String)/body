{
  List<Method> toReturn=new ArrayList<Method>();
  for (  Method method : domainType.getMethods()) {
    if (methodName.equals(method.getName()) && (method.getModifiers() & Modifier.PUBLIC) != 0) {
      toReturn.add(method);
    }
  }
  return toReturn;
}
