{
  try {
    compileToJs(Files.createTempDir(),"com.foo.SimpleModule",Lists.newArrayList(simpleModuleResource,brokenGwtCreateEntryPointResource),new MinimalRebuildCache(),emptySet,JsOutputOption.PRETTY);
    fail("Compile should have failed");
  }
 catch (  UnableToCompleteException e) {
  }
}
