{
  try {
    return new FileInputStream(backing);
  }
 catch (  IOException e) {
    logger.log(TreeLogger.ERROR,"Unable to read backing file for artifact " + getPartialPath(),e);
    throw new UnableToCompleteException();
  }
}
