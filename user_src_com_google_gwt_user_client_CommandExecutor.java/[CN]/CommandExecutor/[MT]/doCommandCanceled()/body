{
  Object cmd=iterator.getLast();
  iterator.remove();
  assert(cmd != null);
  if (cmd instanceof Command) {
    GWT.maybeReportUncaughtException(new CommandCanceledException((Command)cmd));
  }
 else   if (cmd instanceof IncrementalCommand) {
    GWT.maybeReportUncaughtException(new IncrementalCommandCanceledException((IncrementalCommand)cmd));
  }
  setExecuting(false);
  maybeStartExecutionTimer();
}
