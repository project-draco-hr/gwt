{
  if (that == null || that.getClass() != this.getClass()) {
    return false;
  }
  return labelExpr.equals(((JsPropertyInitializer)that).labelExpr) && valueExpr.equals(((JsPropertyInitializer)that).valueExpr);
}
