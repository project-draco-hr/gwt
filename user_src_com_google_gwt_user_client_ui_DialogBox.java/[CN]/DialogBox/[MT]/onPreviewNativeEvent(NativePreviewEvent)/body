{
  Event nativeEvent=event.getNativeEvent();
  if (!event.isCanceled() && nativeEvent.getTypeInt() == Event.ONMOUSEDOWN && isCaptionEvent(nativeEvent)) {
    nativeEvent.preventDefault();
  }
  super.onPreviewNativeEvent(event);
}
