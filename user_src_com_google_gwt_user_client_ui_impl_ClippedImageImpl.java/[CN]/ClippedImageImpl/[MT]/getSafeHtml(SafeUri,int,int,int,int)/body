{
  String style="width: " + width + "px; height: "+ height+ "px; background: url("+ url.asString()+ ") "+ "no-repeat "+ (-left + "px ")+ (-top + "px;");
  return getTemplate().image(clearImage,SafeStylesUtils.fromTrustedString(style));
}
