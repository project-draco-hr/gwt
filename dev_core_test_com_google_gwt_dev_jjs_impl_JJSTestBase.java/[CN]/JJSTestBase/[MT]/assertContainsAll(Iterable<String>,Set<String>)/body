{
  List<String> missing=FluentIterable.from(expectedMethodSnippets).filter(Predicates.not(Predicates.in(actualMethodSnippets))).toList();
  assertTrue(missing + "not contained in " + actualMethodSnippets,missing.size() == 0);
}
