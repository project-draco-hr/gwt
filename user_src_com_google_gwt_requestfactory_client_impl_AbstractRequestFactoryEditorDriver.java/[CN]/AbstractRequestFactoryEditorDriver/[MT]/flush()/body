{
  checkDelegate();
  checkSaveRequest();
  errors=new ArrayList<EditorError>();
  delegate.flush(errors);
  @SuppressWarnings("unchecked") Request<T> toReturn=(Request<T>)saveRequest;
  return toReturn;
}
