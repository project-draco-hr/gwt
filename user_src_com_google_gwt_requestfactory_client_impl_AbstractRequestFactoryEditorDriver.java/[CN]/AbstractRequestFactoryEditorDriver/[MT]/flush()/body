{
  checkDelegate();
  checkSaveRequest();
  errors=new ArrayList<EditorError>();
  delegate.flush(errors);
  @SuppressWarnings("unchecked") RequestObject<T> toReturn=(RequestObject<T>)saveRequest;
  return toReturn;
}
