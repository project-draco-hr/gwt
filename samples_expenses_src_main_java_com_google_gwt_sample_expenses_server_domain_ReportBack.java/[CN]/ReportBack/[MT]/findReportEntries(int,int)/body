{
  EntityManager em=entityManager();
  try {
    List<ReportBack> reportList=em.createQuery("select o from Report o").setFirstResult(0).setMaxResults(maxResults).getResultList();
    reportList.size();
    return reportList;
  }
  finally {
    em.close();
  }
}
