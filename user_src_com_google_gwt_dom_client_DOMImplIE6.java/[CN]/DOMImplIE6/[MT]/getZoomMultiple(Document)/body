{
  if (doc.getCompatMode().equals("CSS1Compat")) {
    return 1;
  }
 else {
    int bodyOffset=doc.getBody().getOffsetWidth();
    return bodyOffset == 0 ? 1 : doc.getBody().getParentElement().getOffsetWidth() / bodyOffset;
  }
}
