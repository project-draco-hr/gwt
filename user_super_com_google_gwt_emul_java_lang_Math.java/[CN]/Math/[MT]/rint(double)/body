{
  if (NativeMath.abs(x) < (double)(1L << 52)) {
    double mod2=x % 2;
    if ((mod2 == -1.5) || (mod2 == 0.5)) {
      x=NativeMath.floor(x);
    }
 else {
      x=NativeMath.round(x);
    }
  }
  return x;
}
