{
  TypeTokenResolver toReturn=d;
  toReturn.typeTokens=Collections.unmodifiableMap(toReturn.typeTokens);
  toReturn.referencedTypes=Collections.unmodifiableSet(new HashSet<String>(toReturn.typeTokens.values()));
  d=null;
  return toReturn;
}
