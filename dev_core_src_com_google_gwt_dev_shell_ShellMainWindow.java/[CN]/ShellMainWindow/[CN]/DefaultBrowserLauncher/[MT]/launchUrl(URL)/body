{
  Throwable caught=null;
  try {
    BrowserLauncher.browse(url.toExternalForm());
    return;
  }
 catch (  IOException e) {
    caught=e;
  }
catch (  URISyntaxException e) {
    caught=e;
  }
  getLogger().log(TreeLogger.ERROR,"Unable to launch default browser",caught);
}
