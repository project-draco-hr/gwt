{
  number*=255;
  if (number <= 0) {
    return "00";
  }
 else   if (number >= 255) {
    return "FF";
  }
  String toReturn=VALUES[(int)number / 16] + VALUES[(int)number % 16];
  return toReturn;
}
