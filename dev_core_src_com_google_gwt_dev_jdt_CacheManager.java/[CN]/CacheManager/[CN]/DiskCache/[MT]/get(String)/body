{
  if (cache.containsKey(key)) {
    return cache.get(key);
  }
  Object value=null;
  if (directory != null) {
    value=new FileEntry(key).getValue();
    cache.put(key,value);
  }
  return value;
}
