{
  Long oldTime=(Long)getCupLastUpdateTime(cup);
  if (oldTime != null) {
    if (oldTime.longValue() >= lastModified.longValue() && (!cup.isTransient())) {
      return true;
    }
  }
  return false;
}
