{
  final int tens=place / 10;
  final int ones=place % 10;
  if (ones == 1 && tens != 1) {
    return place + "st";
  }
 else   if (ones == 2 && tens != 1) {
    return place + "nd";
  }
 else   if (ones == 3 && tens != 1) {
    return place + "rd";
  }
 else {
    return place + "th";
  }
}
