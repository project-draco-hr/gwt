{
  StringBuffer b=new StringBuffer();
  b.append(getModulePrefix(logger,context));
  b.append(result.getJavaScript());
  b.append(getModuleSuffix(logger,context));
  EmittedArtifact toReturn=emitWithStrongName(logger,Util.getBytes(b.toString()),"",getCompilationExtension(logger,context));
  compilationPartialPaths.put(result,toReturn.getPartialPath());
  return toReturn;
}
