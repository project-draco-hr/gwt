{
  TreeMap<String,String> entries=selInfo.getPropMap();
  String strongName=selInfo.getStrongName();
  if (!propMapsByStrongName.containsKey(strongName)) {
    propMapsByStrongName.put(strongName,Lists.<Map<String,String>>create(entries));
  }
 else {
    propMapsByStrongName.put(strongName,Lists.add(propMapsByStrongName.get(strongName),entries));
  }
  return entries;
}
