{
  SpeedTracerLogger.start(CompilerEventType.GET_COMPILATION_UNITS);
  CompilationResults results=new WebModeCompilerFrontEnd(rebindPermOracle,linker).getCompilationUnitDeclarations(logger,seedTypeNames);
  SpeedTracerLogger.end(CompilerEventType.GET_COMPILATION_UNITS);
  return results;
}
