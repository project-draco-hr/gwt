{
  if (isJava) {
    return new DefaultFilters().customJavaFilter(includesList,excludesList,true,true);
  }
  return new DefaultFilters().customResourceFilter(includesList,excludesList,true,true);
}
