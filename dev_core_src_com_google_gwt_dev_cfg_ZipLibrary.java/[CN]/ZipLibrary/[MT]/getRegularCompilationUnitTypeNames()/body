{
  if (regularCompilationUnitTypeNames == null) {
    regularCompilationUnitTypeNames=Collections.unmodifiableSet(zipLibraryReader.readRegularCompilationUnitTypeNames());
  }
  return regularCompilationUnitTypeNames;
}
