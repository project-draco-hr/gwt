{
  Object version=getProperty(domainObject,"version");
  if (version == null) {
    return 0;
  }
  if (!(version instanceof Integer)) {
    die(null,"The getVersion() method on type %s did not return" + " int or Integer",domainObject.getClass().getCanonicalName());
  }
  return ((Integer)version).intValue();
}
