{
  String val=headers.get(header);
  if (val == null) {
    throw new IllegalArgumentException("set of unexpected header " + header);
  }
  if (!val.equals(value)) {
    throw new IllegalArgumentException("set of header " + header + " to unexpected value "+ value+ ", not "+ val);
  }
  headers.remove(header);
}
