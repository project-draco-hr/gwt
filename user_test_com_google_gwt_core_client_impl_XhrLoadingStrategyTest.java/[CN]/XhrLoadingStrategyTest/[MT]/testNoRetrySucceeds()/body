{
  MockXhrLoadingStrategy xls=new MockXhrLoadingStrategy(new MockXhr(200,"200 Ok",true,true));
  xls.startLoadingFragment(1,new LoadErrorHandler(){
    public void loadFailed(    Throwable reason){
      fail();
    }
  }
);
  xls.assertDone();
}
