{
  super.addView(view);
  requestFactory.employeeRequest().countEmployeesByDepartment(department).to(new Receiver<Long>(){
    public void onSuccess(    Long response){
      updateDataSize(response.intValue(),true);
    }
  }
).fire();
}
