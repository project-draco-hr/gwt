{
  StringBuilder sb=new StringBuilder();
  if (name != null) {
    sb.append(name);
  }
  if (isInIterable()) {
    sb.append('[');
    if (key != null) {
      sb.append(key);
    }
 else     if (index != null) {
      sb.append(index);
    }
    sb.append(']');
  }
  return sb.toString();
}
