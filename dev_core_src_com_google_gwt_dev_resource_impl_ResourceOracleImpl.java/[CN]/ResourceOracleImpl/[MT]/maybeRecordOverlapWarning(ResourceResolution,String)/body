{
  if (pathPrefixSet.mergePathPrefixes()) {
    return;
  }
  if (resourceResolution.getSourceModuleNames().size() > 1) {
    if (!overlapWarningsByModuleSet.containsKey(resourceResolution.getSourceModuleNames().toString())) {
      overlapWarningsByModuleSet.put(resourceResolution.getSourceModuleNames().toString(),String.format("Resource %s is included by multiple modules (%s).",resourcePath,Joiner.on(", ").join(resourceResolution.getSourceModuleNames())));
    }
  }
}
