{
  init(UI_XML_TYPED_WITH,generateRendererResource(declaredMethods));
  assertParseFailure("Expected failure due to missing render method.","renderer.OwnerClass.Renderer does not declare a render(SafeHtmlBuilder ...) method");
}
