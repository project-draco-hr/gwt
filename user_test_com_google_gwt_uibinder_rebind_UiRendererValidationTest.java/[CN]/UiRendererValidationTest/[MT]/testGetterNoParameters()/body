{
  initGetterTest();
  declaredMethods.append("    public DivElement getRoot();");
  init(UI_XML,generateRendererResource(declaredMethods));
  assertParseFailure("Expected failure due to getter with no parameters.","Getter getRoot must have exactly one parameter");
}
