{
  if (localIndex == tempLocals.size()) {
    JLocal newTemp=program.createLocal(null,("$e" + localIndex).toCharArray(),program.getTypeJavaLangObject(),false,currentMethod);
    tempLocals.add(newTemp);
  }
  ++localIndex;
}
