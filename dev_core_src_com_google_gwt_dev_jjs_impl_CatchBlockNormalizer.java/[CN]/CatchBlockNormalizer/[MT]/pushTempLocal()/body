{
  if (fLocalIndex == fTempLocals.size()) {
    JLocal newTemp=program.createLocal(("$e" + fLocalIndex).toCharArray(),program.getTypeJavaLangObject(),false,fCurrentMethod);
    fTempLocals.add(newTemp);
  }
  ++fLocalIndex;
}
