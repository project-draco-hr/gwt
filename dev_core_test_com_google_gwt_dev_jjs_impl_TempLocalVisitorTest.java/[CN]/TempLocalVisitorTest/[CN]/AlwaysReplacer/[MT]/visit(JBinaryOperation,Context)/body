{
  super.visit(x,ctx);
  if (x.getRhs() != null) {
    JExpression newRhs=accept(x.getRhs());
    if (newRhs != x.getRhs()) {
      ctx.replaceMe(new JBinaryOperation(x.getSourceInfo(),x.getType(),x.getOp(),x.getLhs(),newRhs));
    }
  }
  return false;
}
