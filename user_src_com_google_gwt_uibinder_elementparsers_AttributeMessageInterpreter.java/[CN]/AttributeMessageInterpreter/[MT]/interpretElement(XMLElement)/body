{
  MessagesWriter messages=writer.getMessages();
  for (  AttributeMessage am : messages.consumeAttributeMessages(elem)) {
    elem.setAttribute(am.getAttribute(),writer.tokenForStringExpression(am.getMessageUnescaped()));
  }
  return null;
}
