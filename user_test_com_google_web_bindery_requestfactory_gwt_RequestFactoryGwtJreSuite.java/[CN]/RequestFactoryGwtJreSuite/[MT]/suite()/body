{
  TestSuite suite=new TestSuite("requestfactory package tests that require the JRE and gwt-user");
  suite.addTestSuite(RequestFactoryModelTest.class);
  suite.addTestSuite(RfValidatorTest.class);
  return suite;
}
