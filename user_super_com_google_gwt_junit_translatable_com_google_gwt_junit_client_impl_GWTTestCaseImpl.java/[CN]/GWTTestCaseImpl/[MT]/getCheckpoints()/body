{
  if (checkPoints == null) {
    return new String[0];
  }
 else {
    int len=checkPoints.size();
    String[] result=new String[len];
    for (int i=0; i < len; ++i) {
      result[i]=checkPoints.get(i);
    }
    return result;
  }
}
