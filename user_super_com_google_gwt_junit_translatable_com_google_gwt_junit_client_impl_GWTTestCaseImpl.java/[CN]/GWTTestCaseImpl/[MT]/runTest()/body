{
  Throwable caught=null;
  testBeginMillis=System.currentTimeMillis();
  results=new TestResults();
  if (testCase == null) {
    reportResultsAndRunNextMethod(new RuntimeException("Could not instantiate the requested class"));
    return;
  }
  if (shouldCatchExceptions()) {
    GWT.setUncaughtExceptionHandler(this);
    try {
      testCase.runBare();
    }
 catch (    Throwable e) {
      caught=e;
    }
  }
 else {
    GWT.setUncaughtExceptionHandler(null);
    runBareTestCaseAvoidingExceptionDecl();
  }
  mainTestHasRun=true;
  if (caught != null) {
    reportResultsAndRunNextMethod(caught);
  }
 else   if (timer != null) {
  }
 else {
    reportResultsAndRunNextMethod(null);
  }
}
