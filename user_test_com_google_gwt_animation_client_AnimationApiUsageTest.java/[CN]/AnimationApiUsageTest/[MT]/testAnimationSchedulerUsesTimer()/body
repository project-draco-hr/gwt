{
  AnimationScheduler scheduler=AnimationScheduler.get();
  assertEquals("Expected timer implementation but got: " + scheduler.getClass().getName(),AnimationSchedulerImplTimer.class,scheduler.getClass());
}
