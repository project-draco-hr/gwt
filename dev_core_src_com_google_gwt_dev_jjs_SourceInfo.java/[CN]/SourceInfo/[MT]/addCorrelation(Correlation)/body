{
  if (!createDescendants) {
    return;
  }
  Axis axis=c.getAxis();
  if (correlations.containsKey(axis)) {
    throw new IllegalArgumentException("Correlation on axis " + axis + " has already been added. Call makeChild() first.");
  }
  correlations.put(axis,c);
}
