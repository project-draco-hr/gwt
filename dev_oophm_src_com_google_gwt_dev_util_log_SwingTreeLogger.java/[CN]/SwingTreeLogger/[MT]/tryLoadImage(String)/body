{
  URL url=SwingTreeLogger.class.getResource(simpleName);
  if (url != null) {
    try {
      ImageIcon image=new ImageIcon(url);
      return image;
    }
  finally {
    }
  }
 else {
    return null;
  }
}
