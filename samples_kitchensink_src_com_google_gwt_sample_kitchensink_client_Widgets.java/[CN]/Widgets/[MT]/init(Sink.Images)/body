{
  return new SinkInfo("Widgets","<h2>Basic Widgets</h2>" + "<p>GWT has all sorts of the basic widgets you would expect from any " + "toolkit.</p><p>Below, you can see various kinds of buttons, check boxes, "+ "radio buttons, and menus.</p>"){
    @Override public Sink createInstance(){
      return new Widgets(images);
    }
    @Override public String getColor(){
      return "#bf2a2a";
    }
  }
;
}
