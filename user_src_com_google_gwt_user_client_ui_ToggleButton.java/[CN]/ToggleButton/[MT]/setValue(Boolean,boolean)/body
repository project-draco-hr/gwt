{
  if (value == null) {
    value=Boolean.FALSE;
  }
  boolean oldValue=fireEvents ? isDown() : false;
  setDown(value);
  if (fireEvents) {
    ValueChangeEvent.fireIfNotEqual(this,oldValue,value);
  }
}
