{
  this.logger=logger.branch(TreeLogger.DEBUG,"Compiling " + perms.length + " permutations");
  this.astMemoryUsage=jjs.getAstMemoryUsage();
  for (  Permutation perm : perms) {
    tasks.add(new PermutationTask(this.logger,jjs,perm));
  }
}
