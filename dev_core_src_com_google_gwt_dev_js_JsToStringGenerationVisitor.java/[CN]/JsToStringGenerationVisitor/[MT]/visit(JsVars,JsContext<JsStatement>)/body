{
  _var();
  _space();
  boolean sep=false;
  for (Iterator<JsVar> iter=x.iterator(); iter.hasNext(); ) {
    sep=_sepCommaOptSpace(sep);
    JsVars.JsVar var=iter.next();
    accept(var);
  }
  return false;
}
