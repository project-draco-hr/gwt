{
  if (remoteObjectFromId.containsKey(refId)) {
    Reference<T> ref=remoteObjectFromId.get(refId);
    T toReturn=ref.get();
    if (toReturn != null) {
      return toReturn;
    }
  }
  return null;
}
