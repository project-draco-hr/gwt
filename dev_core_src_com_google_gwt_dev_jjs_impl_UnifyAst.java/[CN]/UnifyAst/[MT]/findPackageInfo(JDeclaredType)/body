{
  String pkg=type.getName();
  pkg=pkg.substring(0,pkg.lastIndexOf('.'));
  JDeclaredType pkgInfo=internalFindType(pkg + ".package-info",binaryNameBasedTypeLocator,false);
  if (pkgInfo != null) {
    program.addReferenceOnlyType(pkgInfo);
  }
  return pkgInfo;
}
