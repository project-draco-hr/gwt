{
  if (!program.typeOracle.isInteropEnabled()) {
    return false;
  }
  if (intf.isJsType()) {
    return true;
  }
  for (  JInterfaceType subIntf : intf.getImplements()) {
    if (isJsType(subIntf)) {
      return true;
    }
  }
  return false;
}
