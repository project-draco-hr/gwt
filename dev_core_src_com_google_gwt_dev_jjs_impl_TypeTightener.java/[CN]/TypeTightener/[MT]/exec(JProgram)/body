{
  Event optimizeEvent=SpeedTracerLogger.start(CompilerEventType.OPTIMIZE,"optimizer","TypeTightener");
  boolean didChange=new TypeTightener(program).execImpl();
  optimizeEvent.end("didChange","" + didChange);
  return didChange;
}
