{
  Collection<T> collection=map.get(x);
  if (collection == null) {
    return null;
  }
  T toReturn=null;
  for (  T elt : collection) {
    if (elt.isAbstract()) {
      continue;
    }
    if (toReturn != null) {
      return null;
    }
 else {
      toReturn=elt;
    }
  }
  return toReturn;
}
