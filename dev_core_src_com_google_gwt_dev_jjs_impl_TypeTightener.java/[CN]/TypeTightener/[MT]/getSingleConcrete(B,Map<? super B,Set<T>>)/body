{
  Set<T> set=map.get(x);
  if (set == null) {
    return null;
  }
  T toReturn=null;
  for (  T elt : set) {
    if (elt.isAbstract()) {
      continue;
    }
    if (toReturn != null) {
      return null;
    }
 else {
      toReturn=elt;
    }
  }
  return toReturn;
}
