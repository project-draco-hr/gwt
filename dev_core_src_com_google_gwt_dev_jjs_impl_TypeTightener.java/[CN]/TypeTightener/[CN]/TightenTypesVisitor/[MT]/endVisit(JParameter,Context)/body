{
  JMethod currentMethod=getCurrentMethod();
  if (program.codeGenTypes.contains(currentMethod.getEnclosingType()) || currentMethod.isExported() || currentMethod.isOrOverridesJsFunctionMethod()|| currentMethod.isOrOverridesJsTypeMethod()) {
    return;
  }
  tighten(x);
}
