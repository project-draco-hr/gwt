{
  JMethod method=x.getTarget();
  for (  JParameter param : method.params) {
    addAssignment(param,new JParameterRef(program,program.createSourceInfoSynthetic(RecordVisitor.class,"Fake assignment"),param));
  }
}
