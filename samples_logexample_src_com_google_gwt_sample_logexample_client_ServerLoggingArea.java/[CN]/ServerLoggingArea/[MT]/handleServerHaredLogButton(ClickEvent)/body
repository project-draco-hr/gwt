{
  loggingService.logOnServerUsingSharedLibrary(Level.SEVERE.toString(),"Message logged by server side code",new AsyncCallback<Void>(){
    public void onFailure(    Throwable caught){
      Window.alert("Logging on server failed");
    }
    public void onSuccess(    Void result){
    }
  }
);
}
