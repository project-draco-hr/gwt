{
  loggingService.logOnServer(Level.SEVERE.toString(),"server log",new AsyncCallback<Void>(){
    public void onFailure(    Throwable caught){
      Window.alert("Logging on server failed");
    }
    public void onSuccess(    Void result){
    }
  }
);
}
