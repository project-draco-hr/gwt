{
  CellPreviewEvent<T> event=new CellPreviewEvent<T>(nativeEvent,display,index,column,value,isCellEditing,isSelectionHandled);
  if (TYPE != null) {
    source.fireEvent(event);
  }
  return event;
}
