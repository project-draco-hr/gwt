{
  int code=37;
  for (  EmittedArtifact file : allFiles()) {
    if (file == null) {
      code=code * 17 + 37;
    }
 else {
      code=code * 17 + file.getPartialPath().hashCode();
    }
  }
  return code;
}
