{
  Map sigs=new TreeMap();
  for (int i=0; i < asyncMethods.length; ++i) {
    JMethod asyncMethod=asyncMethods[i];
    sigs.put(computeInternalSignature(asyncMethod),asyncMethod);
  }
  return sigs;
}
