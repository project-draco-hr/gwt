{
  String partialPath=".junit_symbolMaps/" + result.getStrongName() + STRONG_NAME_SUFFIX;
  EmittedArtifact symbolMapArtifact=emitBytes(logger,out.toByteArray(),partialPath);
  artifacts.add(symbolMapArtifact);
}
