{
  TreeItem option=parent.addItem(AbstractImagePrototype.create(image).getHTML() + " " + content.getName());
  itemWidgets.put(option,content);
  itemTokens.put(getContentWidgetToken(content),option);
}
