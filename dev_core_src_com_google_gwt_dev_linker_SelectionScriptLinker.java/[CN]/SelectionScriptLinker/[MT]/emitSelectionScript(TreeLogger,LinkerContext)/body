{
  String selectionScript=generateSelectionScript(logger,context);
  byte[] selectionScriptBytes=Util.getBytes(context.optimizeJavaScript(logger,selectionScript));
  doEmit(logger,context,selectionScriptBytes,context.getModuleName() + ".nocache.js");
}
