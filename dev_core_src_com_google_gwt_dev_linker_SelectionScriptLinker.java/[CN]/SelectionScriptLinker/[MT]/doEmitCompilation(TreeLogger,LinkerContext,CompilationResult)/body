{
  StringBuffer b=new StringBuffer();
  b.append(getModulePrefix(logger,context));
  b.append(result.getJavaScript());
  b.append(getModuleSuffix(logger,context));
  String partialPath=emitWithStrongName(logger,context,Util.getBytes(b.toString()),"",getCompilationExtension(logger,context));
  compilationPartialPaths.put(result,partialPath);
}
