{
  MockClock.run(new Runnable(){
    public void run(){
      TestTimer t=new TestTimer();
      t.schedule(100);
      t.cancel();
      MockClock.tick(101);
      assertFalse(t.hasRun);
    }
  }
);
}
