{
  for (int i=0; i < words.length; i++) {
    for (int j=0; j < words.length; j++) {
      GeneralEditDistance ed=g.getInstance(words[i]);
      specificAlgorithmVerify(ed,words[i],words[j],wordsDistances[i][j]);
    }
  }
}
