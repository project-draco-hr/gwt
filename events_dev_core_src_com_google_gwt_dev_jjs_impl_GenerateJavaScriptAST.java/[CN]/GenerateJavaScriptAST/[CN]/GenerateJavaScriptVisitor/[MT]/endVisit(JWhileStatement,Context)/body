{
  JsWhile stmt=new JsWhile();
  if (x.getBody() != null) {
    stmt.setBody((JsStatement)pop());
  }
 else {
    stmt.setBody(jsProgram.getEmptyStmt());
  }
  stmt.setCondition((JsExpression)pop());
  push(stmt);
}
