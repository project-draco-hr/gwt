{
  super.writeSource();
  deprecationBranch=logger.branch(TreeLogger.TRACE,"Scanning Benchmarks for deprecated annotations; " + "Please see " + Benchmark.class.getName() + " for more information.",null);
  generateEmptyFunc(getSourceWriter());
  implementZeroArgTestMethods();
  implementParameterizedTestMethods();
  generateAsyncCode();
  JUnitShell.getReport().addBenchmark(logger,deprecationBranch,getRequestedClass(),getTypeOracle());
}
