{
  if (id == null) {
    return null;
  }
  EntityProxyIdImpl key=new EntityProxyIdImpl(id,schema,false,requestFactory.datastoreToFutureMap.get(id,schema));
  return schema.create(records.get(key));
}
