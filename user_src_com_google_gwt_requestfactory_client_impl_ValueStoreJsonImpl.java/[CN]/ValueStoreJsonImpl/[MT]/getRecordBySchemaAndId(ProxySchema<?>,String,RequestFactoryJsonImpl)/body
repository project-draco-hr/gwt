{
  if (encodedId == null) {
    return null;
  }
  EntityProxyIdImpl key=new EntityProxyIdImpl(encodedId,schema,false,requestFactory.datastoreToFutureMap.get(encodedId,schema));
  return schema.create(records.get(key));
}
