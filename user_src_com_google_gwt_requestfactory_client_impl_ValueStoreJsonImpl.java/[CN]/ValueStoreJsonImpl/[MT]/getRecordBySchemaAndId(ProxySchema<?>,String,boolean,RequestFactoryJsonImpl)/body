{
  if (encodedId == null) {
    return null;
  }
  EntityProxyIdImpl<?> key=new EntityProxyIdImpl<EntityProxy>(encodedId,schema,isFuture,requestFactory.datastoreToFutureMap.get(encodedId,schema));
  return schema.create(records.get(key),isFuture);
}
