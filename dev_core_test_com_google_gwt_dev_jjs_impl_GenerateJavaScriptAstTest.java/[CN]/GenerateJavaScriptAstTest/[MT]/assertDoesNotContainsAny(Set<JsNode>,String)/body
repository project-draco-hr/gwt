{
  Set<String> remainingFunctions=Sets.newHashSet(functionNames);
  for (  JsFunction function : Iterables.filter(functionsForJsInlining,JsFunction.class)) {
    assertFalse(function.getName().getShortIdent() + " should not be considered for JsInliner",remainingFunctions.contains(function.getName().getShortIdent()));
  }
}
