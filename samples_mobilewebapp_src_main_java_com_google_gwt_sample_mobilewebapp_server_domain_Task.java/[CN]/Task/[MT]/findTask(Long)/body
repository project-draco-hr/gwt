{
  if (id == null) {
    return null;
  }
  EMF emf=EMF.get();
  Task task=emf.ofy().find(Task.class,id);
  if (task != null && task.userId.equals(currentUserId())) {
    return task;
  }
 else {
    return null;
  }
}
