{
  TextCell cell=new TextCell(){
    @Override public boolean consumesEvents(){
      return true;
    }
  }
;
  Column<String,String> column=new IdentityColumn<String>(cell);
  assertTrue(column.consumesEvents());
}
