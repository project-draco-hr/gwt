{
switch (writeOperation) {
case PERSIST:
    return creates;
case UPDATE:
  return updates;
default :
throw new IllegalStateException("unknow writeOperation " + writeOperation.getUnObfuscatedEnumName());
}
}
