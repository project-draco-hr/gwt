{
  int outerTimes=1;
switch (sbtype) {
case STRBLD:
    for (int i=0; i < outerTimes; i++) {
      appendWithStringBuilder(times);
    }
  break;
case STRBUF:
for (int i=0; i < outerTimes; i++) {
  appendWithStringBuffer(times);
}
break;
}
}
