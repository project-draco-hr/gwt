{
  if (target.getEnclosingType().isEnumOrSubclass() != null && (target.getName().equals("valueOf") || target.getName().equals("values"))) {
    blackListIfEnum(target.getEnclosingType(),info);
  }
}
