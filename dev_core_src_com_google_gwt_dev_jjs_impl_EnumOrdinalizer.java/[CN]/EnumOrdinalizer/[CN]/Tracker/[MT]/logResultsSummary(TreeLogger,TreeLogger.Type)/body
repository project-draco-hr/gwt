{
  if (logger != null) {
    logger=logger.branch(logType,"EnumOrdinalizer Results:");
    logger.branch(logType,"After pass " + (runCount + 1));
    logger.branch(logType,allEnumsOrdinalized.size() + " of " + allEnumsVisited.size()+ " ordinalized");
    return logger;
  }
  return null;
}
