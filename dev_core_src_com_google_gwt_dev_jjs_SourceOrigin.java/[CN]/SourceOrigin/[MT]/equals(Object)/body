{
  if (!(o instanceof SourceOrigin)) {
    return false;
  }
  SourceOrigin other=(SourceOrigin)o;
  return endPos == other.endPos && fileName.equals(other.fileName) && startLine == other.startLine && startPos == other.startPos;
}
