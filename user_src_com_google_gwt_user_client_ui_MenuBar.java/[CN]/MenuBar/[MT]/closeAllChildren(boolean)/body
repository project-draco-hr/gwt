{
  if (shownChildMenu != null) {
    shownChildMenu.onHide(focus);
    shownChildMenu=null;
    selectItem(null);
  }
  if (popup != null) {
    popup.hide();
  }
  if (focus && parentMenu != null) {
    parentMenu.focus();
  }
}
