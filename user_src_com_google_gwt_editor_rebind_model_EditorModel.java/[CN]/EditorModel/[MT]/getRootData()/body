{
  return new EditorData(logger.branch(TreeLogger.DEBUG,"Calculating root data for " + getEditorType().getParameterizedQualifiedSourceName()),null,EditorAccess.root(getEditorType()),"",null,null);
}
