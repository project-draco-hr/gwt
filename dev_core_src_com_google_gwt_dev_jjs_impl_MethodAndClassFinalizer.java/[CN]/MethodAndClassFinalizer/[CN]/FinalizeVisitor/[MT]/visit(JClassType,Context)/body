{
  if (!x.isFinal() && !isSubclassed.contains(x)) {
    x.setFinal(true);
    didChange=true;
  }
  for (int i=0; i < x.methods.size(); ++i) {
    JMethod method=x.methods.get(i);
    accept(method);
  }
  return false;
}
