{
  this.logger=logger;
  deprecationBranch=logger.branch(TreeLogger.INFO,"Scanning Benchmarks for deprecated annotations; " + "Please see " + Benchmark.class.getName() + " for more information.",null);
}
