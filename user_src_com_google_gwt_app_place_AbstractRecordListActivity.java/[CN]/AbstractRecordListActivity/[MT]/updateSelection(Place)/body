{
  if (newPlace instanceof ProxyPlace) {
    ProxyPlace proxyPlace=(ProxyPlace)newPlace;
    if (proxyPlace.getOperation() != Operation.CREATE && requests.getClass(proxyPlace.getProxy()).equals(proxyType)) {
      selectCoerced(newPlace);
      return;
    }
  }
  select(null);
}
