{
  for (  Property<?> property : getSchema().allProperties()) {
    if ((property != ProxyImpl.id) && (property != ProxyImpl.version) && (isDefined(property.getName()))) {
      return false;
    }
  }
  return true;
}
