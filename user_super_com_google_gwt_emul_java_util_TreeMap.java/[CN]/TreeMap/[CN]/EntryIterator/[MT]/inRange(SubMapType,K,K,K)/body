{
  if (type.toKeyValid()) {
    if (cmp.compare(key,toKey) >= 0) {
      return false;
    }
  }
  if (type.fromKeyValid()) {
    if (cmp.compare(key,fromKey) < 0) {
      return false;
    }
  }
  return true;
}
