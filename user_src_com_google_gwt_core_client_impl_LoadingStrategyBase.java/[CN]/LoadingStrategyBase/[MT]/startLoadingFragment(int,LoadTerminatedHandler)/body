{
  String url=gwtStartLoadingFragment(fragment,loadErrorHandler);
  if (url == null) {
    return;
  }
  RequestData request=new RequestData(url,loadErrorHandler,fragment,downloadStrategy,getMaxRetryCount());
  request.tryDownload();
}
