{
  long acc=0;
  long front=MIN_VALUE;
  int back=1;
  int swing=SIZE - 1;
  while (swing > 15) {
    acc=acc | ((i & front) >> swing) | ((i & back) << swing);
    swing--;
    front=front >> 1;
    back=back << 1;
  }
  return acc;
}
