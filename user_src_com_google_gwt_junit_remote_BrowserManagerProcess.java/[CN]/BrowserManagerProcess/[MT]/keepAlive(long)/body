{
synchronized (this) {
    try {
      process.exitValue();
    }
 catch (    IllegalThreadStateException e) {
      schedule(keepAliveMs);
      return true;
    }
  }
  cleanupBrowser();
  return false;
}
