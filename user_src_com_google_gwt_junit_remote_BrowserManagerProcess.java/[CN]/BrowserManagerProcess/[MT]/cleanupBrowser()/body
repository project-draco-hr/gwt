{
  boolean doCb=false;
synchronized (this) {
    if (!exited) {
      exited=true;
      exitValue=process.exitValue();
      schedule(0);
      doCb=true;
    }
  }
  if (doCb) {
    cb.childExited(token,exitValue);
  }
  return exitValue;
}
