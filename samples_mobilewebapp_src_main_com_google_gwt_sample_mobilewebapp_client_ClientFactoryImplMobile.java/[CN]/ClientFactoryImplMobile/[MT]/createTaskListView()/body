{
  ProvidesPresenter<TaskListView.Presenter,TaskListView> factory=new ProvidesPresenter<TaskListView.Presenter,TaskListView>(){
    @Override public Presenter getPresenter(    TaskListView view){
      TaskListActivity taskListActivity=new TaskListActivity(ClientFactoryImplMobile.this,false);
      taskListActivity.start(new AcceptsOneWidget(){
        @Override public void setWidget(        IsWidget w){
        }
      }
,getEventBus());
      return taskListActivity;
    }
  }
;
  return new MobileTaskListView(factory);
}
