{
  String selectionScript=generateSelectionScript(logger,context,artifacts);
  byte[] selectionScriptBytes=Util.getBytes(context.optimizeJavaScript(logger,selectionScript));
  return emitBytes(logger,selectionScriptBytes,context.getModuleName() + ".nocache.js");
}
