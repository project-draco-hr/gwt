def test_whitespace(self):
    gram = MANY('A')
    self._run_test(grammar=gram, text='A A  A', expected=None)
    self._run_test(grammar=gram, whitespace_rule=' ', text='A A  A', expected=['A', 'A', 'A'])
