{
  if (logger.isLoggable(TreeLogger.TRACE)) {
    logger.log(TreeLogger.TRACE,"Adding attribute  '" + attrName + "' to web app '"+ webapp.getName()+ "'",null);
  }
  webapp.getServletContext().setAttribute(attrName,attrValue);
}
