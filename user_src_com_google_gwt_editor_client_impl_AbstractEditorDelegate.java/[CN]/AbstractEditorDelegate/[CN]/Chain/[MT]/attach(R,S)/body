{
  AbstractEditorDelegate<R,S> subDelegate=createComposedDelegate();
  map.put(subEditor,subDelegate);
  @SuppressWarnings("unchecked") Editor<Object> temp=(Editor<Object>)subEditor;
  subDelegate.initialize(path + composedEditor.getPathElement(temp),object,subEditor);
}
