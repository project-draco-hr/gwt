{
  if (this.serializeMethod != null && this.serializeMethod != serializeMethod) {
    logger.log(TreeLogger.ERROR,"Type " + serializeeClass.getQualifiedSourceName() + " has more than one custom field serializer deserialize method, "+ this.serializeMethod.getReadableDeclaration()+ " and "+ serializeMethod.getReadableDeclaration(),null);
    return false;
  }
  this.serializeMethod=serializeMethod;
  return true;
}
