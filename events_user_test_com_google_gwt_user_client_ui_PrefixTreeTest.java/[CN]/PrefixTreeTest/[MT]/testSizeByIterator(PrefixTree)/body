{
  int count=0;
  for (final Iterator i=tree.iterator(); i.hasNext(); ) {
    i.next();
    count++;
  }
  assertTrue("Iteration count " + count + " did not match size "+ tree.size(),count == tree.size());
}
