{
  precompileTaskArgProcessor.processArgs("-workDir","/tmp","-draftCompile","com.google.gwt.dev.DevModule");
  assertTrue(defaultOptions.getOptimizationLevel() != handledOptions.getOptimizationLevel());
  assertNotEquals(defaultOptions.isAggressivelyOptimize(),handledOptions.isAggressivelyOptimize());
}
