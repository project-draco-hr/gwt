{
  List<JsStatement> expected=parser.parse(new JsProgram().getScope(),new StringReader(js),0);
  List<JsStatement> actual=parse(expected,true);
  ComparingVisitor.exec(expected,actual);
  actual=parse(expected,false);
  ComparingVisitor.exec(expected,actual);
}
